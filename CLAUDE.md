# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# PD-IMU

> **Read order**
> - **Operating quickly:** Commands → Architecture → Inductive Firewall → Gotchas.
> - **Reproducing a number:** Headline Results → script name in Commands.
> - **History, ablations, "what failed":** `findings.md` and `progress.md`. Don't cite older numbers from there as deployment results.
> - **Leakage rules + agent brief:** `AGENTS.md` (source of truth if it disagrees with this file).
> - **Paper context (SOTA, MCID, cross-dataset):** `paper.md` / `findings.md` / `review_report.md`.

## Objective

**First published UPDRS-III regression on WearGait-PD with strict inductive evaluation.** Predict Parkinson's motor severity (MDS-UPDRS Part III, range 0–132) from body-worn IMUs during gait/balance tasks. Dataset: 178 subjects (98 PD rows + 80 HC), 13 IMUs at 100 Hz. Corrected T3 target analyses exclude 3 PD rows with all 33 raw Part III subitems missing and treat raw subitem values outside 0-4 as missing.

**Paper framing (post-2026-04-28 leakage audit):** A cautionary benchmark. The original SSL ranking "breakthrough" (T1 CCC=0.868, T3 CCC=0.776) was almost entirely transductive leakage. Honest inductive ceilings are dramatically lower. Paper leads with: (a) first WearGait-PD UPDRS-III regression with proper inductive eval, (b) anatomy of the leakage, (c) realistic deployment ceilings.

## Headline Results (canonical — cite these only)

Pre-registered lockbox protocol: 5-fold for screening, single LOOCV for headline, 3 seeds × 89 PD folds.

| Target / metric | Pipeline | CCC | MAE | Pre-registration |
|---|---|---|---|---|
| **T1 (items 9-14, axial+truncal) — CANONICAL** | **`compose_t1_iter12_honest.py`** — single iter8 batch (20260430_143044) for all 6 items; no swaps | **0.6550** | 1.561 | `results/preregistration_t1_iter12_honest_*.json` |
| **T1 multi-task LGB chain — CANDIDATE 2026-05-05 (F65)** | **`run_t1_iter30b_lockbox.py --mode lockbox --variant V1_random`** — Stage1 Ridge(H&Y + cv_yrs + cv_sex + cv_dbs) + Stage2 sklearn RegressorChain(LGBMRegressor) jointly predicting items 9-14 residuals (train-mean centered); per-fold K=500 LGB-importance selector. Mean of 3 seeds (42, 1337, 7). | **0.7087** (raw +0.054 over 0.6550; bootstrap frac>0=0.872 vs iter12 honest — BELOW strict 0.95 gate) | 1.933 | `results/preregistration_t1_iter30b_V1_random_20260505_201626.json` (formula_sha256 `512ed04f...`); `results/lockbox_t1_iter30b_V1_random_20260505_211112.json` |
| **T1 multi-task 8-item auxiliary chain — CANDIDATE 2026-05-06 (F68, superseded by F70)** | **`run_t1_iter33b_8item_chain.py --mode lockbox`** — same Stage 1; Stage-2 RegressorChain on 8 items {9-14, 15, 18}. Mean of 3 seeds. Post-council audit: candidate, not canonical — cohort-matched paired bootstrap vs iter12-honest-on-N=93 frac>0=0.9376 < 0.95; FWER-adj n=3 p=0.063. | **0.7219** on N=93 (nominal frac>0=0.979 vs iter5 / 0.9376 vs iter12-on-N=93) | 1.843 | `results/lockbox_t1_iter33b_8item_20260506_071631.json` |
| **T1 hybrid: 8-item auxiliary chain × 3-base-learner ensemble — STRONGEST CANDIDATE 2026-05-06 PM (F70)** | **`run_t1_iter34_hybrid_8item_multibase.py --mode lockbox --n_workers 17`** — pre-registered as single POST-PUBLICATION REPLICATION TARGET (n=1 family-wise). Stage-1 = same Ridge; Stage-2 = average of RegressorChain over 8 items × {LGB, XGB-hist, ExtraTrees}. Mean of 3 seeds, 17-worker ProcessPool fold-parallelism (16-min wall, 10× speedup over iter33-C serial). Audit (F73/F-iter44): P1 scrambled-label z=5.22, P4 pass; P2 noisy-test is not a positive point-estimate leak but remains bootstrap-caveated (`results/iter34_p2_robustness_20260508.json`: max point delta +0.0389 < +0.05, max bootstrap upper +0.0857 > +0.05). Aux-label/order audit: `results/t1_iter34_aux_order_audit.json` finds invalid item15 can be upstream of T1 items in 2/3 random-chain seeds, but all-base 5-fold stale-vs-valid common-SID delta is only `-0.0008` CCC (materiality flag false). LOSO (F72): two-way mean 0.4564 (NLS→WPD 0.6293, WPD→NLS 0.2835). | **0.7366** on N=93 (raw +0.081 over iter12-honest-on-N=93 0.6554; vs iter5 frac>0=**0.9958** clears Bonferroni n=8/9; vs iter12-honest-on-N=93 frac>0=**0.9714** clears strict 0.95; vs iter33-B paired frac>0=0.965, Δ̄=+0.015) | 1.731 | `results/preregistration_t1_iter34_hybrid_20260506_135932.json` (formula_sha256-bound); `results/lockbox_t1_iter34_hybrid_20260506_141720.json`; `results/iter34_vs_iter12_honest_n93_paired_2026_05_06.json`; `results/iter34_leakage_audit_20260506_143922.json` (audit); `results/iter34_p2_robustness_20260508.json` (P2 robustness); `results/t1_iter34_aux_order_audit.json` (aux-order/impact audit); `results/iter34_loso_2026_05_06.json` (LOSO); `results/iter34_figures/*.png` (5 paper figures); paper.md sections 3.8/4.11/4.12/5.5 updated |
| **T3 corrected target (total UPDRS-III) — CANONICAL AUDIT TRUTH** | **`run_t3_iter47_invalid_code_fix.py --mode run`** — minimal valid-range cohort N=95 excluding `NLS151`, `NLS188`, `WPD013`; additionally recodes invalid raw subitem values outside 0-4 to missing (`NLS036` 3.15 R/L = 9/9, old target 46 → 28). Stage 1 = iter5 A3 Ridge(H&Y + cv_yrs + cv_sex + cv_dbs); Stage 2 = current V2 residual pool. Mean of 3 seeds. | **0.3784** | 7.528 | `results/preregistration_t3_iter47_invalidcode_20260508_194605.json`; `results/iter47_invalidcode_20260508_194605.json` |
| T3 corrected target Stage-2 no-cv sensitivity | Same valid-range N=95 cohort and Stage 1, but Stage 2 drops all `cv_*` columns from the V2 pool. This is a sensitivity, not a selected headline. | 0.3771 | 7.680 | `results/preregistration_t3_iter47_invalidcode_20260508_194605.json`; `results/iter47_invalidcode_20260508_194605.json` |
| **T3 corrected LOSO transportability** | `run_t3_iter47_invalid_code_fix.py --mode loso` — same valid-range N=95 cohort and current Stage-2 policy; NLS→WPD `0.194`, WPD→NLS `0.106`, two-way mean shown. | **0.150** | 5.88 / 10.18 | `results/preregistration_t3_iter47_invalidcode_loso_20260508_195424.json`; `results/iter47_invalidcode_loso_20260508_195424.json` |
| **Item 15 (3.15 postural tremor) — NEW 2026-05-03 PM iter17** | `run_per_item_iter17_hypothesis.py --mode lockbox --gated_items 15 --gated_variants item_only` — 10 hypothesis-restricted wrist-tremor features (4-7 Hz Wrist FreeAcc bandpower in Balance pre/post pauses + L/R asymmetry) | **+0.1099** (Δ=+0.20 vs −0.09 baseline) | 1.088 | `results/preregistration_peritem_iter17_20260503_221544.json` |
| **Item 18 (3.18 rest tremor constancy) — NEW 2026-05-03 PM iter17** | `run_per_item_iter17_hypothesis.py --mode lockbox --gated_items 18 --gated_variants hy_residual_item_v2` — Stage-1 Ridge(H&Y) + Stage-2 LGB on V2 ⊕ 8 wrist-burst features (4-6 Hz Wrist FreeAcc burst HMM-like proxy) | **+0.4858** (Δ=+0.236 vs +0.25 baseline) | 0.887 | `results/preregistration_peritem_iter17_20260503_221544.json` |

**T1 iter34 N=93 caveat audit (2026-05-08):** `results/audit_t1_iter34_n93_gap_20260508.json` confirms the only excluded T1-complete subject is `WPD002`, missing auxiliary item 18. WPD002 has T1=4.0 near the cohort mean; holding locked iter34 OOFs fixed, grid-optimal N=94 CCC is `0.736598` vs locked N=93 `0.736594`. The N=93 gap is non-load-bearing; do not run a post-hoc N=94 auxiliary-imputation variant.

**T1 iter12 batch-integrity audit (2026-05-08):** `audit_t1_iter12_batch_integrity.py` verifies that the canonical T1 floor is exactly the sum of the six fixed iter8 per-item lockbox OOF arrays from batch `20260430_143044` (items 9-14; no swaps). Artifact `results/t1_iter12_batch_integrity_audit_20260508.json` passes with hard failures `0`, warnings `0`, recomputed CCC `0.6550`, MAE `1.5614`, and max absolute diff `0.0` versus `results/t1_iter12_honest_composite.oof.npy`. This hardens provenance only; it does not promote a new metric.

**T1 iter48 + aux-order auxiliary-label audit (2026-05-08/09):** `audit_t1_iter48_aux_validrange.py` found that historical iter34's auxiliary chain cohort included `NLS036` with top-level item 15 total `18` (raw 9/9 invalid/missing codes; valid item-15 range is 0-8). The reported T1 target items 9-14 are valid, but the historical auxiliary RegressorChain used this invalid item-15 label; valid-range auxiliary filtering would make the chain cohort N=92 instead of N=93. A follow-up audit corrected a bad fixed-order assumption: iter34 uses `RegressorChain(order="random")`, and item15 is upstream of T1 items in seeds `7` and `1337` (2/3 locked seeds). The bounded all-base 5-fold stale-vs-valid screen in `results/t1_iter34_aux_order_audit.json` found common-SID CCC `0.7154` validated vs `0.7162` stale-trained, delta `-0.0008`, bootstrap CI `[-0.0271,+0.0196]`, materiality flag false at `|delta| >= 0.025`. Kimi's fixed-order rationale is therefore falsified, but the measured impact is tiny. Status remains document-only/no post-hoc N=92 lockbox because iter34 is non-canonical and rerunning the lockbox after seeing the issue would be cohort surgery. `run_t1_iter4.load_per_item_scores()` now fails closed on invalid top-level item totals for future runs; `tests/test_run_t1_iter4_labels.py` covers item15=18 masking while preserving valid item17=18.

**T1 iter46 ET-only robustification (2026-05-08) — diagnostic, not canonical.** `audit_t1_iter34_base_item_decomp.py` decomposed iter34's base learners/items across five 5-fold seeds. No base subset passed the strict ceiling-promotion gate. ET-only was the only robustness candidate: 5-fold CCC `0.7057` vs all-base `0.7088`, P2 max point delta `+0.0081`, P2 bootstrap high max `+0.0442` (passes). One follow-up was pre-registered before LOOCV (`results/preregistration_t1_iter46_etrobust_20260508_160501.json`, formula_sha256 `d20ceb018b25d88b7526dcde9cd3dd78c5f59d5f0b9ad398b102cde3a133dc2d`) and run via `run_t1_iter46_et_robust.py`. Result: CCC **0.7269**, MAE `1.758`, N=93; below iter34 by `-0.0097` (bootstrap frac>0 vs iter34 `0.1660`) and above iter12-on-same-SIDs by `+0.0715` but below strict bootstrap floor (`frac>0=0.9388 < 0.95`). Verdict: ET-only localizes P2 fragility mostly to LGB/XGB components, but it is **not a ceiling break and not a canonical update**. Stop base-subset LOOCVs from this screen.

**T1 iter11A (CCC=0.7241) is REPLACED by iter12.** Independent leakage scrutiny (2026-05-03) found multi-layer adaptive variant selection across iter6/iter8/cccv2/iter10/iter11 batches. Paired bootstrap of (iter11A − iter12) on N=94: mean inflation +0.070, 95% CI [+0.029, +0.113], 99.9% bootstrap > 0. Iter11A remains as supplementary "post-hoc per-item-best with full disclosure."

**T3 iter41 (2026-05-08) — target-construction bug found; T3 canonicals revised downward.** `audit_t3_target_stage2_covariates.py` found that `ablation_v3_features.csv:updrs3` exactly matched the raw 33-column MDS-UPDRS Part III sum, but skipna summing converted three PD rows with all 33 Part III subitems missing (`NLS151`, `NLS188`, `WPD013`) into true zero labels. It also found that the iter5 Stage-2 "V2 residual" pool contained hidden clinical `cv_*` covariates (`cv_age`, `cv_dbs`, `cv_ht`, `cv_sex`, `cv_wt`, `cv_yrs`); this is fold-clean but must be disclosed. `run_t3_iter41_target_fix.py --mode run` refit the fixed 2×2 audit battery. Minimal valid-label same-architecture LOOCV CCC is **0.3948** (N=95), Stage2-no-cv sensitivity is **0.4017**, strict complete-33 sensitivities are **0.3962** current and **0.4117** no-cv. Corrected LOSO two-way is **0.163** (NLS→WPD 0.227, WPD→NLS 0.099). Old T3 iter5 `0.5227`, iter16 IPW `0.4694`, and iter16 LOSO `0.341` are now target-contaminated historical artifacts, not canonical deployment results. Findings F-iter41-20260508.

**T3 iter47 (2026-05-08) — invalid raw Part III code found; T3 audit truth revised downward again.** A continuation audit found `NLS036` has raw `MDSUPDRS_3-15-R = 9` and `MDSUPDRS_3-15-L = 9`; Part III subitems are valid only on 0-4, so the old skipna sum inflated this subject's T3 target by +18 (46 → 28). `updrs_columns.py` and `data_split.py` now fail closed on such invalid subitem codes. `run_t3_iter47_invalid_code_fix.py --mode run` pre-declared and reran the iter41 fixed battery after recoding raw Part III values outside [0,4] to missing. Minimal valid-range same-architecture LOOCV is **0.3784**, MAE **7.528**, N=95; Stage2-no-cv sensitivity is **0.3771**. Complete33-validrange sensitivity is **0.4281** current / **0.4010** no-cv on N=88, not a headline. LOSO on the minimal valid-range cohort is **0.150** current (NLS→WPD 0.194, WPD→NLS 0.106); no-cv LOSO is **0.163**. This is a target hygiene correction, not a ceiling break. Findings F-iter47-20260508.

**T3 iter47 target-integrity audit (2026-05-08):** `audit_t3_iter47_target_integrity.py` verifies the corrected T3 target/prediction artifact chain without refitting. Artifact `results/t3_iter47_target_integrity_audit_20260508.json` passes with hard failures `0`, warnings `0`, 33 raw Part III columns, minimal valid-range N=`95`, complete33 N=`88`, excluded all-missing SIDs `{NLS151,NLS188,WPD013}`, exactly two invalid raw values (`NLS036` item 3.15 R/L `9/9`), one target-changed row (`46→28`), subject-CSV recomputed CCC `0.3784` / MAE `7.5280`, and LOSO row recomputed two-way CCC `0.1498`. This is provenance hardening only; it does not move T3.

**T3 complete33 claim-labeling audit (2026-05-09):** `audit_t3_complete33_claim_labeling.py` scans `paper.md`, `CURRENT_PAPER.html`, current handoff/planning files, and artifact indexes for `complete33` / `0.4281` / `0.4010` / `N=88` near headline, canonical, deployment, breakthrough, or completion wording. Latest artifact `results/t3_complete33_claim_labeling_audit_20260509.json` passes with zero findings and zero missing snippets after the complete33 rows were explicitly labeled sensitivity-only. Complete33-validrange N=88 is not a headline and cannot replace the N=95 minimal valid-range T3 audit truth (`0.3784`).

**T3 iter42 (2026-05-08) — partial-missing Part III proration audit — PRIMARY FAIL; loose sensitivity not promotable.** `run_t3_iter42_target_prorate.py` tested literature-backed target hygiene after iter41. Primary `prorate_le3` (observed mean × 33 for 1-3 missing Part III subitems, exclude >3) failed: LOOCV CCC **0.3468** current / **0.3643** no-cv; LOSO **0.144** / **0.125**. Loose `prorate_le7` sensitivity produced LOOCV **0.4165** with current Stage 2 and LOSO about **0.191**, but is not a canonical rescue because it includes `NLS210` with all five rigidity sub-scores missing, was sensitivity only, is unstable across Stage-2 cv policy (`0.4165` current vs `0.3793` no-cv), and still underperforms old iter5 predictions on the same prorated target. Superseded by iter47 valid-range target hygiene; current minimal T3 audit truth is `0.3784` / LOSO `0.150`.

**T3 clinical-dependency audit (2026-05-08) — corrected T3 is clinical/intake + IMU, not pure IMU.** `audit_t3_clinical_dependency.py` fixed the iter41 corrected-target N=95 cohort and removed all hidden `cv_*` columns from Stage 2, then varied only Stage 1. Full two-stage CCCs: A3 H&Y+intake **0.4017**, intake-only (`cv_yrs`, `cv_sex`, `cv_dbs`) **0.3871**, H&Y-only **0.2899**, intercept/IMU-only **0.2449**. Bootstrap A3 minus intake-only was small and uncertain (`+0.0136`, CI `[-0.0984,+0.1203]`), while A3 clearly beat IMU-only (`+0.1519`, CI `[+0.0064,+0.2885]`). Superseded numerically by iter47's stricter valid-range target, but the framing conclusion remains: corrected T3 is clinical/intake + IMU decomposition, not pure wearable-only deployment.

**T3 iter50 low-degree nested convex mix (2026-05-08) — corrected-target screen FAIL.** After Kimi advised against post-hoc T1 OOF mixing and recommended the F56 low-degree T3 escape hatch instead, `run_t3_iter50_lowdf_convex.py` wrote a screen declaration (`results/preregistration_t3_iter50_lowdfconvex_screen_20260508_225105.json`, formula_sha256 `64d85ad663d71561882711a37a3443f0de2a975ddcd24f94ec827e87d8bda29d`) and tested a one-parameter nested convex mix on the iter47 valid-range N=95 cohort. Predictors were A3 clinical-only Ridge and direct IMU-only/no-`cv_*` LGB; alpha was selected by inner 4-fold CV inside each outer 5-fold train split. Result artifact `results/iter50_lowdf_convex_screen_20260508_225105.json`: same-loop baseline CCC **0.3759**, nested-convex CCC **0.3083**, delta **-0.0676**, seed-delta std **0.0319**, bootstrap mean delta **-0.0646** (95% CI `[-0.1286,+0.0068]`, frac>0 `0.0348`). Alpha was unstable and often extreme (mean `0.584`, std `0.411`, min `0.0`, max `1.0`). **Gate FAIL; no LOOCV and no canonical T3 change.** Do not retry low-degree clinical/IMU convex mixers at this N without genuinely new predictors or a new target representation.

**T3 iter5 (2026-05-02) — historical clinical-augmented Stage 1 result, now target-contaminated.** Original report was +0.114 LOOCV CCC vs prior 0.4092 baseline, but both used the skipna-summed `updrs3` target that included three all-missing Part III rows as zero labels. Mechanism note remains useful: cv_yrs, cv_sex, and cv_dbs are intake patient-state covariates recorded before gait. Do not cite iter5 `0.5227` as a deployable result after iter41.

**T3 iter6 (2026-05-02) — IMU feature additions failed.** V2+unsigned-asymmetry (1170 max(L,R)/min(L,R) per L/R pair): LOOCV CCC=0.5008, Δ=−0.022 vs iter5, bootstrap CI [−0.075, +0.025]. V2+event-axial (450 features gated to TUG events) hurt at 5-fold by −0.030. **Rule formalized: at N=98 with V2 baseline, require 5-fold delta ≥+0.05 with seed std <0.02 before lockboxing any IMU feature addition.**

**T3 iter16 (2026-05-03) — historical LOSO/IPW audit, superseded by target correction.** IPW on Stage 2 hurt LOOCV by Δ=−0.053 and the original target-contaminated LOSO two-way was 0.341. After iter47, use valid-range corrected LOSO two-way **0.150** on the minimal valid-range cohort instead.

**T1 iter14 (2026-05-03) — FoG-summary scalar additions for items 9, 12 NULL.** Six FoG-summary cols (label-free `item11_multiscale.csv`) appended to V2 for items 9, 12: 5-fold gate FAIL (item 9 Δ=+0.001 / std=0.06; item 12 Δ=+0.007 / std=0.026). Mechanism: K=500 LGB-importance selector absorbs 6 scalars in ~2200-col incoming pool. Same dead-list pattern as F19 sensor-fusion at N=94. Pre-registration NOT written. Findings F44.

**T1 iter15 (2026-05-03) — UKB OxWearables HARNet 2048-d embedding additions for items 9, 10, 12, 14 NEGATIVE.** Frozen `harnet30.feature_extractor` (~700K UKB person-days SSL) embeddings appended to V2 for 4 items: T1-sum 5-fold gate FAIL with **Δ = −0.031 across all 5 seeds (every seed: control > harnet_aug)**. Triangulates with MOMENT (F41) and HC SSL (F41): **frozen healthy-population-pretrained encoders are orthogonal to within-PD severity at any embedding scale.** Plus K=500 displacement of useful V2 moments. Findings F45.

**Cache provenance hardening/backfill (2026-05-08/09) — placeholder `git_sha` is no longer accepted.** `cache_provenance.py` and `audit_cache_manifests.py` now reject required manifest values such as `git_sha: "unknown"` and require a concrete hex-like git SHA. Latest `audit_cache_manifests.py` gives 45 cache-like artifacts: 4 complete clean manifests (`clinical_extras.csv`, `item11_multiscale.csv`, `item11_multiscale_recordings.csv`, `harnet_subj_embeddings.csv`), 8 partial, 33 missing. `item11_multiscale_recordings.csv` was backfilled only as the recording-level companion emitted by the same already-proven `cache_item11_multiscale.py` command as the subject cache; Harnet was backfilled only after `audit_cache_backfill_candidates.py` showed its manifest script_sha256 exactly matched `cache_harnet_embeddings.py` at commit `d281a0e`. These fix provenance only and do not revive any negative route. The remaining backfill reports classify item-specific/unused-channel caches as manual candidates needing command/schema evidence, phase-locked caches as needing a committed exact script first, indomain-SSL/COPS/TLVMC caches as not suitable for internal-headline backfill, and 33 missing sidecars by producer evidence (`blocked_by_upstream_diagnostic_cache=5`, `insufficient_producer_evidence=9`, `manual_backfill_candidate_needs_human_patch=5`, `manual_review_label_or_clinical_tokens=14`). `audit_cache_backfill_decisions.py` explicitly leaves the item-specific and unused-channel caches partial because exact command/runtime fields are missing and should not be inferred from narrative docs. `audit_manual_cache_backfill_evidence.py` inspected the 5 missing-manifest manual candidates and leaves all five `leave_missing_no_patch`: MOMENT/HC-SSL/TUG depend on a broken `results/rocket_recordings.npz` symlink, joints/stride depend on a missing raw CSV directory, the current remote has only the same broken rocket symlink and none of the five artifacts/logs, and exact command/runtime evidence is absent. Findings F-cache-provenance-hardening-20260508, F-cache-backfill-candidates-20260508, F-cache-backfill-decisions-20260508, F-missing-cache-manifest-origins-20260509, and F-manual-cache-backfill-evidence-20260509.

**Cache consumer guard audit (2026-05-08) — 53 model/composer scripts remain diagnostic-only.** `audit_cache_consumer_guards.py` scans Python scripts for references to cache-like artifacts from `results/cache_manifest_audit_20260508.json`. Latest artifact `results/cache_consumer_guard_audit_20260508.{json,md}` classifies 4 current safe consumers as guarded (`compose_t1_iter14_fog.py`, `compose_t1_iter15_harnet.py`, `run_t3_iter23_clinical_ablation.py`, `run_t3_iter24_stage2_forced.py`), 53 model/composer scripts as diagnostic-only because they reference missing/partial-manifest caches, and 32 references as non-model/cache-producer code. Findings F-cache-consumer-guards-20260508.

**Transitive/runtime cache dependency audits (2026-05-08) — direct guard status is insufficient for provenance claims.** `audit_transitive_cache_dependencies.py` walks local AST import closures for 12 current headline/reportable entrypoints and writes `results/transitive_cache_dependency_audit_20260508.{json,md}`. After narrowing the iter12 composer, latest counts are 5 direct diagnostic-cache entrypoints (`compose_t1_iter12_honest.py`, `run_t3_iter41_target_fix.py`, `run_t3_iter5_clinical.py`, `run_t3_iter16_site_ipw.py`, `run_t3_iter49_cops.py`) and 7 conservative transitive-reachability entrypoints. `audit_runtime_cache_dependencies.py` then traces lightweight data-load/recompute paths with `sys.addaudithook('open')`: iter12 now recomputes CCC `0.6550` while opening only `ablation_v3_features.csv` among cache-like artifacts, not the old peritem/MOMENT/HC-SSL/walkway caches; iter47 also executes `ablation_v3_features.csv`, while static `velinc_features.csv` reachability is not executed in the smoke path. Runtime audit also documents that current fail-closed iter34 source loading now produces N=92 after the auxiliary valid-range fix, so it is not a reproduction path for the historical N=93 lockbox. Treat these as provenance boundaries, not automatic invalidation: future cache-manifest-clean headlines need real backfill/regeneration for reachable live caches or narrower helper extraction. Findings F-transitive-cache-deps-20260508 and F-runtime-cache-deps-20260508.

**T3 `dst_*` walkway-distillation provenance audit (2026-05-08) — caveat measured, no material T3 change.** `audit_dst_walkway_leakage.py` found that `ablation_v3_features.csv` contains 31 `dst_*` columns from `run_ablation_v2.distill_walkway()`, which trained an XGBoost model once on the historical dev split to predict pressure-walkway metrics and then wrote predictions for all subjects. Current V2 filters include all 31 columns, so they are not fold-local for LOOCV and must be disclosed as a provenance caveat. The three-seed sensitivity on the iter47 valid-range N=95 cohort removes only `dst_*` from Stage 2: current CCC **0.3784**, MAE `7.528`, selected `dst_*` count `1611`; no-`dst_*` CCC **0.3766**, MAE `7.580`, selected `dst_*` count `0`; paired bootstrap delta no-`dst` minus current mean `-0.0004`, 95% CI `[-0.0479,+0.0523]`, frac>0 `0.480`. Verdict: `dst_*` is a real fold-firewall/provenance issue but not a load-bearing source of the corrected T3 point estimate. Do not call the current V2 pipeline cache-manifest-clean; when reporting T3, pair iter47 current `0.3784` with the no-`dst_*` sensitivity `0.3766`. Artifact: `results/dst_walkway_leakage_audit_20260508_multiseed.json`.

**Ablation V3 cache provenance audit (2026-05-08) — live V2 cache remains diagnostic-boundary, no clean sidecar synthesized.** `audit_ablation_v3_cache_provenance.py` gathers concrete evidence for `results/ablation_v3_features.csv`: SHA256 `b405d90a6a35808d556d726b58bf7d9361d26e020a79091e52c868ee98f9c2b4`, shape `178 x 1877`, git-tracked from commit `94842a4`, log SHA256 `3a8beb404e1d58b73bff57268ebd4d44d31da47a2fdf032e3a20e6b4b02b1ed1`, and current V2 filters selecting 1752 cols including 31 `dst_*` and 6 `cv_*`. The audit links runtime evidence showing this is the only diagnostic cache opened by lightweight iter12/iter34/iter47 paths, but records `decision=do_not_synthesize_clean_manifest` because the exact command, raw-data hash, creation timestamp, producing git SHA, and fold/normalization/cohort-statistics manifest fields cannot be proven from existing evidence. Use this as a disclosure/provenance boundary, not as a cache-manifest-clean claim. Artifact: `results/ablation_v3_cache_provenance_audit_20260508.json`.

**Ablation V3 non-destructive regeneration probe (2026-05-09) — BLOCKED on current remote raw-data completeness.** `audit_ablation_v3_regeneration.py` was added to monkeypatch `run_ablation_v3.FEATURE_CACHE` to a timestamped path and compare a regenerated cache against the frozen `results/ablation_v3_features.csv` without overwriting it. Remote preflight confirmed all Python deps (`antropy`, `pywt`, LightGBM, XGBoost, CatBoost) and the frozen cache SHA `b405d90a6a35808d556d726b58bf7d9361d26e020a79091e52c868ee98f9c2b4`, but the probe exited `blocked_missing_regeneration_inputs`: current `fiod@165.22.71.91:2243` has PD CSVs/PD clinical only and is missing `CONTROLS - Demographic+Clinical - datasetV1.csv`, `CONTROL PARTICIPANTS/CSV files`, and `Walkway-derived metrics/PKMAS Walkway Gait Metrics - HP+SP.csv`. Fresh Synapse probing identified the exact recovery IDs: control clinical `syn55105521`, control CSV folder `syn61370552` (680 CSVs), and walkway metrics `syn64589881`. `scripts/download_weargait_missing_synapse.py` now writes a credential-safe dry-run report at `results/weargait_missing_synapse_recovery_preflight_20260509.{json,md}`; current status is `missing_inputs` with no token/config present, and full control CSV recovery requires `--confirm-large-control-csvs`. Frozen cache mtime/SHA were unchanged and no regenerated CSV was written. Do not synthesize a clean sidecar; full 178-subject regeneration requires restoring controls + walkway raw inputs first. Artifact: `results/ablation_v3_regeneration_probe_20260509.json`.

**WearGait raw-data recovery runbook (2026-05-09) — READY, but NO DOWNLOAD/CACHE PROMOTION.** `scripts/weargait_raw_data_recovery_runbook.md` is now the human-facing recovery guide for the three missing Synapse inputs above. `audit_weargait_raw_data_recovery_runbook.py` writes `results/weargait_raw_data_recovery_runbook_audit_20260509.{json,md}` and passes with decision `raw_data_recovery_runbook_ready_no_download`: the runbook preserves parent project `syn52540892`, recovery IDs `syn55105521`, `syn61370552`, `syn64589881`, the 680-control-CSV large-transfer guard, and the no-clean-manifest/no-model-run boundary. Current stored status remains credentials absent, preflight `missing_inputs`, regeneration probe `blocked_missing_regeneration_inputs`, and frozen cache unchanged. This fills a provenance-action gap only; it does not complete the active goal or make `ablation_v3_features.csv` cache-manifest-clean.

**T3 iter47 residual anatomy (2026-05-09) — diagnostic-only, no model route.** `audit_t3_iter47_residual_anatomy.py` writes `results/t3_iter47_residual_anatomy_20260509.{json,md}` from saved valid-range-corrected OOF predictions. It reproduces CCC `0.3784` / MAE `7.5280`, but shows the current failure mode more sharply: residual corr(true severity) `-0.7771`, Q1 low-severity mean residual `+10.02`, Q4 high-severity mean residual `-9.20`, WPD within-site CCC `0.0515`, and top global post-hoc residual-feature |r| only `0.290`. Do not use this as feature selection; it supports the stop rule against another scalar WearGait-only feature-fishing pass.

**T3 iter47 CCC-rescale sanity (2026-05-09) — diagnostic-only, no model route.** `audit_t3_iter47_ccc_rescale_sanity.py` writes `results/t3_iter47_ccc_rescale_sanity_20260509.{json,md}` from the saved iter47 OOF vector. OOF-level leave-one affine recalibration lowers CCC to `0.2572`; OOF-level variance matching raises CCC to `0.3996`, but the paired-bootstrap CCC delta is small/uncertain (`+0.0208`, CI `[-0.0104,+0.0578]`) and MAE worsens by `+1.1398` (CI `[+0.4659,+1.8440]`). The second-level transform is not fully nested because calibration rows include base predictions from models trained with the held-out subject in other folds. Do not promote this or use CCC range expansion as a reportable route without a fully nested outer/inner artifact.

**Current headline influence audit (2026-05-09) — diagnostic-only robustness guard.** `audit_current_headline_influence.py` writes `results/current_headline_influence_audit_20260509.{json,md}` from saved OOF vectors. No single-subject redline was found: T3 iter47 max absolute leave-one CCC delta is `0.0381` (<0.05) and top-5 influence share is `0.284`; T1 iter34-minus-iter12 matched CCC delta stays positive under every leave-one deletion (minimum `+0.0629`), and iter34 leave-one CCC never drops below `0.6997`. Caveat: influence is severity-tail concentrated (T3 abs(target-median) vs abs(delta CCC) r `0.6779`, Gini `0.6009`), so use this as claim-fragility evidence, not as a filtering rule or model route.

**T3 iter47 domain residual audit (2026-05-09) — diagnostic-only target-anatomy guard.** `audit_t3_iter47_domain_residuals.py` writes `results/t3_iter47_domain_residual_audit_20260509.{json,md}` from saved iter47 OOF predictions plus valid-range true Part III item/domain labels. The parsed clinical item totals exactly reproduce the iter47 target (max diff `0.0`). The current residual is dominated by true non-gait burden: `unobservable_non_gait` residual r `-0.8004`, upper-limb bradykinesia residual r `-0.6224`, and appendicular bradykinesia residual r `-0.6156`. Privileged leave-one domain oracles are large (`unobservable_non_gait` dCCC `+0.4716`; multidomain Ridge oracle CCC `0.8533`), but they use true clinical domain labels at test time and are non-deployable. The best gait-observable true-domain oracle is `gait_balance_7_14` (dCCC `+0.2083`), confirming target-representation headroom but not a WearGait-only model route.

**T3 iter47 item-level residual audit (2026-05-09) — stop-rule evidence, not a roadmap.** `audit_t3_iter47_item_residuals.py` writes `results/t3_iter47_item_residual_audit_20260509.{json,md}` from saved iter47 OOF predictions plus true Part III item scores. Top residual-correlated items are non-WearGait-observable: item 6 pronation/supination `r=-0.571` (oracle dCCC `+0.282`), item 4 finger tapping `r=-0.528` (dCCC `+0.256`), item 5 hand movements `r=-0.469` (dCCC `+0.226`), item 3 rigidity `r=-0.460` (dCCC `+0.195`). Best gait/balance-observable items are much weaker: item 8 leg agility dCCC `+0.148`, item 7 toe tapping `+0.125`, item 10 gait `+0.091`. Mean `|r(item,residual)|` observable items 7-14 = `0.247` vs non-observable = `0.371`. This is an anatomical ceiling: WearGait's gait/balance protocol does not capture upper-limb bradykinesia, rigidity, or tremor, and the residual structure is explained by target composition, not missing model capacity. Combined with the dead-list (scalar additions, per-item composite, nested stacks, low-degree convex mix, frozen/fine-tuned encoders, local residual smoothing, sample weights, calibration all FAILED), there is **no valid next WearGait-only model route to break T3 CCC**. Document this as stop-rule evidence; do not launch another WearGait-only T3 scalar-feature or calibration screen. Findings F-t3-item-residual-stoprule-20260509.

**Canonical-claim consistency audit (2026-05-08) — stale active-scope T3 wording fixed.** `audit_canonical_claim_consistency.py` scans current-scope docs and exports for old T3 numbers (`0.5227`, `0.341`, `0.3948`) used with current/canonical/headline/deployment wording unless the surrounding text labels them historical, superseded, target-contaminated, retracted, archived, or time-local. It initially found stale active-scope wording in `task_plan.md`, `progress.md`, `findings.md`, and a manuscript sentence; these were patched. Latest artifact `results/canonical_claim_consistency_audit_20260508.json` passes with `stale_findings=0` and `missing_required_snippets=0`.

**Task-plan current-scope audit (2026-05-09) — old success tiers are archive-bound.** `audit_task_plan_current_scope.py` verifies that the active head of `task_plan.md` carries post-iter47 completion criteria and that the old success-tier table (`0.4092`, `0.43`, `0.46`, `0.50`) remains below the historical archive boundary. Latest artifact `results/task_plan_current_scope_audit_20260509.json` passes with decision `task_plan_current_scope_guard_passed`, hard failures `0`, and current-scope legacy success findings `0`. This is planning/claim governance only; it does not complete the T1/T3 ceiling-break goal.

**Headline metric recompute audit (2026-05-08) — stored predictions reproduce headline numbers.** `audit_headline_metric_recompute.py` recomputes current T1/T3 headline and sensitivity metrics from stored per-subject prediction artifacts (and LOSO from per-seed rows) instead of trusting only summary JSON fields. Latest artifact `results/headline_metric_recompute_audit_20260508.json` passes 9/9 checks within tolerance `5e-4`, including T1 iter12 `0.6550`, T1 iter34 `0.7366`, T3 iter47 current `0.3784`, no-cv `0.3771`, no-`dst_*` `0.3766`, and valid-range LOSO `0.1498`. This is a reproducibility guard, not a model update.

**OOF artifact integrity audit (2026-05-08) — binary OOF files match JSON predictions.** `audit_oof_artifact_integrity.py` verifies selected lockbox `.oof.npy` companions exactly match their JSON `per_subject.y_pred` arrays. Latest artifact `results/oof_artifact_integrity_audit_20260508.json` passes 4/4 checks with max absolute diff `0.0` for T1 iter12, T1 iter34, T1 iter46 diagnostic, and historical target-contaminated T3 iter5. This prevents JSON/OOF artifact drift; it does not promote historical T3.

**Pre-registration temporal integrity audit (2026-05-08) — no hard ordering failures, several weak-field warnings.** `audit_preregistration_temporal_integrity.py` checks selected reportable artifacts using embedded timestamps or filename timestamps rather than pulled-file mtimes, and compares formula hashes where both sides record them. Latest artifact `results/preregistration_temporal_integrity_audit_20260508.json` passes 8/8 with `hard_failures=[]`. It records 11 warnings: legacy/no formula hashes for older artifacts, result-side formula links missing for T1 iter34 and FoG-STAR iter39, several `git_sha: unknown` preregs, and two filesystem-mtime caveats where pulled artifact mtimes are not temporal-order evidence. Treat this as a caveat register, not as proof of fully manifest-clean provenance.

**Pre-audit claim labeling audit (2026-05-08) — old held-out/stacking/ceiling claims must stay locally historical.** `audit_pre_audit_claim_labeling.py` scans `paper.md` and `CURRENT_PAPER.html` for the old pre-audit held-out/stacking/ceiling claims (`MAE = 6.89`, `r = 0.860`, `MAE = 6.43`, `r = 0.848`, "proper held-out", "most rigorous evaluation", "approaching clinical utility"). Latest artifact `results/pre_audit_claim_labeling_audit_20260508.json` passes with zero findings after the manuscript explicitly labels Section 4.2, Section 4.7, and Section 5.3 as historical pre-audit. This is a paper-claim guard, not a model result.

**Historical subdomain/sensor claim labeling audit (2026-05-09) — old auxiliary analyses must not become deployment claims.** `audit_historical_subdomain_claim_labeling.py` scans `paper.md` and `CURRENT_PAPER.html` for the historical sensor-ablation and subdomain-prediction claims (`MAE = 7.58`, `MAE = 2.61`, "observable motor subdomain", lower-back redundancy, and related deployment phrasing). Latest artifact `results/historical_subdomain_claim_labeling_audit_20260509.json` passes with zero findings after the abstract and Sections 4.8/4.10/5.1/6 were relabeled as historical pre-audit context. Current observability claims should cite strict-inductive T1 and iter47 residual/domain/item audits, not the old auxiliary tables alone.

**T1 candidate claim labeling audit (2026-05-08) — iter34 must not drift into canonical wording.** `audit_t1_candidate_claim_labeling.py` scans current paper/handoff surfaces for `iter34` / `0.7366` near canonical, deployment, headline, replacement, or completion wording unless local context preserves the candidate/caveat framing. Latest artifact `results/t1_candidate_claim_labeling_audit_20260508.json` passes with zero findings and zero missing snippets. Required framing: iter34 is the strongest candidate / post-publication replication target with N=93, P2, and auxiliary-label caveats; iter12-honest `0.6550` remains the canonical floor.

**T3 complete33 claim labeling audit (2026-05-09) — N=88 sensitivity must not become the T3 headline.** `audit_t3_complete33_claim_labeling.py` scans active claim surfaces for complete33/N=88/0.4281 overclaim. Latest artifact `results/t3_complete33_claim_labeling_audit_20260509.json` passes with zero findings and zero missing snippets. Required framing: complete33-validrange is sensitivity-only complete-case context; the current corrected T3 internal headline remains N=95 minimal valid-range CCC `0.3784`.

**Reportable artifact flag audit (2026-05-09) — archived booleans are not current claim policy.** `audit_reportable_artifact_flags.py` checks reportable JSON artifacts whose raw flags could mislead automation after later audits. Latest artifact `results/reportable_artifact_flag_audit_20260509.json` passes 5/5 checks with zero hard failures and records three superseded raw flags. Most importantly, `results/lockbox_t1_iter34_hybrid_20260506_141720.json` still contains raw `is_canonical_update=true`, but the audit explicitly overrides that archived field with current status `strongest_candidate_caveated_not_canonical_replacement`. `results/lockbox_t3_iter5_A3_tier1_20260502_171604.json` likewise keeps `is_lockbox_headline=true` only as historical target-contaminated metadata. Downstream code should read this audit/current policy layer, not raw lockbox booleans alone.

**CCC metric integrity audit (2026-05-09) — metric convention is not a hidden ceiling lever.** `audit_ccc_metric_integrity.py` writes `results/ccc_metric_integrity_audit_20260509.{json,md}` and pins reportable CCC to Lin's population-moment convention. Latest run passes with zero hard failures across 7 headline/candidate vectors and 7 synthetic implementation checks. The common sample-moment convention would move any checked headline by at most `0.0000028` CCC, so it cannot explain T1/T3 ceiling behavior. `inductive_lib.ccc` was aligned with `eval_utils.lins_ccc` for non-finite masking and the `<3` finite-pair guard; `tests/test_inductive_lib.py` now pins the population formula and non-finite behavior. The only retained warning is deliberate: fewer than three finite pairs returns `0.0`.

**Per-item evidence map audit (2026-05-08) — item CCCs are claim-scoped.** `audit_per_item_evidence_map.py` writes `results/per_item_evidence_map_20260508.{json,md}` and labels all 18 UPDRS-III item artifacts by current claim scope. Current map: 6 iter12 T1 components (items 9-14), 2 supplementary iter17 per-item wins (items 15 and 18), 7 historical iter8 supplementary lockboxes (items 4-8, 16, 17), and 3 backfill-only items (1-3). It verifies item9 CCC `0.4437`, item12 CCC `0.5928`, item15 CCC `0.1099`, item18 CCC `0.4858`, canonical T1 sum `0.6550`, and marks the old 18-item T3 sum CCC `0.2646` as `historical_dead_route_not_current_t3`. Use it to prevent per-item CCCs from drifting into current T3 or standalone deployment claims.

**Per-item OOF companion scope audit (2026-05-08) — per-item JSONs are summary-only.** `audit_per_item_oof_companion_scope.py` writes `results/per_item_oof_companion_scope_audit_20260508.{json,md}` and checks all 15 OOF-backed per-item rows. Latest run passes: row-level JSON comparison availability is `0` because the per-item JSONs lack `per_subject.y_pred`; all OOF companions are finite expected-length arrays; the six current T1 item OOF companions (items 9-14) sum exactly to `results/t1_iter12_honest_composite.oof.npy` with max absolute diff `0.0`; the retained warning is supplementary item18 JSON N=`93` versus a 94-slot companion array. The map now reads individual lockbox N values, including historical item17 N=`93`.

**T1 iter12 batch-integrity audit (2026-05-08) — single-batch composer provenance.** `audit_t1_iter12_batch_integrity.py` writes `results/t1_iter12_batch_integrity_audit_20260508.{json,md}` and verifies all six item-level preregs, lockbox JSONs, OOF arrays, target ranges, summary metrics, and composite metric recomputation for the canonical iter12 floor. Latest run passes with zero hard failures/warnings, single coherent batch `20260430_143044`, no swaps, recomputed CCC `0.6550`, MAE `1.5614`, and max summed-OOF diff `0.0`. This is a provenance guard only.

**T1 iter17 (2026-05-03 PM, 100x researcher push):**
- **A1 unused-channels (Mag/VelInc/OriInc) — NEGATIVE.** 255 features from entirely-unused IMU channels concatenated to V2-augmented X failed both gates (T1-sum Δ=−0.043, item 11 collapsed −0.15). Same K=500 absorption mechanism as F19/F44/F45. Findings F48.
- **A2 hypothesis-restricted item submodels — TWO PASSERS.** Items 15 (postural tremor) and 18 (rest tremor constancy) lockboxed with item-specific wrist-tremor features. Item 15 LOOCV +0.1099 (Δ=+0.20 vs −0.09 baseline, seed std 0.0065); item 18 LOOCV +0.4858 (Δ=+0.236 vs +0.25 baseline, seed std 0.020). Items {4, 6, 16, 17} screened but failed strict gate; reported as supplementary. Findings F50. **First successful iter at this N showing that small, clinically-anchored feature sets BYPASS K=500 absorption when the model bypasses V2 entirely (item_only) or uses H&Y residual decomposition.**

**T1 iter18 (2026-05-04) — In-domain SSL pretraining + canary gate + 5-fold screen — NEGATIVE.** 256-d transformer-MAE embeddings pretrained on 178-cohort raw IMU (40 epochs, mask=0.5, loss flat at 0.99 → essentially mean prediction). Canary gate PASSED (|Δ|=0.003 < 0.020) — no SID-identity leakage. Sum-T1 5-fold gate FAIL: Δ=−0.009 across 5 seeds (mixed direction: +0.019/−0.012/−0.026/+0.012/−0.036). Findings F51. **4th frozen-encoder triangulation now spans generic-TS (MOMENT) → healthy-pop gait (HC-SSL) → large-scale population accelerometer (UKB HARNet) → in-domain same-cohort (iter18). All four NULL/NEGATIVE. Wall is N=94, not domain-gap.**

**T1 iter37 (2026-05-08) — End-to-end HARNet fine-tuning pilot — NEGATIVE.** This closed the final encoder loophole left by the frozen-embedding dead list. `run_t1_iter37_harnet_finetune.py` used raw wrist Acc XYZ windows (861 windows across 94/94 T1 subjects), strict subject-level 5-fold CV, train-fold-only target scaling, OxWearables `harnet30.feature_extractor` tail unfreezing, and an attention MIL head. Screen artifact `results/iter37_harnet_finetune_screen_20260508_110641.json`: OOF CCC = **+0.1324**, MAE = 2.1949, fold CCCs = [+0.0516, +0.1481, +0.4740, −0.1199, −0.0052], feasibility gate FAIL vs direct-CCC floor 0.60. Mechanism: validation optimism plus held-out fold collapse at N=94. Do not retry HARNet fine-tuning variants on this WearGait-only cohort. Findings F-iter37-20260508.

**T3 iter38 / FoG-STAR (2026-05-08) — Public external T3 route found; Stage-1 augmentation screen NEGATIVE.** FoG-STAR (`Scientific Data` 2026 / Zenodo `17838806`) is public CC-BY 4.0 and has 22 PD subjects with wearable IMU plus total `updrs_iii` labels, correcting the earlier "Hssayeni only direct route" assumption. `run_t3_iter38_fogstar_stage1.py` tested the lowest-variance internal-ceiling use: augment iter5 Stage-1 Ridge with FoG-STAR clinical rows (`h_y`, disease duration, sex, `updrs_iii`) while keeping Stage-2 exactly WearGait train-fold V2 residual. Remote screen artifact `results/iter38_fogstar_stage1_screen_20260508_142623.json`: seed-mean baseline CCC **0.4888**, augmented CCC **0.4896**, delta **+0.0008**; mean seed delta +0.0066, seed std 0.0297, bootstrap CI [-0.0566, +0.0658], frac>0 0.4938. **Gate FAIL; no LOOCV lockbox.** FoG-STAR remains useful for pre-registered zero-shot external validation / paper rigor, not as a demonstrated internal CCC booster. Findings F-fogstar-20260508.

**T3 iter39 / FoG-STAR zero-shot (2026-05-08) — PARTIAL external validity, no internal ceiling movement.** Pre-reg `results/preregistration_t3_iter39_fogstar_zeroshot_20260508_143717.json` (formula_sha256 `e82d3c10c6199813f32d70144f959c7b8d61cb3d9d938311551ac0d0c11917d1`) trained on WearGait-PD only and tested once on all 22 FoG-STAR subjects. Track A WearGait wrist-direct CCC **−0.0180** (CI [−0.0912, +0.0465], MAE 22.61). Track B iter5-style clinical+wrist CCC **+0.2499** (CI [+0.0281, +0.5028], MAE 12.89). Track C FoG-STAR-only LOOCV sanity CCC **+0.0821** (CI [−0.3058, +0.5096]). Visualization: `results/iter39_fogstar_zeroshot.html`. OpenRouter Grok 4.3 and DeepSeek V4 Pro both advised recording FoG-STAR as partial external-validity evidence only, not launching another internal T3 ceiling-break experiment. Findings F-fogstar-20260508.

**T3 iter49 / COPS external validation (2026-05-08) — wrist-only NULL, clinical+wrist PARTIAL, no internal ceiling movement.** COPS (`Scientific Data` 2026 / OSF `5xvwn`) is a public unblocked external route: 66 OSF ZIP records, 64 unique subject ZIP filenames after OSF duplicates, bilateral wrist GENEActiv accelerometry at 100 Hz, demographics, symptom diaries, and UPDRS-III OFF/ON CSVs. `run_t3_iter49_cops.py --mode write_prereg` froze a zero-shot battery before full subject-archive download (formula_sha256 `0bc80ef0b6bd9c40da6a7a1282ce9f8898273c6e2dc01e7987ea2ecaa4715b15`). Full artifacts: `results/iter49_cops_download_manifest.json`, `results/iter49_cops_features_full.csv`, `results/iter49_cops_zeroshot_20260508_185226.json`, stable `results/iter49_cops_zeroshot.json`. Scale check passed: COPS raw magnitude after g→m/s² conversion mean `9.87` m/s², matching WearGait raw `R_Wrist_Acc` magnitude `~9.8-10.1`. Primary OFF-label N=62 results: Track A right-wrist magnitude-only CCC **−0.0193** (CI [−0.1030,+0.0704]); Track B right clinical+wrist CCC **+0.2412** (CI [+0.1061,+0.3916]); Track D bilateral clinical+wrist CCC **+0.2535** (CI [+0.1199,+0.3989]); Track C COPS-only LOOCV sanity CCC **+0.3100** (CI [+0.1321,+0.4818]). Kimi post-result advice: close COPS as documented transportability; do not use it to justify another internal T3 architecture iteration. Findings F-iter49-cops-20260508.

**TLVMC / DeFOG iter51 zero-shot (2026-05-09) — PUBLIC DIRECT T3 external route, PARTIAL external validity only.** Zenodo `10959560` / Kaggle `tlvmc-parkinsons-freezing-gait-prediction` is public CC-BY 4.0. `scripts/probe_tlvmc_fog_route.py` confirmed the clean DeFOG subset: 137 medication-matched recordings, 45 subjects, 70 subject-visits, split into 68 OFF records from 44 subjects and 69 ON records from 45 subjects; `tdcsfog` has 0 joined UPDRS-III targets in the public metadata probe. Pre-model preregistration: `scripts/write_tlvmc_defog_prereg.py` -> stable `results/preregistration_t3_iter51_tlvmc_defog_zeroshot.json`, formula_sha256 `665479765911e8192e4db570babeb5fcef3e2b6553dfd6259187f76685ec08cd`. Full run artifacts: `run_t3_iter51_tlvmc_defog.py`, `results/iter51_tlvmc_defog_features.csv`, `results/iter51_tlvmc_defog_features.csv.manifest.json`, stable `results/iter51_tlvmc_defog_zeroshot.json`, timestamped `results/iter51_tlvmc_defog_zeroshot_20260509_013357.json`, and `results/iter51_tlvmc_defog_zeroshot_rows_20260509_013357.csv`. Primary OFF Track A lower-back magnitude zero-shot CCC `+0.2695` (95% CI `[+0.1693,+0.3600]`, MAE `8.069`, r `0.5635`); Track B wrist-to-lumbar stress CCC `+0.0485`; Track C DeFOG-only LOSO sanity CCC `+0.3450`; ON Track A sensitivity CCC `+0.0548`. One raw file (`02ab235146`) was skipped because it lacked `Valid`/`Task`. Decision: unblocked external-validation route only; no TLVMC/DeFOG result may update the internal WearGait-PD T3 canonical. Findings F-tlvmc-defog-route-20260509, F-tlvmc-defog-prereg-20260509, and F-tlvmc-defog-result-20260509.

**PDFE turning-in-place iter52 zero-shot (2026-05-09) — PUBLIC DIRECT T3 external route, transfer NEGATIVE/weak, no internal ceiling movement.** Figshare `14984667` / Frontiers 2022 is public CC-BY 4.0 and exposes `PDFEinfo.csv` plus shank `IMU.zip`; session-1 metadata has 35 PD subjects with UPDRS-III totals, session 2 has 23, and session 3 has 13. Local metadata inspection also surfaced Figshare `14896881` (public ON/OFF UPDRS-III totals/items) as motion-capture/force-plate biomechanics, not wearable IMU. Iter52 froze the PDFE formula before modeling (`results/preregistration_t3_iter52_pdfe_turning_zeroshot.json`, formula_sha256 `f0eb5985a15b271a333b3d9e1d093e32889814a0f48d0ca4f5131b9674c7b2f2`) and ran `run_t3_iter52_pdfe_turning.py` as an external-only stress test: WearGait valid-range T3 trained on bilateral lateral-shank magnitude features and scored PDFE trial/session-1 shank magnitude features. Artifacts: `results/iter52_pdfe_turning_probe.json`, `results/iter52_pdfe_turning_download_manifest.json`, `results/iter52_pdfe_turning_features.csv`, `results/iter52_pdfe_turning_features.csv.manifest.json`, stable `results/iter52_pdfe_turning_zeroshot.json`, timestamped `results/iter52_pdfe_turning_zeroshot_20260509_092223.json`, and row predictions. Results: Track A WearGait shank-to-PDFE CCC `-0.1008` (95% CI `[-0.2877,+0.0554]`, MAE `14.154`), Track B clinical+shank CCC `+0.1340` (CI `[-0.0426,+0.3369]`, MAE `12.585`), Track C PDFE-only LOOCV sanity CCC `+0.4020` (CI `[+0.1569,+0.6519]`, MAE `10.283`). Kimi initially recommended document-only due shank/protocol mismatch; Claude CLI failed low-credit and `glmcode` is unavailable. Empirical decision: PDFE is a real public external transportability row, but it confirms protocol-specific signal with failed WearGait transfer; no internal WearGait-PD T3 canonical update. Findings F-pdfe-turning-route-20260509.

**External-result claim labeling audit (2026-05-09):** `audit_external_result_claim_labeling.py` scans `paper.md`, `CURRENT_PAPER.html`, active handoff/planning files, and the FoG-STAR/COPS/TLVMC/PDFE external zero-shot JSONs for external route names or CCCs near internal, canonical, headline, deployment, or ceiling-break wording. Artifact `results/external_result_claim_labeling_audit_20260509.json` passes with findings `0`, missing required snippets `0`, and artifact failures `0`. Policy: external FoG-STAR, COPS, TLVMC/DeFOG, PDFE, and PADS numbers are transportability or within-dataset sanity evidence only; they cannot update the internal WearGait-PD T3 headline or canonical.

**Paper generator routing audit (2026-05-09):** `audit_paper_generator_routing.py` scans active docs, current renderer artifacts, and legacy paper-generation surfaces. Latest artifact `results/paper_generator_routing_audit_20260509.json` passes with decision `current_paper_renderer_route_guard_passed`, hard failures `0`, and eight active docs checked. Current paper work must use `render_current_paper.py` -> `CURRENT_PAPER.html`; `generate_paper_v4.py` / `NEW4.html` are explicitly legacy/stale archaeology and not current evidence.

**README claim-routing audit (2026-05-09):** `audit_readme_claim_routing.py` scans the root README for stale SSL/XGBRanker numbers and methodology claims. Latest artifact `results/readme_claim_routing_audit_20260509.json` passes with decision `readme_current_claim_route_guard_passed`, hard failures `0`, and unguarded stale hits `0`. The README must route readers to current post-audit claims (T1 canonical floor `0.6550`, T1 candidate `0.7366`, T3 `0.3784`, LOSO `0.150`) and may mention old `0.868` / `0.776` only as legacy/retracted/pre-audit target-contaminated archaeology.

**Legacy manuscript-surface quarantine audit (2026-05-09):** `audit_legacy_manuscript_surfaces.py` scans retained pre-audit paper/narrative surfaces for visible stale/do-not-cite banners and current-route pointers. Latest artifact `results/legacy_manuscript_surface_audit_20260509.json` passes with decision `legacy_manuscript_surfaces_quarantined`, hard failures `0`, and 16 surfaces checked. `paper.tex`, `paper_new2.tex`, `CALIB-EXPERIMENTS.md`, `HOW.md`, `REPRODUCIBILITY.md`, `review_report*.md`, legacy `generate_paper*.py`, and `NEW4.html` / `NEW5.html` / `NEW6.html` are retained only as archaeology and must not be cited as current evidence.

**Historical archive-surface quarantine audit (2026-05-09):** `audit_historical_archive_surfaces.py` scans retained project-note/archive surfaces for archive-status banners and stale T3/current-route drift. Latest artifact `results/historical_archive_surface_audit_20260509.json` passes with decision `historical_archive_surfaces_quarantined`, hard failures `0`, and 11 surfaces checked. `CONT.md`, `EXP*.md`, `LEARNINGS.md`, `VNEXT.md`, `NEXTNEXT.md`, `literature_review.md`, `paper_supplement_iter33_gate_demo.md`, `CODEX-PROPOSALS.md`, `PROPOSALS.md`, and `leakage_onepager.html` are retained only as historical planning context; `leakage_onepager.html` now points T3 to iter47 CCC `0.3784` / LOSO `0.150` instead of the superseded iter5 `0.5227` row.

**Secret hygiene audit (2026-05-09):** `audit_secret_hygiene.py` scans text surfaces for high-confidence credential patterns and writes `results/secret_hygiene_audit_20260509.{json,md}` without recording raw secret strings. During the 2026-05-09 continuation, local ignored `TOKEN.md` and `.env` files containing JWT-like credentials were removed. Latest artifact passes with decision `secret_hygiene_guard_passed`, findings `0`, and hard failures `0`. Any credential that was ever written to those files should be revoked/rotated.

**Papadopoulos phone-call tremor route refresh (2026-05-09) — PUBLIC, but TREMOR-SUBITEM ONLY.** Zenodo `7273759` is public CC-BY 4.0 and contains smartphone embedded-IMU acceleration recorded unobtrusively during phone calls. It has 45 clinically examined subjects and 454 self-reported subjects, but the clinical annotations are UPDRS II item 16 plus Part III item 20/21 left/right hand tremor, signal-expert binary tremor, and PD status. It has no total MDS-UPDRS Part III label, no T1 items 9-14, and no WearGait-aligned gait/balance protocol. Kimi and Gemini recommended no new model/probe; Claude still fails low-credit and `glmcode` is unavailable. Verdict: no preregistration, download, scaffold, or remote job for the active T1/T3 CCC objective; record only as tremor-subitem/free-living context. Findings F-phone-tremor-route-20260509.

**Harmonized Upper/Lower Limb Accelerometry route refresh (2026-05-09) — PUBLIC/CONTROLLED, but NOT T1/T3 ELIGIBLE.** The 2025 Data in Brief / NICHD DASH resource has 790 participants, 2,885 recording days, and about 7% PD. It exposes daily-life ActiGraph upper/lower-limb accelerometry and processed summary variables; code lives at `keithlohse/HarmonizedAccelData`. It has demographic/clinical fields but no confirmed total MDS-UPDRS Part III or T1 items 9-14, and Part1/Part2 require DASH access. Kimi and Gemini both recommended no preregistration/download/scaffold because the target gap is disqualifying and the modality is daily-life ActiGraph summaries rather than WearGait-aligned structured gait/balance raw IMU. Verdict: document only as rehab/free-living accelerometry context. Findings F-harmonized-accel-route-20260509.

**Monipar/BIOCLITE smartwatch subitem route refresh (2026-05-09) — PUBLIC, but NOT T1/T3 ELIGIBLE.** Monipar (Zenodo `8104853`) has 21 PD / 7 HC participants but only 6 supervised PD subjects / 46 trials with exercise-level MDS-UPDRS item labels (3.17, 3.15, 3.4, 3.5, 3.6, 3.10). BIOCLITE (Zenodo `16408199`) has 24 PD / 16 healthy participants with smartwatch acc+gyro at 50 Hz and per-exercise MDS-UPDRS scores for items including 3.17, 3.15, 3.4, 3.5, 3.6, 3.9, and 3.10. Neither exposes total Part III or a full T1 items 9-14 composite. Kimi advised `NO-PREREG / DOCUMENT-ONLY`; Claude still fails low-credit and `glmcode` is unavailable. Artifacts: `results/smartwatch_subitem_route_refresh_20260509.{json,md}` and `results/external_route_audit_monipar_bioclite_20260509.md`. Verdict: cite only as related work for consumer-smartwatch exercise/subitem monitoring; no preregistration, download, scaffold, or remote job. Findings F-smartwatch-subitem-routes-20260509.

**Zenodo `14848598` derivative multimodal route refresh (2026-05-09) — PUBLIC, but NOT A SENSOR-CLINICAL T1/T3 ROUTE.** The "Comprehensive Multi-Modal Dataset for Parkinson's Disease Prediction" record says it combines AMP Kaggle clinical/CSF data with a Mendeley gait repository. Direct CSV inspection found `Updated_Clinical_Gait_Dataset.csv` has 2,223 rows / 771 patient IDs with UPDRS part totals and scalar gait summaries (`gait_time`, `gait_steps`, `freezing`), while `Final_Integrated_MultiModal_Dataset.csv` has 1,113 rows / 1,196 columns dominated by CSF protein/peptide features keyed by `visit_id`. There is no raw wearable IMU, no T1 item-level labels, and no auditable contemporaneous wearable-to-UPDRS subject alignment. Kimi advised `NO-PREREG / DOCUMENT-ONLY`; Claude still fails low-credit and `glmcode` is unavailable. Artifact: `results/derivative_multimodal_route_refresh_20260509.{json,md}`. Verdict: cite only as a derived public benchmark / provenance caution, not an external validation route or ceiling-break candidate. Findings F-derivative-multimodal-route-20260509.

**Request-only actigraphy/smartwatch route refresh (2026-05-09) — ACCESS REQUEST ONLY, no scaffold.** Two newly surfaced rows are relevant context but not immediate compute routes. Fay-Karmon 2024 / Scientific Reports has 21 advanced-PD participants, Intel Pharma Analytics smartwatch+iPhone home monitoring, MDS-UPDRS II/III ON/OFF plus Part IV, and datasets available only from the corresponding author; SWA scoring is proprietary/schema-hidden and no T1 item-level route is visible. The Sensors 2023 marital-dyad study has 27 PD/spouse dyads (54 individuals), non-dominant wrist GeneActiv at 100 Hz for 7 days, and MDS-UPDRS III for the PD participant; source data are author-request-only and the target is daily-life/social actigraphy, not a structured WearGait-like gait/balance protocol. Kimi advised `NO-PREREG / DOCUMENT-ONLY / ACCESS-REQUEST-ONLY`; Claude still fails low-credit and `glmcode` is unavailable. Artifact: `results/request_only_actigraphy_route_refresh_20260509.{json,md}`. Verdict: no preregistration, download, scaffold, or remote job until data-owner approval and row-level schema exist. Findings F-request-only-actigraphy-routes-20260509.

**Luxembourg / NCER-PD upper-limb route refresh (2026-05-09) — REQUEST-ONLY SUBITEM CONTEXT, no runbook.** Sensors 2024 reports 33 PD patients and 12 controls, six elicited upper-limb MDS-UPDRS III hand/arm tasks, and bilateral compact hand IMUs. The data-availability statement is request-only under national/institutional rules, the public endpoint is upper-limb subitems rather than total Part III or full T1 items 9-14, and the study is ON-medication with hand-IMU task geometry rather than WearGait-PD gait/balance IMU geometry. Kimi advised `skip_runbook_document_only`; Claude still fails low-credit and `glmcode` is unavailable. Artifact: `results/luxembourg_upper_limb_route_refresh_20260509.{json,md}`. Verdict: cite only as related work for the corrected T3 upper-limb observability ceiling; no access runbook, preregistration, download, scaffold, or remote job for the active T1/T3 CCC objective. Findings F-luxembourg-upper-limb-route-20260509.

**Pre-QuantiPark / ActiMyo levodopa-challenge route refresh (2026-05-09) — REQUEST-ONLY TINY-N LCT CONTEXT, no runbook.** Scientific Reports 2025 reports 10 PD patients undergoing a single-dose L-dopa challenge while wearing ActiMyo sensors on the most affected wrist and ankle at 130.69 Hz. MDS-UPDRS Part III was collected before drug intake and every 15 minutes for 90 minutes. Data are request-gated for academic non-commercial proposal review and a data access agreement. Kimi advised `document_only_no_runbook_no_prereg_no_scaffold`; Claude still fails low-credit and `glmcode` is unavailable. Artifact: `results/prequantipark_route_refresh_20260509.{json,md}`. Verdict: cite only as related work for wearable pharmacological motor-fluctuation monitoring; N=10 makes a subject-level 5-fold gate/lockbox incoherent, the endpoint is a within-subject trajectory rather than WearGait-PD cross-sectional severity, no T1 item path is visible, and no access runbook, preregistration, download, scaffold, request packet, or remote job is justified. Findings F-prequantipark-route-20260509.

**TUM Donié ROCKET/InceptionTime route refresh (2026-05-09) — PUBLIC CODE, but HSSAYENI/MJFF ALIAS + ALGO-DEAD.** Scientific Reports 2025 reports ROCKET and InceptionTime experiments on a 27-patient subset of MJFF `syn20681023`, with GENEActiv wrist acceleration at 50 Hz and task-level symptom labels. The public labels are tremor severity plus bradykinesia/dyskinesia presence/absence, not total MDS-UPDRS Part III or T1 items 9-14. The public code repository still requires Synapse credentials for raw data. Kimi advised `document_only_alias_no_scaffold`; Claude still fails low-credit and `glmcode` is unavailable. Artifact: `results/tum_rocket_inception_route_refresh_20260509.{json,md}`. Verdict: cite only as related work for small-N wrist-IMU symptom classification; do not clone/scaffold/preregister/download/run because it is the existing Hssayeni DUA gate plus already-negative local ROCKET/MultiROCKET and learned time-series fine-tuning branches. Findings F-tum-rocket-inception-route-20260509.

**ParaDigMa + Yin et al route refresh (2026-05-09) — DOCUMENT-ONLY RELATED WORK, no scaffold.** ParaDigMa is an open GitHub/JOSS/Zenodo Python toolbox for wrist accelerometer/gyroscope/PPG Parkinson digital biomarker extraction; it is software rather than a labeled cohort, and running it on WearGait would repeat the dead local scalar handcrafted feature-addition category at N=94. Yin et al Frontiers Neurology 2025 reports N=20 PD / 17 HC OFF/ON gait-parameter regression against total MDS-UPDRS III, tremor, and non-tremor scores, but raw data are corresponding-author request-only, no schema is public, and the inputs are likely motion-capture or instrumented-walkway gait parameters rather than raw WearGait-aligned IMU. Kimi advised document-only; Claude still fails low-credit and `glmcode` is unavailable. Artifact: `results/paradigma_yin_route_refresh_20260509.{json,md}`. Verdict: cite only as related work; no access runbook, preregistration, download, scaffold, or remote job for either route. Findings F-paradigma-yin-document-only-20260509.

**Parkinson@Home route refresh (iter53, 2026-05-09) — PUBLIC DIRECT T3 ROUTE, but HARD-STOPPED BEFORE SCORING.** Radboud DOI `10.34973/fr4z-a489` exposes 25 PD + 25 controls, bilateral wrist acc/gyro at 200 Hz, OFF/ON medication recordings, prepared parquet files, and public MDS-UPDRS Part III subitems in `patient_info.csv`. Pre-reg `results/preregistration_t3_iter53_parkinsonathome_zeroshot.json` (formula_sha256 `417fdfe0bd2f07c8c5415bd49c87b70725979a26517fe353ca376e2b85387888`) froze Track A WearGait-to-Parkinson@Home wrist zero-shot, Track C Parkinson@Home-only LOOCV sanity, Track D OFF/ON response sensitivity, and a hard stop requiring at least 20 valid OFF PD subjects after feature-readability filtering. Probe/extraction artifacts are `results/iter53_parkinsonathome_probe.json`, `results/iter53_parkinsonathome_features.csv`, `results/iter53_parkinsonathome_features.csv.manifest.json`, and `results/parkinsonathome_route_refresh_20260509.{json,md}`. Extraction retained only 18 valid OFF PD subjects; therefore no Track A/C/D CCC/MAE exists, no Parkinson@Home labels entered WearGait training, and no internal T1/T3 canonical can change. Do not rerun iter53 under the same preregistration; a shorter-window or alternate fallback policy requires a fresh preregistration and remains external-validity-only. Findings F-parkinsonathome-hard-stop-20260509.

**PPMI / Verily Study Watch route refresh (2026-05-08) — PRIORITY ACCESS-GATED, no scaffold yet.** Fresh web search surfaced a larger direct external route not in the previous closeout. PPMI says qualified researchers can access individual-level clinical and sensor data after DUA/application; FAQ lists MDS-UPDRS Part III and H&Y; the 2025 npj Parkinson's Disease Verily analysis used 100 Hz wrist Study Watch accelerometer data with MDS-UPDRS assessments within 90 days. Kimi recommended documenting PPMI as an access-gated priority route, not building a scaffold without credentials. Claude still fails for low credit; `glmcode` is unavailable. Verdict: add to `results/external_dataset_route_audit_20260508.{md,json}` as `access_gated_no_scaffold_until_credentials`; access runbook now lives at `scripts/ppmi_verily_setup.md` and explicitly blocks scaffolds/downloads/remote jobs before credentials. If the user applies for one new route, prioritize PPMI over Hssayeni because it is wrist-native, larger, longitudinal, and already has a Verily/MDS-UPDRS publication trail. Findings F-ppmi-verily-route-20260508.

**Personalized Parkinson Project / PD-VME route refresh (2026-05-09) — STRONG VERILY-WATCH PEER, but ACCESS-GATED.** PPP data sharing lists 517 PD participants with clinical assessments and Verily Study Watch monitoring; the PD Virtual Motor Exam paper reports 388 early PD participants and validates active smartwatch tasks against MDS-UPDRS Part III / consensus subitem ratings. Access is governed by the PPP Research and Data Sharing Review Committee and may involve fees; there is no local approval or row-level schema. Verdict: add to the gated access queue beside PPMI, but do not scaffold, preregister, download, or run until access and schema exist. Findings F-smartwatch-subitem-routes-20260509.

**WATCH-PD route refresh (2026-05-09) — REQUEST-GATED, document-only.** Fresh web search surfaced WATCH-PD as a strong protocol-matched T3 route: 82 early untreated PD + 50 controls, 12-month longitudinal design, Apple Watch/iPhone BrainBaseline plus APDM Opal sensors worn during MDS-UPDRS Part III, with mean PD Part III 24.1. Data availability is the blocker: datasets are available to C-Path 3DT Stage 2 members, non-members must submit a WATCH-PD Steering Committee proposal, and C-Path's Integrated Parkinson's Database explicitly excludes digital health technology data. Kimi and Gemini advised adding WATCH-PD as request-gated/document-only and not scaffolding until access plus row-level schema exists; Claude still fails for low credit and `glmcode` is unavailable. Access-only checklist: `scripts/watchpd_request_setup.md`. PPMI remains the first gated application target because it is larger and already has a Verily/MDS-UPDRS publication trail; WATCH-PD is a strong wrist/APDM peer after access. Findings F-watchpd-route-20260509.

**ICICLE-PD / ICICLE-GAIT route refresh (2026-05-08) — REQUEST-GATED, document-only.** Fresh web search surfaced the 2026 Frontiers ICICLE federated-learning paper: 89 PD participants, lower-back Axivity AX3 accelerometer at 100 Hz (+/-8g), 7-day free-living gait at 18-month visits over 6 years, and MDS-UPDRS Part III + Hoehn & Yahr labels. Published global benchmarks are modest (traditional ML MAE 10.43, r 0.26, ICC 0.389; best global FL MAE 9.26, r 0.43, ICC 0.438). Data are available by request, not public download. Kimi and Gemini both advised adding it to the external-route audit as request-gated and not scaffolding until files/schema exist; Claude still fails for low credit and `glmcode` is unavailable. Access runbook: `scripts/icicle_request_setup.md`. PPMI remains the first gated application target if only one route is pursued because it is wrist-native; ICICLE is a valuable lower-back longitudinal gait route once access exists. Findings F-icicle-route-20260508.

**Mobilise-D route refresh (2026-05-09) — TVS SKIP, CVS WATCH/REQUEST, no scaffold.** Fresh web search surfaced Mobilise-D as a lower-back longitudinal route family. The public TVS Zenodo record `15861907` has N=108 mixed cohorts and lower-back IMU/reference-system archives, but explicitly says the dataset is for algorithm validation, not clinical insights, so it is not a current UPDRS-III regression target. The CVS evidence is attractive on paper: MDS 2024 reports 600 PD at baseline with MDS-UPDRS in the clinical battery and 7-day lower-back wearable monitoring, while the UK HRA summary reports 602 PD among 2388 total participants and up to 5 visits. No row-level public wearable + MDS-UPDRS schema/release was found. Kimi and Gemini advised: skip TVS, keep CVS as watch-list/request only, no scaffold/download/remote job until access/schema exist; Claude still fails for low credit and `glmcode` is unavailable. PPMI remains the first gated application target because it is wrist-native. Findings F-mobilised-route-20260509.

**CNS Portugal / Lobo IS2022 AX3 gait route refresh (2026-05-08) — REQUEST-GATED, document-only.** Fresh web search surfaced a direct structured-gait T3 route: 74 PD subjects at CNS Portugal, Axivity AX3 on wrist and lower back at 100 Hz, 267 gait instances from 104 sessions of a 10-meter walk, and MDS-UPDRS Part III + H&Y 2-4 labels. Published benchmarks are 10% heldout-window MAE 4.26 and LOSO MAE 9.99, but the 10% split is optimistic because the paper describes windows from patients already seen by the selected models. Kimi and Gemini advised adding it to the external-route audit as request-gated and not scaffolding until files/schema exist; Claude still fails for low credit and `glmcode` is unavailable. Access runbook: `scripts/cns_portugal_request_setup.md`. PPMI remains first if only one gated route is pursued; CNS is a strong structured-gait second/peer request because it is wrist + lower-back AX3 with direct T3 labels. Findings F-cns-portugal-route-20260508.

**Prompt-to-artifact objective audit (2026-05-08) — NOT COMPLETE.** `audit_prompt_objective_evidence.py` writes `results/prompt_objective_evidence_audit_20260508.{json,md}` and now maps 12 explicit objective requirements to concrete evidence: web/SOTA search, CLI consult/tool friction, remote utilization, logs/visualizations, artifact/reproducibility/claim-label guards, sit-with-data methodology fixes, T1/T3 ceiling attempts, external-route triage, conformal/uncertainty work, and the final completion condition. Latest result: `goal_complete=false`; the single hard gap is the unmet clean ceiling-break completion condition. T1 has an attempted caveated candidate (`0.7366`, N=93), while T3 has no corrected breakthrough (`0.3784`). Findings F-prompt-objective-audit-20260508.

**Remaining blocker action audit (2026-05-09) — no local WearGait-only model action remains.** `audit_remaining_blocker_actions.py` writes `results/remaining_blocker_action_audit_20260509.{json,md}` and classifies all `35` current verifier blockers into next-action boundaries. Latest run passes with unclassified blockers `0`, ambiguous blockers `0`, and local WearGait-only model actions remaining `0`. Valid next actions are user/data-owner access for gated external cohorts, raw-data restoration for cache provenance, or paper/provenance hardening; this is a no-repeat guard, not a completion marker.

**External access readiness audit (2026-05-09) — six access packets ready, zero compute routes.** `audit_external_access_readiness.py` writes `results/external_access_readiness_audit_20260509.{json,md}` and turns the gated-route queue into an ordered action matrix. Latest run passes with application/request packets ready for PPMI/Verily, PPP/PD-VME, WATCH-PD, CNS Portugal/Lobo, Hssayeni/MJFF, and ICICLE-GAIT; compute-ready routes before access `0`; top priority `PPMI / Verily Study Watch`; raw WearGait recovery class `raw_data_recovery_credentials_needed`. New PPP runbook: `scripts/ppp_pd_vme_request_setup.md`. Hssayeni setup now explicitly says the existing iter26 scaffold is not permission to download/cache/model before Synapse DUA approval. Mobilise-D CVS, Fay-Karmon, marital-dyad actigraphy, and Luxembourg/NCER-PD upper-limb IMU remain watch/request-only or subitem-only without runbooks by design.

**Current conformal + abstention (2026-05-08) — uncertainty evidence only, no rescue.** `run_current_conformal_abstention.py` regenerated the open conformal/abstention angle on current post-audit OOF rows, not the old target-contaminated iter5 vector. It uses leave-one-subject-out residual quantiles over existing OOF predictions; no model refit or hyperparameter tuning. Artifacts: `results/current_conformal_abstention_20260508.json`, `results/current_conformal_abstention_intervals_20260508.csv`, `results/current_conformal_abstention_curves_20260508.csv`, `results/current_conformal_abstention.html`. Results: T1 iter12 80/95% widths `4.99` / `9.08`; T1 iter34 widths `5.74` / `8.81`; corrected T3 iter47 current widths `25.94` / `34.72`; corrected T3 no-cv widths `26.22` / `35.35`. A deployable abstention proxy that discards predictions farthest from the prediction median does not improve T3: after 50% discard, current T3 CCC is `0.0108` and no-cv CCC is `0.0550`. Oracle residual abstention reaches high CCC but is explicitly non-deployable. Findings F-current-conformal-20260508.

**T3 iter40 / local residual wildcard (2026-05-08) — NEGATIVE; no LOOCV.** In response to the user's request to try wildcards, `run_t3_iter40_local_residual.py` tested a different Stage-2 bias class from LGB and tail-weighting: iter5 Stage-1 unchanged, then fold-local K=500 residual feature selection -> train-only normalization -> PCA(24) -> inverse-distance 12-neighbor residual smoothing. Same 5-fold seeds {42,1337,7}. Remote artifact `results/iter40_local_residual_screen_20260508_144905.json`: same-fold iter5 baseline seed-mean CCC **0.4888**, wildcard CCC **0.4332**, delta **−0.0556**; per-seed deltas **−0.0440 / −0.0337 / −0.0529**; bootstrap CI **[−0.1151, −0.0006]**, frac>0 **0.0235**. Strict (+0.05) and relaxed (+0.025) promotion gates failed. No pre-registration or LOOCV lockbox.
- **A3 site-centered Stage 2 — NEGATIVE on both LOOCV and LOSO.** Per-fold per-site centering of V2 features in Stage 2 hurt the old target-contaminated iter16 LOOCV/LOSO artifact. Findings F49. After iter47, use valid-range corrected LOSO two-way 0.150, not iter16's historical 0.341.

**T3 iter22 (2026-05-04 PM) — 9-cell ablation around plan-next.md — ALL CELLS FAIL (F58).** Plan synthesized from grok-4.3 + deepseek-v4-pro consult predicted Δ=+0.029 to +0.040 LOOCV CCC for the 1-parameter convex blend `α·iter5 + (1-α)·β·T1-iter12`. Master pre-reg `results/preregistration_t3_iter22_ablation_20260504_213817.json` (formula_sha256 `64aae388a2134126`). Result: every blend variant lands negative.

| Cell | CCC | Δ vs iter5 | 95% CI | frac>0 |
|---|---|---|---|---|
| AB1 (canonical: α-only, 4cov, T1=94) | 0.4262 | −0.0209 | [−0.091, +0.043] | 0.283 |
| AB1_N98 (canonical-cohort backfill) | 0.4999 | −0.0230 | [−0.082, +0.032] | 0.212 |
| BB1 ((α,β) joint search) | 0.4341 | −0.0130 | [−0.096, +0.065] | 0.386 (closest) |
| BB2/BB3 (Ridge/OLS k=2 meta) | 0.3446 | −0.1010 | [−0.164, −0.039] | 0.001 |
| CC3_N98 (8-cov-Ridge widening alone) | 0.5004 | −0.0226 | [−0.070, +0.024] | 0.167 |
| AB1_N98_8cov (full stack) | 0.4822 | −0.0408 | [−0.124, +0.037] | 0.156 (compounding) |

α* well-behaved (mean 0.682, 0% degenerate); β* stable in T1=94 cohort (β=5.27, 0 sign flips). **F55's r=+0.327 5-fold residual orthogonality does NOT survive at LOOCV scale** — that's the load-bearing falsification. Ridge/OLS k=2 meta catastrophically destroys iter5's calibration (coef pattern 0.49/1.01) for the same reason F56's k=19 meta blew up. **7th N=94/98 wall data point**, now spanning FIVE probe-strategy classes: feature eng / composition / single-loop hybrid / nested k=19 / **convex blend k=1 + Stage-1 widening (NEW)**. No LOOCV lockbox runs (sensitivity gate failed per protocol). Historical negative; after iter47, compare future T3 work against valid-range corrected-target 0.3784 rather than old 0.5227. Findings F58.

**T3 iter22 LC — empirical learning curve confirms structural ceiling near 0.60 (2026-05-04 23:12 UTC).** 600-job sweep (4 N-levels × 50 subsamples × 3 seeds) of iter5 LOOCV at N ∈ {30, 50, 70, 89}: subsample CCC 0.356 / 0.424 / 0.456 / 0.478 (with seed std 0.194 / 0.138 / 0.084 / 0.050 — variance collapses as N grows). **Pareto fit** `CCC(N) = 0.5975 − 2.1308·N^(−0.6408)` AIC = −52.75 vs Loglinear AIC = −39.22 — Pareto wins. **Asymptote = 0.5975**: even at N=∞ the iter5 architecture caps near 0.60 CCC. Bootstrap projection (Pareto, 2000 resamples): N=200 → Δ = +0.003 [−0.037, +0.039]; N=300 → Δ = +0.020 [−0.035, +0.074] — neither reliably passes +0.05 gate. Loglinear (worse fit, optimistic upper bound) says N=200 → +0.050. Both consultants' N-expansion priors (grok +0.11 at N=250; deepseek +0.05 at N=200) match Loglinear, not the better-fit Pareto. **First-principles read: the wall is structural (12 of 18 UPDRS-III items are non-gait-observable), not data-quantity-bound within this cohort.** External labeled cohorts (Hssayeni / MJFF for transfer at external N>200) or task-protocol expansion are the only theoretically-bounded levers above 0.60. Findings F58 LC section.

**T3 iter21 (2026-05-04 PM) — Nested-CV hybrid (iter5 + 18 per-item gated) → Ridge meta — Phase B 5-fold gate FAIL by wide margin.** Implements all 4 F54 bug fixes (genuinely nested CV; T3-native loader at N=98; pre-reg `--write-prereg`/`--run` split with `formula_sha256` validation; hybrid endpoint = `updrs3` directly via Ridge meta — no sum-of-items intercept correction). Pre-reg `results/preregistration_t3_iter21_nested_20260504_152155.json` (formula_sha256 `3e6557bf4d9150a6`). Result at N=98 5-fold (3 seeds × 5 outer × 5 inner): hybrid CCC = +0.3389 ± 0.0429 vs iter5 5-fold (recomputed in same nested wrapper) +0.4856 ± 0.0300 → **Δ = −0.1467** (worse than F53's −0.107); bootstrap 95% CI [−0.2542, −0.0197], frac>0=0.013. LOOCV lockbox SKIPPED per protocol. Mechanism: Ridge α=1.0 too weak for 19 collinear inner-OOF predictors at N≈78 outer-train; meta-learner inflated per-item weights (item 11 +5×; suppressors items 6/14/16) and SUPPRESSED iter5 weight to ~0.4. F55 orthogonality probe (+0.327) was real but raw residual Pearson r over-estimates harvestable lift — extracting it via 19-parameter meta-model at N≈100 hit curse of dimensionality. **6th N=94/N=98 wall data point** — wall now affects all four probe-strategy classes: feature engineering (F19/F44/F45/F48/F51), composition (F53), AND nested mixing (F56). Findings F56.

**T3 iter28 (2026-05-05 PM) — SOTA pipeline shootout — NEGATIVE; 10th wall data point spans algorithm class (F63).** Triple-CLI consult (codex gpt-5.5 xhigh + gemini-3.1-pro-preview + kimi via opencode/openrouter) selected three SOTA candidates: AutoGluon (best tabular AutoML, 8-model ensemble), MultiROCKET (top time-series shape-feature extractor, aeon library, 79968 random-kernel features per subject), TabPFN-v7 / TabM 0.0.3 / TabR (DL tabular). Pre-registered shootout: 5-fold × 3 seeds {42, 1337, 7} under iter5 lockbox conditions; same-fold same-seed iter5 baseline as comparator; gate Δ ≥ +0.025 with seed std < 0.020. **Results:** iter28a iter5_Stage1+AutoGluon_Stage2 (180s/fold) → mean CCC=0.4534, **Δ=−0.0322** (3/3 seeds <iter5: −0.006/−0.034/−0.057); iter28b iter5_Stage1+MultiROCKET-RidgeCV_Stage2 → mean CCC=0.2323, **Δ=−0.253 CATASTROPHIC** (Ridge α* pinned at 100 for all seeds, confirms Ridge maxes regularization but cannot recover residual variance from 79968 ROCKET kernels); iter28c TabPFN/TabM/TabR BLOCKED (TabPFN-v7 paywalled post-Nov 2025 PriorLabs license; TabM 0.0.3 ships as raw PyTorch module without sklearn fit/predict). **Mechanism (codex+gemini+kimi convergence):** AutoGluon 8-model ensemble diversity gain washed out by per-model bagging variance at N=98; MultiROCKET shape features orthogonal to V2's 1751 hand-crafted statistical features for residual modeling. Historical negative; after iter41, do not cite old 0.5227 as canonical. CSVs `results/iter28a_autogluon_5fold_20260505_154123.csv` + `results/iter28b_multirocket_5fold_20260505_154557.csv`. Findings F63.

**T3 iter26 (2026-05-05 PM) — Hssayeni MJFF acquisition — BLOCKED at Synapse DUA gate (F62).** Verified all candidate Synapse IDs: `syn20681023` MJFF Levodopa Response Study (Hssayeni 2021 source) is the correct project but anonymous children listing returns 404 (DUA-gated). Most richer PD+UPDRS Synapse projects (`syn8717496` PDDB DREAM, `syn4993293` mPower, `syn21344932` BEAT-PD) are similarly gated. Built `run_t3_iter26_hssayeni.py` orchestrator (~250 lines, modes: probe/download/extract/write_prereg/run) with corrected Synapse ID. Probe surfaces the gate cleanly: "AUTH FAIL: No valid authentication credentials provided" + actionable next steps for user. iter26 architecture FROZEN: Stage 1 Ridge on shared clinical {age, sex} + Stage 2 LGB on common wrist features (mirrors iter25b 64-col schema). Two evaluations were planned: E1 WG-LOOCV (joint-trained on WG+Hssayeni; after iter47 this must compare against valid-range corrected T3 0.3784, not old iter5 0.5227), E2 Hssayeni-LOOCV (first published cross-cohort UPDRS regression number). **USER ACTION REQUIRED to unblock**: Synapse account → DUA application on syn20681023 (1-3 day approval) → Personal Access Token → place in `~/.synapseConfig`. iter26 scaffolding ready; pre-reg deferred until data lands. Findings F62. FoG-STAR and COPS are now known public direct T3 external routes; both are external-validity evidence only, with no internal T3 canonical change. A follow-up 2026-05-08 route audit closed mPower (phone/self-reported), REMAP Bristol (controlled N=12/range labels), Oxford OPDC/OxQUIP (no current public aligned IMU route), and PD-BioStampRC21 (N=17 forearm/chest/thigh sensors) as no-prereg/no-download leads for the active T1/T3 CCC objective.

**T3 iter27 (2026-05-05 PM) — Tail-aware Stage-2 retrain — NEGATIVE; 9th wall data point (F61).** Codex consult on 5 candidate angles ranked Stage-3 calibration as cheapest but recommended **wildcard: tail-aware direct iter5 retraining**. Empirical pre-check: nested-LOO post-hoc calibration (linear/isotonic/poly2) on iter5 OOF gave Δ ≈ −0.08 with bootstrap frac>0=0 — confirming F54 residual structure (corr(residual, y_true)=−0.699; Q1 over-predicted by +9.4, Q4 under-predicted by −7.6) is **regression-to-the-mean shrinkage**, not recoverable signal. iter27 applied codex's wildcard via `run_t3_iter27_tail_aware.py` with 5 weight schemes (uniform / inv_density / abs_z / tail_focused / quartile_balanced) and severity-stratified KFold. Best variant: **tail_focused Δ=+0.0128** (driven entirely by seed=42 outlier; per-seed 0.027/0.004/0.007). Std=0.041 fails the std<0.020 floor; Δ fails the +0.025 floor. **Tail residuals barely moved** (Q1 +9.61→+9.71; Q4 −8.34→−8.79 across schemes) — confirms shrinkage is in LGB-tree-leaf-prediction-mean structure, not loss-weighting space. With CCC objective enabled (post-hoc affine calibration per F50/F46), CCC dropped catastrophically to 0.31-0.41 — CCC objective is a trap at iter5 T3 architecture level. **5-fold gate FAIL on both weight-only and CCC-objective variants.** LOOCV lockbox SKIPPED. The shrinkage is necessary at N=98; removing it costs Pearson r more than it gains MAE. Hssayeni scaffolding (`cache_hssayeni_features.py`, `scripts/synapse_hssayeni_setup.md`) in place for iter26 if Synapse DUA is pursued. Findings F61.

**T3 iter25b (2026-05-05 PM, SUPERSEDES iter25 F60) — Cross-dataset zero-shot transportability on PADS, FIXED — NO TRANSFER STANDS, narrative SHARPENED (F60b).** After first-order debug + triple-CLI consult flagged 4 upstream bugs in iter25 (units/gravity, axis frame, gait_reg meaningless, LeftWrist mirror), iter25b applied all fixes: WG `R_Wrist_FreeAcc_E/N/U` + PADS ×9.81 (g→m/s²); drop gait_reg; RightWrist-only; runtime fs + gravity-removal assertions; primary headline = magnitude-only Track A3 (frame-invariant). Full PADS 7810/7810 download. **355 subjects (276 PD + 79 HC), 3843 RightWrist sessions.** Sanity checks PASSED (fs=99.35Hz vs 100; mean |acc|=0.0037g gravity-removed). Scale ratios collapsed from 60-110× to 1.3-2.4× — Fix A worked. **Track A3 AUROC = 0.4975 (chance) — primary headline; iter25 verdict STANDS.** Other tracks: A2=0.4049, B2=0.3284 (mean-imputed Stage-1 mechanism confirmed BELOW chance), D2=0.3364, A3D2=0.4387. **NEW: Track C2 PADS-only ceiling JUMPED from 0.63 → 0.7874 ± 0.025** with full data — wrist features have substantial within-cohort PD signal but iter5's WearGait representation does not transport. Pre-reg `results/preregistration_t3_iter25b_pads_20260505_131413.json` (formula_sha256 `4f67518ee293178f`). Both consults converged: **task/protocol mismatch dominates** (WG gait/balance training vs PADS stationary upper-limb), not sensor-frame mismatch. **Sharpened paper Table 3: 0.79 within-PADS ceiling vs 0.50 cross-dataset = clean representation-orthogonality finding** — feature space contains signal, but iter5's learned axis can't read it. Core lesson: **structural harmonization (units/axes) is meaningless without semantic (clinical protocol) harmonization.** Findings F60b.

**T3 iter23 + iter24 (2026-05-05) — Clinical-extras Stage-1 ablation + Stage-2 forced-inclusion — DEAD at N=98 (F59).** Built `cache_clinical_extras.py` extracting Tomlinson-2010 LEDD + MDS-UPDRS Part 1 + ON/OFF state + assistive-device + race + PT-OT + days-since-Part3 from raw clinical CSV (98/98 V2-cohort match, manifest leakage-clean). **Partial-r residualization** against (H&Y + cv_yrs + cv_sex + cv_dbs) collapses signal across the board: only `part1_cognitive` (+0.232, 37% NaN), `assistive_device_yn` (+0.156), and `hours_since_last_dose` (−0.158) retain |partial r| > 0.15. **iter23** 19-set 5-fold ablation (`run_t3_iter23_clinical_ablation.py`, 76s wall on 11 workers): zero passers, monotone Δ ≤ 0; pairs/kitchen-sink hurt MORE than singles (mechanism: partial-r collapse + Ridge α=1.0 DOF amplifier per codex+gemini synthesis). **iter24** Stage-2 forced-inclusion (`run_t3_iter24_stage2_forced.py`, custom `_feature_select_fold_forced` always retains the 3 partial-r winners + K-3 V2 cols by importance): pre-reg `results/preregistration_t3_iter24_stage2forced_20260505_053134.json` (formula_sha256 `7194964bd5ec195b`). Result at N=98 5-fold: iter24 +0.4747 ± 0.0341 vs iter5 +0.4856 ± 0.0300 → **Δ = −0.0110**; bootstrap CI [−0.0371, +0.0150] **straddles zero**, frac>0 = 0.176. Smallest negative of any architectural variant tested in this codebase but still gate-FAIL. iter5 ≡ iter24 statistically indistinguishable. **8th N≈98 wall data point.** Wall now spans all 8 probe-strategy classes including Stage-2 forced-inclusion (the cleanest architectural lever for new features). Structural ceiling 0.5975 (F58 Pareto fit) re-confirmed. Findings F59.

## Commands

```bash
# Reproduce canonical headlines
./gpu.sh compose_t1_iter12_honest.py                                       # T1: LOOCV CCC=0.6550
./gpu.sh run_t3_iter47_invalid_code_fix.py --mode run                      # T3 corrected valid-range target: LOOCV CCC=0.3784
./gpu.sh run_t3_iter47_invalid_code_fix.py --mode loso                     # T3 corrected valid-range LOSO two-way CCC=0.150

# Remote GPU workflow (slave has torch, lightgbm, xgboost, momentfm; master does not)
./gpu.sh <script.py> [args]    # rsync code + run on remote
./gpu.sh --pull                # fetch results back to ./results/
./gpu.sh --status              # GPU + running jobs
./gpu.sh --push-cache          # upload local cache to remote
./gpu.sh --log                 # tail latest log
./gpu.sh --ssh                 # shell into remote
./gpu.sh --setup               # provision a fresh slave
./gpu.sh --nuke                # kill all python jobs on remote

# Local environment (no torch/lightgbm — use uv for tests, paper-gen, syntax checks)
uv sync                                                  # install deps
uv run pytest tests/ -v                                  # all tests
uv run pytest tests/test_inductive_leakage_fix.py -v     # leakage regression suite
uv run pytest tests/test_inductive_lib.py -v             # fold-local helpers
uv run python -m py_compile run_*.py compose_*.py        # syntax-check before any GPU push
uv run python render_current_paper.py                    # authoritative current manuscript → CURRENT_PAPER.html
```

**Swap GPU servers:** edit lines 19-20 of `gpu.sh` (or `export GPU_REMOTE=user@x.x.x.x GPU_PORT=NNNN`) and run `./gpu.sh --setup`. Current slave: `fiod@165.22.71.91:2243` (RTX 4060, PyTorch/cu runtime in `.venv`).

## Architecture

**Pattern: shared modules + self-contained `run_*.py` experiment scripts + one fold-firewall library + per-item composers.**

```
data_split.py       ← shared: clinical parsing, windowing, split creation
project_paths.py    ← shared: artifact paths, env overrides (WEARGAIT_*)
updrs_columns.py    ← shared: UPDRS subitem column resolution
eval_utils.py       ← shared: lins_ccc, cal_slope, feature selection, bootstrap CIs
inductive_lib.py    ← shared: FoldImputer / FoldNormalizer / FoldSeverityBins / 5-null gate
                      Single source of truth for the train/test firewall. Anything that "fits"
                      on training-fold data must use these helpers.

run_*.py            ← self-contained experiments (import the 5 modules above + std libs only)
cache_*.py          ← feature-extraction one-shots (write to results/*.csv)
compose_*.py        ← per-item OOF composers; produce hybrid composite predictions
```

**Cross-import exceptions:** `run_clean_benchmark.py`, `run_ablation_v3.py`, `run_paper_supplements.py` import feature-extraction functions from `run_ablation_v2.py` and `run_proven_stack.py`. No other cross-imports between `run_*.py` files.

**Execution model:** Code lives on MASTER. `gpu.sh` rsyncs to the remote GPU slave, runs there, `--pull` fetches results. Remote has the 52GB dataset at `data/raw/weargait-pd/`.

**Architectural patterns that won (the playbook):**
- **Per-item gated architecture** — predict each item separately, sum to get T1. Each item picks its best architecture (V2 alone vs V2+TUG vs hy_residual vs self_norm vs CCC objective).
- **H&Y residualization (Stage-1 Ridge → Stage-2 LGB)** — adds clinical signal not in IMU. Drives T3 (CCC 0.21 → 0.41 → 0.52 with extra clinical) and items 9, 10, 18.
- **Self-normalization across homologous-metric sensor groups** — subtract subject's median across e.g. all Pitch_mean values. Removes idiosyncratic baseline (scoliosis, body habitus, mounting) while preserving inter-sensor differences. Cracks items 10 and 13; items 11, 14 don't benefit.
- **Custom CCC objective for LightGBM (v2)** — needs `init_score=mean(y)` + Pearson feature selector + `hessian=1.0` scaling + post-hoc affine calibration. Without these fixes the CCC objective HURTS most items. Drives items 12 and 18.
- **TUG phase-segmented features** — 6-phase segmentation around Lumbar Acc-mag spike. Boosts items 10, 12, 14, 7, 8 but HURTS items 9, 11, 13 (use hy_residual for those).
- **Per-fold K=500 LGB importance feature selection** — must be inside CV; never global.

**T3 theoretical ceilings (derived 2026-04-29 iter 1.3):** Bound D (perfect-T1 → T3) = 0.683; Bound A (oracle T1 + mean R, realistic IMU-only max) = 0.351; Bound E (inductive shrinkage T1_pred → T3) = 0.171. hy_residual breaks Bound A by adding non-IMU clinical signal. Don't chase numbers > Bound A without external/clinical data.

## Data: WearGait-PD

On remote at `data/raw/weargait-pd/` (52GB). Clean split: `results/paper3_split.json` (seed=20260309).

**Per sensor (13 sensors × 22 channels = 286 IMU channels):** Acc_XYZ + Gyr_XYZ used; FreeAcc_E/N/U + Roll/Pitch/Yaw partially used (axial cache); Mag_XYZ + VelInc + OriInc unused.

**Tasks:** SelfPace, HurriedPace, TUG, Balance, TandemGait + _mat/_matTURN variants.

**Two collection sites** identifiable from SID prefix: NLS (70 PD) and WPD (28 PD). Asymmetric leave-site-out (NLS→WPD CCC=0.66; WPD→NLS CCC=0.12). Site is a **strong T3 confounder** (LOSO CCC ≈ 0).

**T1 = items 9-14 = Schrag 2007 axial subscore (items 9-13) + body bradykinesia (3.14).** Items 1-18 in `per_item_scores.json` are standard MDS-UPDRS Part III items 3.1-3.18.

## Inductive Firewall (the architectural law)

After the 2026-04-28 leakage audit, every new experiment script must:

1. **Fit fold-local helpers only** — use `inductive_lib.py` (`FoldImputer`, `FoldNormalizer`, `FoldSeverityBins`). No global imputers, no cohort-wide z-scoring, no pre-computed ranks/anchors/prototypes touching test-fold data.
2. **Pass the 5-null gate** before any reported number:
   - **Scrambled-label sanity** — shuffle train PD targets; expect test CCC ≈ 0.
   - **Subject-ID shuffle before cache join** — also CCC ≈ 0; catches leaks via cache join keys.
   - **Canary feature** (injected only into test fold) — model must be unable to use it.
   - **Library-exclusion assertion** (kNN/retrieval) — assert no test SID in retrieval pool.
   - **Transductive sanity variant** — intentionally leak target; expect CCC ≈ 0.85; proves architecture CAN learn.
3. **Lockbox protocol** — 5-fold for screening across many configs, single pre-registered LOOCV for headline. Never re-run LOOCV on multiple variants and pick the best — that's adaptive test-set reuse.
4. **Composite-level cherry-picking** — The lockbox catches single-pipeline cherry-picking but NOT composite-level cherry-picking across multiple pre-registered lockboxes (the iter11A failure mode). Composer scripts must use single coherent batches OR pre-register the composite formula before any per-item LOOCV.
5. **Report transductive AND inductive numbers side-by-side** — the gap is the leakage estimate.

### Concrete leakage-cause citations (grep patterns to avoid)

- **Pre-computed target-derived structures outside the CV loop:** `run_compression_ablation.py:1015` (XGBRanker fit on all 178; leaf indices encoded test-fold rank). Cost ΔCCC=0.343 on T1 5-fold.
- **Hyperparameter tuned on the test vector and reported on the same vector:** `run_calibration_v2.py:861` (T_grid optimised on the same N=94 LOOCV preds it then evaluated; slope pinned to 1.000 by construction). Always nest CV.

## Companion docs / Ownership

- **`CLAUDE.md` (this file)** — architecture, commands, current canonical numbers.
- **`AGENTS.md`** — leakage rules and agent-facing brief. **Source of truth if it disagrees with CLAUDE.md.**
- **`findings.md`** — full ablation history, codex/gemini consultations, "what failed" anatomy, F0–F12 sections.
- **`progress.md`** — append-only progress log with timestamps and errors.
- **`task_plan.md`** — current execution plan.
- **`SWARM_PLAN.md` / `SWARM_PLAN.json`** — current live swarm plan (when present). Pick up in-flight phases from here before starting new work.
- **`paper.md`, `review_report.md`** — paper context (SOTA, MCID 3.25, Hssayeni cross-dataset).
- **`~/.claude/projects/-home-fiod-medical/memory/MEMORY.md`** — auto-memory across sessions.

## Gotchas

- **FM embedding aggregation:** `fm_embeddings.npz` has recording-level data with no SIDs. Use `rocket_recordings.npz["sids"]` for subject mapping.
- **Two split files:** `data_split.json` (old, seed=42, CONTAMINATED) vs `paper3_split.json` (clean, seed=20260309). Always use `paper3_split.json`.
- **Feature selection inside folds:** XGB importance-based selection (K=500) must be per fold, not once globally.
- **LightGBM `device='gpu'` is 2.2× SLOWER than CPU** for N<200. Always use CPU.
- **Demographics ridge claim was wrong on T3:** older notes saying "demographics beats IMU on T3" do not reproduce. **B1_v2_only > B4_demo on T3.**
- **HC anchors HURT inductively:** `inductive_pd > inductive_pd_hc`. Drop HC from any deployment pipeline. HC are diagnostic-only (transductive sanity / leakage tests).
- **Use `render_current_paper.py` for current paper work** — it renders `paper.md` to `CURRENT_PAPER.html` and validates post-audit snippets. `generate_paper_v4.py` / `NEW4.html` are legacy archaeology with stale pre-leakage narrative fragments, not current evidence.
- **N=94 vs N=89 filter** shifts absolute CCC by ~0.06 but preserves ranking. Filter to T1 items only (94) vs all T3 items (89). Cite the filter when reporting.
- **T3 target construction:** the old `updrs3` column exactly matches a raw 33-subitem Part III skipna sum, converted three all-missing rows into zero labels, and treated invalid `9` codes in `NLS036` item 3.15 R/L as real severity. Current T3 claims must use iter47 valid-range cohorts from `run_t3_iter47_invalid_code_fix.py`; the old V2 `updrs3` target and iter41's no-valid-range correction are historical/superseded audit artifacts.
- **Custom CCC LGB objective** silently HURTS without the v2 fixes (init_score, Pearson selector, hessian scaling, affine calibration).
- **Remote-only deps:** torch, torchvision, lightgbm, xgboost, momentfm, sktime, tslearn — installed via `gpu.sh --setup`, NOT in `pyproject.toml`.

## What Failed

Full archive in `findings.md` (sections F0–F12) and the "What Failed" section there. Don't retry without reading it. High-level categories that are dead:
- All handcrafted feature group expansions (stride-var, asymmetry, nonlinear, frequency, interactions) at this N.
- 5 end-to-end DL architectures, HARNet end-to-end fine-tuning (iter37), item decomposition, MoE, severity stratification.
- HC anchors / HC normative AE / HC-anchored rankers.
- Frozen MOMENT FM as severity feature (captures group, not within-PD).
- Privileged distillation, FM MLP adapter, 4-base-learner stacking.
- Post-hoc isotonic/Platt/polynomial calibration; per-item ordinal + temperature; NGBoost; pairwise contrastive boosting.
- TabPFN-2.5 (paywalled post-Nov 2025).
- IMU feature additions at iter5 architecture (event-axial, unsigned-asymmetry — both hurt; the formalized 5-fold ≥+0.05 / seed-std <0.02 gate is the lesson).
- Sensor-fusion at N=94 (stride-locked, joints_v2, cross-sensor coherence, Mahalanobis-to-HC, late-fusion Ridge stack — all fail 5-null gate or LOOCV +0.015 threshold).
- **Per-item gated T3 composite (iter19, F53 2026-05-04):** sum 18 per-item OOFs under a pre-registered single-batch architecture map (items 1-3 v2_baseline backfill, items 7-8 iter17:hy_residual_item_v2 5-fold winners, items 16-17 iter17:item_plus_v2 5-fold winners, items 4-6/9-14 iter8 lockboxed, items 15+18 iter17 lockboxed). 5-fold vs iter5 (both N=94 subset): composite +0.299 ± 0.020 vs iter5 +0.405 ± 0.036 → Δ = −0.107. **Variance compounding** (gemini's predicted Angle-1 failure mode #1) overwhelms per-item gains. Direct iter5 regression captures cross-item shared variance via 9-feature Stage-1 (H&Y + cv_yrs + cv_sex + cv_dbs) more efficiently than 18 separately-fit per-item models at this N. 5th N=94 wall data point — wall affects PROBE STRATEGY (composition vs direct), not just FEATURE STRATEGY.
- **Plan-next.md ablation: 1-parameter convex blend + Stage-1 widening (iter22, F58 2026-05-04):** 9-cell ablation matrix synthesized from grok-4.3 + deepseek-v4-pro consult. ALL 9 cells fail their gates. Best blend (BB1 (α,β) joint) lands Δ=−0.013; every other configuration is worse. α* non-degenerate (mean 0.682, 0% boundary); β* stable in T1=94 cohort (5.27, 0 sign flips). Mechanism: F55's r=+0.327 5-fold residual orthogonality does NOT survive at LOOCV scale (raw Pearson r overestimates harvestable lift across CV-scale changes). Ridge/OLS k=2 meta destroys iter5 calibration via the same overfit mechanism as F56 k=19. Stage-1 widening alone (8-cov Ridge, A_iter22_8cov) hurts by Δ=−0.023; widening + blend compounds to Δ=−0.041. **Don't retry any blend or Stage-1-widening variant at this N.** The k=1 mixer is now confirmed dead, joining k=2 (BB2/BB3) and k=19 (F56). Historical 7th wall data point; after iter47, interpret future T3 work against valid-range corrected-target 0.3784 rather than old 0.5227.

- **Nested-CV hybrid iter5 + 18 per-item → Ridge meta (iter21, F56 2026-05-04):** properly nested CV with `formula_sha256`-validated single pre-reg, T3-native N=98 loader, hybrid endpoint = updrs3 via Ridge meta. 5-fold gate FAIL by Δ = −0.1467 (worse than F53). Mechanism = curse of dimensionality at k=19 / N≈78 outer-train + Ridge α=1.0 under-regularization; meta blew up (iter5 weight suppressed to ~0.4, item 11 inflated to +5×, suppressor items 6/14/16). F55 +0.327 orthogonality is necessary-but-not-sufficient for hybrid lift — raw residual Pearson r over-estimates harvestable lift at N≈100 with k≈20 base predictors. iter20 (single-loop hybrid, F54 leakage) and iter21 (nested) both DEAD; nested is the methodologically clean negative. **Don't retry 19-feature meta-stack on this composite at this N**; would need ≪10 base predictors OR α≥10–100 Ridge OR 1–2-parameter convex mix.

- **SOTA pipeline shootout (iter28, F63 2026-05-05 PM):** triple-CLI consult selected AutoGluon + MultiROCKET + TabPFN/TabM/TabR. **iter28a** iter5_Stage1+AutoGluon_Stage2 (180s/fold, 8-model ensemble): mean Δ = −0.0322 across 3 seeds (all <iter5: −0.006/−0.034/−0.057). **iter28b** iter5_Stage1+MultiROCKET-RidgeCV_Stage2 (79968 random kernels): mean CCC=0.2323 vs iter5 0.4856 → Δ=−0.253 CATASTROPHIC; Ridge α* pinned at top of grid for all seeds. **iter28c** TabPFN-v7 paywalled / TabM 0.0.3 raw PyTorch / TabR similar — blocked. Mechanism: AutoGluon diversity gain washed out by per-model bagging variance at N=98; ROCKET shape-features orthogonal to V2 statistical features for residual modeling. **F63 triangulates with F58 Pareto asymptote 0.5975 — wall is structural, orthogonal to algorithm choice.** 10th wall data point closing the algorithm-class angle. **Don't retry**: AutoGluon variants (longer budget, fastai install, fusion-stacking — codex prior all <Δ+0.02), pure ROCKET-family (Mini/Multi/Hydra) Stage-2 heads, Ridge-on-shape-features at this N. TabPFN-v7 evaluation requires PriorLabs license.
