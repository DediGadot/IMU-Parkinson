# Repository Instructions

## Current Truth
- This repo is a WearGait-PD Parkinson's motor-severity research codebase, organized as shared utilities plus many standalone `run_*.py` experiment scripts.
- Read `CLAUDE.md`, `findings.md`, `progress.md`, and `task_plan.md` before changing experiments; they contain the post-2026-04-28 leakage audit and lockbox results. **CLAUDE.md is the canonical-numbers source of truth; this file (AGENTS.md) governs leakage discipline and agent behaviour.**
- The original SSL/ranking breakthrough numbers are transductive and leaky; do not cite them as deployment results.
- **Current honest canonical numbers (post-2026-05-03):**
  - T1 LOOCV CCC `0.6550`, MAE `1.561` via `compose_t1_iter12_honest.py` (single iter8 batch 20260430_143044, no swaps). Pre-reg: `results/preregistration_t1_iter12_honest_*.json`. Batch-integrity audit: `results/t1_iter12_batch_integrity_audit_20260508.json` passes with max summed-OOF diff `0.0`.
  - Strongest current corrected non-canonical T1 candidate: iter34 hygiene-corrected hybrid CCC `0.7170`, MAE `1.736` on N=92 via `run_t1_iter34_hybrid_8item_multibase.py` with prereg `results/preregistration_t1_iter34_hygiene_corrected_20260510_200037.json` and result `results/lockbox_t1_iter34_hybrid_20260510_233019.json`; report only as "hygiene-corrected candidate / post-publication replication target", not canonical floor. The original iter34 CCC `0.7366` on N=93 is now superseded for current-candidate citation by this valid-auxiliary rerun because `NLS036` had invalid auxiliary item15 total `18` from raw 9/9 missing codes and item15 is upstream of at least one T1 item in 2/3 locked seeds. The hygiene-corrected run excludes both `NLS036` and `WPD002`, classifies as degraded-but-still-strong (`0.7000 <= CCC < 0.7366`), remains above iter12, and does not complete the model ceiling-break objective. P2 noisy-test audit status for the original architecture remains caveated: `results/iter34_p2_robustness_20260508.json` shows all point deltas pass the one-sided leakage margin, but the max bootstrap upper bound is `+0.0857` (> +0.05), so do not describe iter34 as "all null gates green".
  - T1 iter46 ET-only robustification is diagnostic only: `audit_t1_iter34_base_item_decomp.py` found ET-only clears the P2 bootstrap screen, but the pre-registered LOOCV `run_t1_iter46_et_robust.py` produced CCC `0.7269`, below iter34 by `-0.0097` and not strictly above iter12 by paired bootstrap (`frac>0=0.9388 < 0.95`). Do not promote it and do not run more base-subset LOOCVs from that screen.
  - **T3 corrected valid-range target LOOCV CCC `0.3784`, MAE `7.528` via `run_t3_iter47_invalid_code_fix.py --mode run`**, minimal valid-range cohort N=95 excluding PD rows with all 33 raw Part III subitems missing (`NLS151`, `NLS188`, `WPD013`) and recoding raw Part III subitem values outside 0-4 to missing. Load-bearing target change vs iter41: `NLS036` item 3.15 R/L were both coded `9`, so old target `46` becomes `28`. Architecture is the same iter5 A3 Stage 1 (H&Y + cv_yrs + cv_sex + cv_dbs) and current Stage 2 V2 residual pool. Pre-reg/audit declaration: `results/preregistration_t3_iter47_invalidcode_20260508_194605.json`. Stage-2 no-cv sensitivity CCC `0.3771`, MAE `7.680`; complete33-validrange sensitivity N=88 CCC `0.4281` current / `0.4010` no-cv, not a headline.
  - T3 clinical-dependency framing audit: `audit_t3_clinical_dependency.py` / `results/t3_clinical_dependency_20260508.json` was run on the iter41 target and showed no-Stage2-cv corrected-target sensitivity decomposes as A3 H&Y+intake CCC `0.4017`, intake-only (`cv_yrs`, `cv_sex`, `cv_dbs`) `0.3871`, H&Y-only `0.2899`, and intercept/IMU-only `0.2449`. Superseded numerically by iter47, but framing holds: corrected T3 is **clinical/intake + IMU**, not pure IMU deployment.
  - **T3 corrected valid-range LOSO two-way mean CCC `0.150`** (NLS→WPD `0.194`, WPD→NLS `0.106`) via `run_t3_iter47_invalid_code_fix.py --mode loso` on the same N=95 minimal valid-range cohort and current Stage 2 policy. Stage2-no-cv LOSO sensitivity is `0.163`; complete33-validrange sensitivities are `0.106` current and `0.116` no-cv.
- **Retracted numbers — do NOT cite:**
  - T1 iter11A `0.7241` (2026-05-01) was retracted on 2026-05-03 after independent leakage scrutiny found multi-layer adaptive variant selection across iter6/iter8/cccv2/iter10/iter11 batches. Paired bootstrap of (iter11A − iter12) on N=94: mean inflation `+0.070`, 95% CI `[+0.029, +0.113]`, 99.9% > 0. iter11A remains as supplementary "post-hoc per-item-best with full disclosure"; the canonical T1 is iter12 honest at `0.6550`.
  - T3 iter5 `0.5227`, T3 iter16 IPW `0.4694`, T3 iter16 LOSO `0.341`, and iter41 corrected-target `0.3948` are **not current canonicals** after the 2026-05-08 iter47 valid-range target audit. Root causes: `updrs3` was built by skipna summing raw Part III columns, so three PD rows with all 33 Part III subitems missing were included as true zero labels, and `NLS036` item 3.15 R/L invalid `9` codes were treated as real severity (+18). They remain historical audit artifacts only.
  - Pre-iter5 T3 `0.4092` via canonical hy_residual (no clinical extras) is also target-contaminated by the same `updrs3` construction and is historical only.
  - The very old (pre-leakage-audit) T1 `0.588` via `inductive_pd` and T3 `0.217` via `B1_v2_only` are historical baselines, not current canonicals.
- The paper framing is now a cautionary benchmark: first strict inductive WearGait-PD UPDRS-III regression, anatomy of leakage, and realistic deployment ceilings under both internal validity and cohort shift.
- Historical clean-split Paper3 notes (`CONT.md`, `EXP.md`, `LEARNINGS.md`, `VNEXT.md`) remain useful for contamination history, but later post-leakage files supersede old `6.89`, `0.86 CCC`, and transductive ranking claims.
- `task_plan.md` is intentionally a current head plus historical archive; `audit_task_plan_current_scope.py` must pass before using it for next-action decisions. Latest audit `results/task_plan_current_scope_audit_20260509.json` passes and keeps old success-tier thresholds archive-bound.

## Commands
- Set up local env with `uv sync`; use `uv run ...` for local Python commands.
- Run all tests locally with `uv run pytest tests/ -v`.
- Focus leakage tests with `uv run pytest tests/test_inductive_leakage_fix.py tests/test_inductive_lib.py -v`.
- Check shared split/path contracts with `uv run pytest tests/test_data_split.py tests/test_project_paths.py -v`.
- Syntax-check scripts with `uv run python -m py_compile data_split.py project_paths.py inductive_lib.py run_*.py render_current_paper.py generate_paper_v4.py`; `generate_paper_v4.py` is legacy syntax-only coverage, not the current paper route.
- Run heavy experiments remotely through `./gpu.sh <script.py> [args]`, then pull artifacts with `./gpu.sh --pull`.
- Check remote state with `./gpu.sh --status`, tail the latest remote log with `./gpu.sh --log`, and upload cached artifacts with `./gpu.sh --push-cache`.
- Render the current post-audit paper with `uv run python render_current_paper.py`, which writes `CURRENT_PAPER.html`. Treat `generate_paper_v4.py` and `NEW4.html` as legacy/stale archaeology only; they still contain pre-leakage narrative fragments and must not be used as current paper evidence.

## Remote And Artifacts
- `gpu.sh` is the master/slave boundary: local repo is source of truth, current remote `/home/fiod/pd-imu` holds data, GPU deps, and heavy caches.
- Current `gpu.sh` default remote is `fiod@165.22.71.91:2243`; override with `GPU_REMOTE` and `GPU_PORT` instead of editing code when possible.
- Raw WearGait-PD data is about 52 GB and should stay under remote `data/raw/weargait-pd/` or an env-provided `WEARGAIT_DATA_DIR`.
- Canonical generated metrics, caches, and logs belong in `results/`; figures belong in `figures/`.
- Use `project_paths.py` helpers and `WEARGAIT_*` env overrides for data/results/cache/split paths instead of hard-coded repo-root, `/home/fiod/pd-imu`, or historical `/root/pd-imu` paths.
- `results/paper3_split.json` is the clean historical Paper3 split; `results/data_split.json` is the default compatibility split and should not be silently substituted in papers.

## Architecture
- Shared modules agents may import: `data_split.py`, `project_paths.py`, `updrs_columns.py`, `eval_utils.py`, and `inductive_lib.py`.
- `inductive_lib.py` is the fold-firewall source of truth: use `FoldImputer`, `FoldNormalizer`, `FoldSeverityBins`, `full_metrics`, and null-gate helpers instead of reimplementing fit/transform logic.
- Keep experiment runners self-contained. Avoid cross-imports between `run_*.py` files except existing helper reuse from `run_inductive_ablation.py`, `run_calibration_v2.py`, or legacy feature extractors when already established.
- **Canonical lockbox scripts (cite numbers from these only):**
  - T1: `compose_t1_iter12_honest.py` (uses pre-existing per-item iter8 OOFs from batch 20260430_143044).
  - T3 corrected valid-range LOOCV + LOSO audit truth: `run_t3_iter47_invalid_code_fix.py`.
  - T3 all-missing-row target audit predecessor: `run_t3_iter41_target_fix.py`.
  - T3 iter5 / iter16 target-contaminated historical reproduction only: `run_t3_iter5_clinical.py`, `run_t3_iter16_site_ipw.py`.
- **Cache scripts with complete concrete manifest sidecars (safe to feed inductive headlines):** `cache_item11_multiscale.py` → `results/item11_multiscale.csv` + `.manifest.json` and recording-level companion `results/item11_multiscale_recordings.csv` + `.manifest.json`; `cache_clinical_extras.py` → `results/clinical_extras.csv` + `.manifest.json`; `cache_harnet_embeddings.py` → `results/harnet_subj_embeddings.csv` + `.manifest.json` (git SHA backfilled on 2026-05-08 from matching script_sha256 evidence at commit `d281a0e`; the frozen-HARNet route is empirically dead but the cache sidecar is now concrete). Latest manifest audit has 45 cache-like artifacts: 4 complete clean, 8 partial, 33 missing. `results/cache_backfill_candidates_20260508.{json,md}` is non-mutating triage only: item-specific/unused-channel caches have committed script-hash evidence but still need command/schema backfill, phase-locked caches need a committed exact script first, and indomain-SSL/COPS/TLVMC external caches should not be backfilled for internal headlines. `results/missing_cache_manifest_origin_audit_20260509.{json,md}` classifies the 33 still-missing sidecars by producer evidence and explicitly does not make them headline-safe. `results/cache_backfill_decisions_20260508.{json,md}` records that item-specific/unused-channel remain partial because command/runtime evidence is missing. `results/manual_cache_backfill_evidence_20260509.{json,md}` inspects the 5 missing-manifest manual candidates (`hc_ssl_subj_embeddings.csv`, `joints_v2_subj.csv`, `moment_subj_embeddings.csv`, `stride_locked_subj.csv`, `tug_transition_features.csv`) and leaves all five `leave_missing_no_patch` because exact command/runtime evidence is absent and required source inputs are broken/missing. **All other `cache_*.py` outputs need manifest backfill before reuse for inductive lockboxes** (per pd-imu-100x-researcher skill provenance rule).
- Current post-leakage source scripts include `run_inductive_ablation.py`, `run_baselines.py`, `run_demo_residual.py`, `run_event_features.py`, `run_phase4_distill.py`, `run_phase5_fm_adapter.py`, `run_phase6_stack.py`, `run_nested_temperature.py`, `run_lockbox_winner.py`, plus the post-2026-04-30 per-item line (`run_per_item_v2.py` + companion caches) and the canonical lockbox scripts listed above.
- Pre-leakage scripts such as `run_compression_ablation.py`, `run_calibration_v2.py`, `run_clean_benchmark.py`, and older paper builders are for reproduction/audit unless a task explicitly targets those historical analyses.

## Leakage Rules
- Always use subject-level splits; never window-level or stride-level splits across the same subject.
- Fit anything target-derived or distribution-derived inside the fold only: rankers, anchors, prototypes, feature selectors, imputers, scalers, severity bins, calibration temperatures, and meta-learners.
- Never pre-compute XGBRanker ranks or leaf features on all subjects for a number reported as inductive; the prior global ranker was the main leak.
- Never tune a temperature or calibration parameter on the same test prediction vector you report; use nested CV.
- Healthy controls are diagnostic-only for current deployment work. HC anchors hurt strict inductive within-PD ranking and must not be used to justify deployable performance unless a new fold-clean experiment proves otherwise.
- Every new reportable experiment should pass the 5-null gate pattern: scrambled labels, SID-shuffle before cache join, test-only canary feature, retrieval-library test exclusion where applicable, and a transductive sanity variant.
- Follow lockbox discipline: screen many variants with 5-fold CV, pre-register exactly one winner in `results/preregistration_*.json`, then run LOOCV once and report it regardless of outcome.
- **Composite-level cherry-picking ban (2026-05-03 iter11A retraction lesson):** composer scripts that combine per-item OOFs into a T1/T3 sum must use ONE coherent pre-registered batch OR pre-register the composite formula (with `formula_sha256`, `created_at_utc`, `git_sha`) BEFORE any per-item LOOCV is run/observed. A pre-reg JSON whose mtime is later than the per-item OOF artifacts it cites is invalid (post-hoc selection masquerading as pre-registration).
- **Cache provenance requirement:** every reusable `cache_*.csv` / `.npz` feeding an inductive headline must have a `<name>.manifest.json` sidecar recording `script`, `git_sha`, `command`, `created_at_utc`, `data_sha256`, `labels_used`, `fold_scope`, `cohort_statistics_used`, `normalization_scope`, `leakage_status`, `leakage_rationale`. Caches without manifests are diagnostic-only.
- **Promotion gate to LOOCV (formalized 2026-05-02 / 2026-05-03):** at N=98 with V2 baseline (T3) or T1-sum at N=94, an IMU-feature-addition lockbox is permitted only when the 5-fold delta is ≥ +0.05 with seed std < 0.02 (per-item gate) OR T1-sum delta ≥ +0.025 with sum seed std < 0.020 across 5 seeds (sum-level gate, more honest at N=94 where per-item std is intrinsically ~0.06). If both gates fail, do not write a pre-registration; document as null.

## Modeling Gotchas
- Do not per-subject z-normalize regression inputs or use amplitude-scaling augmentation; amplitude carries severity signal.
- Compute XGB gain feature selection inside each fold. Global top-K selection is leakage.
- Use multi-seed ensembles for reported metrics; single seeds are noise at N<200.
- Report PD-only metrics alongside pooled metrics because HC subjects can inflate apparent performance.
- LightGBM CPU is preferred for these small tabular fits; prior notes found GPU mode slower at N<200.
- FM embeddings in `fm_embeddings.npz` do not carry SIDs; use `rocket_recordings.npz["sids"]` for subject mapping before aggregation.
- `obs_subscore`, H&Y, and per-item UPDRS values are ground truth clinical information; use only for targets, validation, ceilings, or explicitly labeled clinical-augmented analyses, not as deployable IMU features.

## Do Not Re-Run As Fresh Ideas
- Broad handcrafted feature fishing, generic scratch DL sweeps, MOMENT-only adapters, privileged distillation, HC-anchored rankers, post-hoc isotonic/Platt/polynomial calibration, per-item ordinal summing, NGBoost/SMOGN/pairwise contrastive fixes, and non-nested HP sweeps have already failed or leaked.
- **Frozen healthy-population-pretrained encoders are dead at this N for within-PD severity** (triangulated 2026-05-03 across MOMENT generic-TS, HC-SSL on 80 HC, and UKB OxWearables HARNet ~700K person-days; all NULL or NEGATIVE). The embedding subspace is approximately orthogonal to UPDRS-III severity, and high-dim embeddings displace useful V2 features in the per-fold K=500 LGB-importance selector. Do not attempt another frozen-encoder addition without a fundamentally different downstream architecture (end-to-end fine-tuning at acceptable variance, or a PD-specific SSL pretraining cohort that does not currently exist publicly).
- **End-to-end HARNet fine-tuning is dead at N=94 (F-iter37, 2026-05-08).** The strict subject-level pilot `run_t1_iter37_harnet_finetune.py` used raw wrist Acc XYZ windows (861 windows, 94/94 T1 subjects), fold-local target scaling, HARNet tail unfreezing, and a MIL head. Real 5-fold screen artifact `results/iter37_harnet_finetune_screen_20260508_110641.json`: OOF CCC `+0.1324`, MAE `2.1949`, fold CCCs `[+0.0516, +0.1481, +0.4740, -0.1199, -0.0052]`, feasibility gate FAIL vs CCC `0.60` floor. Do not retry longer epochs, full unfreezing, larger heads, or another HARNet fine-tune variant on this N=94 WearGait-only cohort; the failure mode is held-out fold collapse / small-N variance, not under-training.
- **T1 iter34 base-subset robustification is closed (F-iter46, 2026-05-08).** The base/item/P2 decomposition localized the P2 bootstrap fragility mostly to LGB/XGB: ET-only cleared P2 bootstrap (`+0.0442` high) while all-base did not. But the single pre-registered ET-only LOOCV got CCC `0.7269`, below iter34 `0.7366` and not strict vs iter12 (`frac>0=0.9388`). This is not a ceiling break or canonical update. Do not try LGB-only, XGB-only, LGB+ET, LGB+XGB, XGB+ET, or another base subset from the same diagnostic screen.
- **T1 iter34 auxiliary-label caveat is document-only (F-iter48 + aux-order audit, 2026-05-08/09).** `audit_t1_iter48_aux_validrange.py` found `NLS036` item15 total `18` in the historical auxiliary chain, caused by raw item-15 R/L values `9/9`; item15 valid total range is 0-8. Target items 9-14 are clean. `audit_t1_iter34_aux_order.py` then found the invalid item is not structurally irrelevant: iter34's random chain order places item15 before at least one T1 item in seeds `7` and `1337` (2/3 locked seeds). A bounded all-base 5-fold stale-vs-valid impact screen found common-SID CCC delta `-0.0008` and no materiality flag at `|delta| >= 0.025`, so this remains caveat evidence rather than a model update. Valid-range aux filtering would make iter34's chain cohort N=92, but do not run a post-hoc N=92 lockbox or another base-subset run unless the user explicitly overrides the conservative decision after this caveat. `run_t1_iter4.load_per_item_scores()` now masks invalid top-level item totals for future experiments.
- **Handcrafted scalar feature additions (≤10 cols) at N=94 are dead** (iter14 FoG-summary scalars NULL on items 9, 12; same K=500 absorption mechanism as F19 sensor-fusion). Forced-inclusion of the new block (always-include + K-rest-from-V2) is a meaningfully different architecture, but requires a fresh pre-registration with the new selection rule and is not chained from a failing standard-K=500 attempt.
- **T3 IMU feature additions to the iter5 clinical-augmented architecture are dead** (iter6 unsigned-asymmetry NULL Δ=−0.022; iter6 event-axial NULL Δ=−0.030 at 5-fold; both pre-registered and lockboxed). The +0.05 / seed-std<0.02 gate is real at N=98 and was missed by both consult priors (codex +0.015 to +0.04, gemini same range — both wrong).
- **Per-item gated T3 composite is dead at N=94 (F53 iter19 2026-05-04).** Sum of 18 per-item OOFs under a pre-registered single-batch architecture map (items 1-3 Phase A1 backfill, items 7-8 iter17 5-fold winners, items 16-17 iter17 5-fold winners, items 4-14 iter8 lockboxed, items 15+18 iter17 lockboxed) yielded composite 5-fold CCC = +0.299 ± 0.020 vs iter5 5-fold +0.405 ± 0.036 (both at N=94 T1-cohort subset) → Δ = −0.107 ≪ +0.025 floor. Mechanism: **variance compounding** (gemini's predicted Angle-1 failure mode #1). 5th data point on the N=94 sample-size wall, joining F19/F44/F45/F48/F51 — wall affects PROBE STRATEGY (composition vs direct regression), not just feature engineering.
- **Nested-CV hybrid (iter5 + 18 per-item) → Ridge meta is dead at N=98 (F56 iter21 2026-05-04).** 19-feature Ridge(α=1.0) meta-learner trained on inner-OOF predictions failed 5-fold gate by Δ = −0.1467 (hybrid +0.3389 ± 0.0429 vs iter5 +0.4856 ± 0.0300; bootstrap CI [−0.2542, −0.0197], frac>0=0.013). The script `run_t3_iter21_nested.py` correctly fixes all 4 F54 bugs (nested CV, T3-native N=98 loader, pre-reg `--write-prereg`/`--run` split with `formula_sha256` validation, hybrid endpoint = updrs3 directly). The methodologically clean version PERFORMS WORSE than the F53 raw-sum because nested CV exposes inner-CV variance starvation that iter20's single-loop leakage hid. Mechanism: Ridge α=1.0 + 19 collinear predictors + N≈78 outer-train = curse-of-dimensionality blow-up; meta inflates noisy item-11 weight to +5× and suppresses iter5 to +0.4×. **Don't retry 19-feature meta-stack on this composite at this N**; would need ≪10 base predictors OR α≥10–100 Ridge OR 1-2-parameter convex mix (e.g., one OLS α* on (composite_residual, target_residual)).
- **T3 low-degree nested convex mix is dead at N=95 (iter50, 2026-05-08).** `run_t3_iter50_lowdf_convex.py` tested the F56 escape hatch on the corrected valid-range cohort: A3 clinical-only Ridge + direct IMU-only/no-`cv_*` LGB, with one alpha selected inside each outer train fold by inner CV. Screen artifact `results/iter50_lowdf_convex_screen_20260508_225105.json`: baseline CCC `0.3759`, nested-convex CCC `0.3083`, delta `-0.0676`, seed-delta std `0.0319`, bootstrap frac>0 `0.0348`; alpha was unstable/extreme (`std=0.411`, min `0`, max `1`). Gate FAIL, no LOOCV, no canonical T3 change. Do not retry low-degree clinical/IMU convex mixers at this N without new predictors or a new target representation.
- **iter26 Hssayeni MJFF acquisition is BLOCKED at Synapse DUA gate (F62, 2026-05-05 PM).** Verified `syn20681023` (MJFF Levodopa Response Study, Hssayeni 2021 source) exists but anonymous children listing returns 404 — DUA-gated. Most other richer PD+UPDRS Synapse projects are similarly gated. Scaffolding complete: `run_t3_iter26_hssayeni.py` orchestrator (probe/download/extract/write_prereg/run modes), `cache_hssayeni_features.py` extractor, `scripts/synapse_hssayeni_setup.md` 10-step DUA runbook. **User action required**: Synapse DUA application on `syn20681023` (1-3 day approval) + Personal Access Token in `~/.synapseConfig`. The scaffolding probe surfaces the gate cleanly with actionable next steps. **Don't try to bypass the DUA gate**; it's a regulatory/privacy access requirement, not a technical one.
- **FoG-STAR external Stage-1 augmentation is dead as an internal T3 ceiling breaker (F-fogstar / iter38, 2026-05-08).** FoG-STAR is a newly surfaced public CC-BY 4.0 direct T3 external route (`updrs_iii`, 22 PD subjects, IMUs on ankles/back/wrist; Zenodo `17838806`). The conservative screen `run_t3_iter38_fogstar_stage1.py` augmented only iter5 Stage-1 Ridge with FoG-STAR clinical rows and kept Stage-2 as WearGait train-fold V2 residual. Remote 5-fold screen artifact `results/iter38_fogstar_stage1_screen_20260508_142623.json`: seed-mean baseline CCC `0.4888`, augmented CCC `0.4896`, delta `+0.0008`; mean seed delta `+0.0066`, seed std `0.0297`, bootstrap frac>0 `0.4938`; gate FAIL. No lockbox and no canonical T3 change. Do not retry external clinical Stage-1 augmentation variants at this N without a new, pre-registered rationale; FoG-STAR's remaining value is zero-shot external-validity evidence, not a demonstrated CCC booster.
- **FoG-STAR zero-shot external validation is partial, not a new internal T3 route (iter39, 2026-05-08).** Pre-reg `results/preregistration_t3_iter39_fogstar_zeroshot_20260508_143717.json` (formula_sha256 `e82d3c10c6199813f32d70144f959c7b8d61cb3d9d938311551ac0d0c11917d1`) trained on WearGait only and scored all 22 FoG-STAR subjects. Track A wrist-direct CCC `-0.0180` (CI `[-0.0912,+0.0465]`), Track B iter5-style clinical+wrist CCC `+0.2499` (CI `[+0.0281,+0.5028]`, MAE `12.89`), Track C FoG-STAR-only LOOCV sanity CCC `+0.0821` (CI `[-0.3058,+0.5096]`). OpenRouter Grok 4.3 and DeepSeek V4 Pro both advised recording this as partial external-validity evidence only. Do not use FoG-STAR to justify another internal T3 ceiling-break experiment; use it as a paper transportability row.
- **COPS external validation is partial, not a new internal T3 route (iter49, 2026-05-08).** COPS (`Scientific Data` 2026 / OSF `5xvwn`) is public and unblocked: 66 OSF ZIP records / 64 unique subject ZIP filenames, bilateral wrist GENEActiv accelerometry at 100 Hz, demographics, symptom diaries, and UPDRS-III OFF/ON CSVs. Pre-reg `results/preregistration_t3_iter49_cops.json` (formula_sha256 `0bc80ef0b6bd9c40da6a7a1282ce9f8898273c6e2dc01e7987ea2ecaa4715b15`) froze Track A right-wrist magnitude-only, Track B iter47/iter5-style clinical+wrist, Track C COPS-only LOOCV sanity, Track D bilateral sensitivity. Full artifacts: `results/iter49_cops_download_manifest.json`, `results/iter49_cops_features_full.csv`, `results/iter49_cops_zeroshot_20260508_185226.json`, stable `results/iter49_cops_zeroshot.json`. Scale check passed after converting COPS g to m/s². Primary OFF N=62: Track A right wrist CCC `-0.0193` (CI `[-0.1030,+0.0704]`), Track B right clinical+wrist CCC `+0.2412` (CI `[+0.1061,+0.3916]`), Track D bilateral clinical+wrist CCC `+0.2535`, Track C COPS-only LOOCV sanity CCC `+0.3100`. Kimi advised closing COPS as documented transportability. Do not use COPS to justify another internal T3 ceiling-break experiment.
- **TLVMC/DeFOG iter51 is partial external-validity evidence, not a new internal T3 route (2026-05-09).** Zenodo `10959560` / Kaggle `tlvmc-parkinsons-freezing-gait-prediction` is public CC-BY 4.0. Probe `scripts/probe_tlvmc_fog_route.py` -> `results/tlvmc_fog_route_probe_20260509.{json,md}` confirmed 137 medication-matched DeFOG recordings across 45 subjects / 70 subject-visits, split into 68 OFF records from 44 subjects and 69 ON records from 45 subjects; `tdcsfog` has 0 joined UPDRS-III targets in the public metadata probe. Pre-model preregistration: `scripts/write_tlvmc_defog_prereg.py` -> `results/preregistration_t3_iter51_tlvmc_defog_zeroshot.json`, formula_sha256 `665479765911e8192e4db570babeb5fcef3e2b6553dfd6259187f76685ec08cd`. Full run artifacts: `run_t3_iter51_tlvmc_defog.py`, `results/iter51_tlvmc_defog_features.csv`, `results/iter51_tlvmc_defog_features.csv.manifest.json`, stable `results/iter51_tlvmc_defog_zeroshot.json`, timestamped `results/iter51_tlvmc_defog_zeroshot_20260509_013357.json`, and row predictions `results/iter51_tlvmc_defog_zeroshot_rows_20260509_013357.csv`. Primary OFF Track A lower-back magnitude zero-shot CCC `+0.2695` (95% CI `[+0.1693,+0.3600]`, MAE `8.069`, r `0.5635`); Track B wrist-to-lumbar stress CCC `+0.0485`; Track C DeFOG-only LOSO sanity CCC `+0.3450`. ON Track A CCC is only `+0.0548`. One raw file (`02ab235146`) was skipped because it lacked `Valid`/`Task`. Do not use TLVMC/DeFOG as an internal WearGait ceiling-break screen, and do not update internal T3 canonicals from any external result.
- **PDFE turning-in-place iter52 is external-validity evidence only, not a new internal T3 route (2026-05-09).** Figshare `14984667` / Frontiers 2022 is public CC-BY 4.0 and exposes shank IMU plus session-level UPDRS-III totals; session 1 has 35 PD subjects. Pre-reg `results/preregistration_t3_iter52_pdfe_turning_zeroshot.json` (formula_sha256 `f0eb5985a15b271a333b3d9e1d093e32889814a0f48d0ca4f5131b9674c7b2f2`) froze Track A WearGait lateral-shank magnitude -> PDFE shank, Track B clinical+shank, and Track C PDFE-only LOOCV sanity. Full artifacts: `run_t3_iter52_pdfe_turning.py`, `results/iter52_pdfe_turning_probe.json`, `results/iter52_pdfe_turning_download_manifest.json`, `results/iter52_pdfe_turning_features.csv`, `results/iter52_pdfe_turning_features.csv.manifest.json`, stable `results/iter52_pdfe_turning_zeroshot.json`, timestamped `results/iter52_pdfe_turning_zeroshot_20260509_092223.json`, and row predictions. Results: Track A WearGait shank-to-PDFE CCC `-0.1008` (CI `[-0.2877,+0.0554]`), Track B clinical+shank CCC `+0.1340` (CI `[-0.0426,+0.3369]`), Track C PDFE-only LOOCV sanity CCC `+0.4020` (CI `[+0.1569,+0.6519]`). This confirms within-PDFE protocol signal but failed WearGait transport; no internal T3 canonical update and no follow-up PDFE augmentation without a new pre-registered rationale. Figshare `14896881` is public ON/OFF UPDRS-III gait biomechanics, but it is motion capture/force plates rather than wearable IMU and is document-only for this objective.
- **Parkinson@Home iter53 is a public direct T3 route, but hard-stopped before scoring (2026-05-09).** Radboud DOI `10.34973/fr4z-a489` exposes 25 PD + 25 controls, bilateral wrist acc/gyro at 200 Hz, OFF/ON recordings, prepared parquet files, and public MDS-UPDRS Part III subitems in `patient_info.csv`. Pre-reg `results/preregistration_t3_iter53_parkinsonathome_zeroshot.json` (formula_sha256 `417fdfe0bd2f07c8c5415bd49c87b70725979a26517fe353ca376e2b85387888`) froze WearGait-to-Parkinson@Home wrist zero-shot, Parkinson@Home-only LOOCV sanity, and OFF/ON response sensitivity, with a hard stop requiring at least 20 valid OFF PD subjects after feature-readability filtering. Probe/extraction artifacts: `results/iter53_parkinsonathome_probe.json`, `results/iter53_parkinsonathome_features.csv`, `results/iter53_parkinsonathome_features.csv.manifest.json`, and `results/parkinsonathome_route_refresh_20260509.{json,md}`. Extraction retained only 18 valid OFF PD subjects, so no Track A/C/D metric exists and no internal T1/T3 canonical can change. Do not rerun iter53 under the same preregistration; any shorter-window or fallback policy requires a fresh preregistration and remains external-validity-only.
- **External-result claim labeling audit is now required for external T3 rows (2026-05-09).** `audit_external_result_claim_labeling.py` -> `results/external_result_claim_labeling_audit_20260509.{json,md}` passes with findings `0`, missing required snippets `0`, and artifact failures `0`. It scans FoG-STAR, COPS, TLVMC/DeFOG, PDFE, and PADS names/CCCs across active paper/handoff surfaces plus external zero-shot JSON policy fields. External rows may support transportability or within-dataset sanity claims only; they must not be framed as internal WearGait-PD T3 headline, canonical, deployment, or ceiling-break updates.
- **Papadopoulos phone-call tremor is not a T1/T3 route (2026-05-09).** Zenodo `7273759` is public CC-BY 4.0 and has smartphone embedded-IMU acceleration captured during phone calls, with 45 clinically examined subjects plus 454 self-reported subjects. The clinical annotations are tremor-specific only (UPDRS II item 16, Part III item 20/21 left/right hand tremor, binary tremor annotation, PD status). There is no total MDS-UPDRS Part III target, no T1 items 9-14, and no WearGait-aligned gait/balance protocol. Kimi and Gemini both advised route-audit/paper hardening only. Do not preregister, download, scaffold, or run this dataset for the active T1/T3 CCC objective; cite it only as tremor-subitem/free-living context if needed.
- **Harmonized Upper/Lower Limb Accelerometry is not a T1/T3 route (2026-05-09).** The 2025 Data in Brief / NICHD DASH resource contains 790 participants, 2,885 recording days, and about 7% PD, with daily-life ActiGraph upper/lower-limb accelerometry and processed summary variables. It is valuable rehab/free-living context, but no total MDS-UPDRS Part III or T1 items 9-14 are confirmed, access is controlled through DASH Part1/Part2, and the public code computes activity summaries rather than WearGait-aligned structured gait/balance raw-IMU features. Kimi and Gemini both advised no preregistration/download/scaffold. Do not run this route for the active T1/T3 CCC objective.
- **Monipar/BIOCLITE are public smartwatch subitem datasets, not T1/T3 routes (2026-05-09).** Monipar (Zenodo `8104853`) has 21 PD / 7 HC but only 6 supervised PD subjects / 46 trials with exercise-level MDS-UPDRS item labels (3.17, 3.15, 3.4, 3.5, 3.6, 3.10). BIOCLITE (Zenodo `16408199`) has 24 PD / 16 HC with smartwatch acc+gyro and per-exercise MDS-UPDRS scores for items including 3.17, 3.15, 3.4, 3.5, 3.6, 3.9, 3.10. Neither provides total Part III or full T1 items 9-14; Kimi advised `NO-PREREG / DOCUMENT-ONLY`. Artifacts: `results/smartwatch_subitem_route_refresh_20260509.{json,md}` and `results/external_route_audit_monipar_bioclite_20260509.md`. Cite only as related work for consumer-smartwatch exercise/subitem monitoring.
- **Zenodo `14848598` Comprehensive Multi-Modal Dataset is a derived benchmark table, not a WearGait-PD route (2026-05-09).** Fresh web/CSV inspection found `Updated_Clinical_Gait_Dataset.csv` has UPDRS part totals plus scalar gait summaries (`gait_time`, `gait_steps`, `freezing`), while `Final_Integrated_MultiModal_Dataset.csv` is dominated by CSF protein/peptide features keyed by `visit_id`. The Zenodo record states it merges AMP Kaggle and a Mendeley gait repository; there is no raw wearable IMU, no T1 item-level labels, and no auditable contemporaneous wearable-to-UPDRS subject alignment. Kimi advised `NO-PREREG / DOCUMENT-ONLY`. Artifact: `results/derivative_multimodal_route_refresh_20260509.{json,md}`. Cite only as an example of a public derived benchmark that is not strict sensor-clinical validation.
- **Request-only actigraphy/smartwatch false positives are not immediate T1/T3 routes (2026-05-09).** Fay-Karmon 2024 / Scientific Reports has 21 advanced-PD participants with Intel Pharma Analytics smartwatch+iPhone monitoring, MDS-UPDRS II/III ON/OFF and Part IV, but data are corresponding-author request-only, SWA scoring is proprietary/schema-hidden, and no T1 item-level route is visible. The Sensors 2023 marital-dyad actigraphy study has 27 PD/spouse dyads (54 individuals), non-dominant wrist GeneActiv at 100 Hz for 7 days, and MDS-UPDRS III for the PD participant, but source data are author-request-only and the endpoint is daily-life/social actigraphy rather than structured WearGait-like gait/balance. Kimi advised `NO-PREREG / DOCUMENT-ONLY / ACCESS-REQUEST-ONLY`; Claude remains low-credit and `glmcode` unavailable. Artifact: `results/request_only_actigraphy_route_refresh_20260509.{json,md}`. Do not scaffold, download, preregister, or launch remote jobs for either route unless data-owner approval and row-level schema are obtained.
- **Luxembourg / NCER-PD upper-limb IMU is document-only observability context (2026-05-09).** Sensors 2024 reports 33 PD + 12 controls, six elicited hand/arm MDS-UPDRS III tasks, and bilateral compact hand IMUs, but the data are request-only, ON-medication, subitem-only, and do not expose a total T3 or full T1 endpoint. Kimi advised `skip_runbook_document_only`; Claude remains low-credit and `glmcode` unavailable. Artifact: `results/luxembourg_upper_limb_route_refresh_20260509.{json,md}`. Use only as related work for the T3 upper-limb observability ceiling; do not create an access runbook, scaffold, preregistration, download, or remote job for the active T1/T3 CCC objective.
- **Pre-QuantiPark / ActiMyo levodopa-challenge IMU is document-only tiny-N context (2026-05-09).** Scientific Reports 2025 reports 10 PD DBS-candidate participants in a 90-minute L-dopa challenge with repeated MDS-UPDRS Part III ratings and ActiMyo wrist+ankle IMUs at 130.69 Hz. Data are request-gated for academic non-commercial proposal review and data access agreement. Kimi advised `document_only_no_runbook_no_prereg_no_scaffold`; Claude remains low-credit and `glmcode` unavailable. Artifact: `results/prequantipark_route_refresh_20260509.{json,md}`. Treat as pharmacological motor-fluctuation related work only; N=10 makes a subject-level lockbox incoherent, no T1 item path is visible, and no access runbook/scaffold/prereg/download/remote job is justified for the active T1/T3 CCC objective.
- **TUM Donié 2025 ROCKET/InceptionTime is an Hssayeni/MJFF alias, not a new algorithm route (2026-05-09).** Scientific Reports 2025 uses a 27-patient subset of MJFF `syn20681023` with GENEActiv wrist acceleration at 50 Hz and task-level tremor/bradykinesia/dyskinesia labels. The paper/code are public, but data remain Synapse DUA-gated and the endpoint is symptom classification, not T1 items 9-14 or total T3 regression. Local ROCKET/MultiROCKET and learned time-series fine-tuning branches have already failed. Artifact: `results/tum_rocket_inception_route_refresh_20260509.{json,md}`. Do not clone/scaffold/preregister/download/run this as a fresh idea.
- **ParaDigMa and Yin et al 2025 are document-only related-work leads, not lockbox routes (2026-05-09).** ParaDigMa is open software for wrist accelerometer/gyroscope/PPG PD biomarker extraction, not a labeled T1/T3 cohort; running it on WearGait would be another local scalar handcrafted feature addition, a dead category at N=94. Yin et al Frontiers Neurology 2025 reports request-only N=20 PD gait-parameter regression against MDS-UPDRS III in OFF/ON states, but there is no public row-level schema and the modality is likely gait-parameter/motion-capture or instrumented-walkway derived rather than raw WearGait-aligned IMU. Artifact: `results/paradigma_yin_route_refresh_20260509.{json,md}`. Do not create scaffolds, preregistrations, downloads, access runbooks, or remote jobs for either route under the active T1/T3 CCC objective.
- **PPMI / Verily Study Watch is a priority access-gated route, not an immediate scaffold (2026-05-08).** Fresh web search found PPMI qualified-researcher access to individual-level clinical and sensor data after DUA/application; FAQ lists MDS-UPDRS Part III + H&Y; the 2025 npj Verily paper used 100 Hz wrist Study Watch accelerometer data and MDS-UPDRS assessments within 90 days. Kimi recommended adding PPMI to the external-route audit and waiting for credentials before any probe/scaffold. Access runbook: `scripts/ppmi_verily_setup.md`. Claude still fails for low credit; `glmcode` is unavailable. If the user applies for only one gated route, prioritize PPMI over Hssayeni because it is wrist-native, larger, longitudinal, and has a published Verily/MDS-UPDRS trail. Do not launch remote work until PPMI credentials exist.
- **Personalized Parkinson Project / PD-VME is a strong gated Verily-watch peer route, not an immediate scaffold (2026-05-09).** PPP data sharing lists 517 PD participants with clinical assessments and Verily Study Watch monitoring; the PD Virtual Motor Exam paper reports 388 early PD participants and smartwatch active tasks validated against MDS-UPDRS Part III / consensus subitem ratings. Access is governed by the PPP Research and Data Sharing Review Committee and may involve fees; there is no local approval or row-level schema. Add to gated access queue next to PPMI, but do not scaffold, preregister, download, or run until approval and schema exist.
- **ICICLE-PD / ICICLE-GAIT is request-gated/document-only, not an immediate scaffold (2026-05-08).** Fresh web search found a 2026 Frontiers ICICLE route with 89 PD participants, lower-back Axivity AX3 accelerometry at 100 Hz (+/-8g), 7-day free-living gait at 18-month visits over 6 years, and MDS-UPDRS Part III + H&Y labels. Published global benchmarks are modest (best global FL MAE 9.26, r 0.43, ICC 0.438), and the data are available by request rather than public download. Kimi and Gemini recommended documenting it as request-gated and not building code before the schema is visible. Access runbook: `scripts/icicle_request_setup.md`. PPMI remains higher priority if only one access route is pursued because PPMI is wrist-native. Do not launch remote work until ICICLE data access exists.
- **Mobilise-D TVS / CVS is not an immediate scaffold (2026-05-09).** Fresh web search found public Mobilise-D TVS Zenodo `15861907` and CVS cohort summaries. TVS is public but explicitly algorithm-validation oriented, not a clinical UPDRS-III regression target; CVS reports 600-602 PD participants, MDS-UPDRS clinical assessments, up to 5 visits, and 7-day lower-back wearable monitoring, but no row-level public wearable + MDS-UPDRS schema/release was found. Kimi and Gemini recommended TVS skip, CVS watch-list/request only, and no scaffold/download/remote job until access/schema exist. PPMI remains first priority because it is wrist-native.
- **CNS Portugal / Lobo IS2022 AX3 gait is request-gated/document-only, not an immediate scaffold (2026-05-08).** Fresh web search found a direct structured-gait T3 route with 74 PD subjects, Axivity AX3 on wrist and lower back at 100 Hz, 267 gait instances from 104 10-meter-walk sessions, and MDS-UPDRS Part III + H&Y labels. Published 10% heldout-window MAE 4.26 is optimistic because the paper describes validation windows from patients already seen by the selected models; strict future use must be subject/session-grouped. Kimi and Gemini recommended documenting it as request-gated and not building code before schema/data access. Access runbook: `scripts/cns_portugal_request_setup.md`. PPMI remains first priority if only one gated route is pursued; CNS is a strong structured-gait second/peer request. Do not launch remote work until CNS data access exists.
- **WATCH-PD (C-Path / MJFF) is request-gated/document-only, not an immediate scaffold (2026-05-09).** 82 early untreated PD + 50 controls, 12-month longitudinal, Apple Watch/iPhone BrainBaseline + APDM Opal sensors worn during MDS-UPDRS Part III (mean PD Part III 24.1). Data available to C-Path 3DT Stage 2 members; non-members may propose to the WATCH-PD Steering Committee for de-identified datasets. Code is proprietary. C-Path Integrated Parkinson's Database explicitly excludes digital health technology data. Direct contemporaneous MDS-UPDRS III + wrist sensor acquisition is an ideal T3 transportability protocol match, but proprietary code blocks feature-extraction scaffolding even if data access is granted. Early untreated PD (no levodopa, mean 24.1) may yield different gait signatures and a lower transportability ceiling vs WearGait-PD's treated/mixed cohort. Ranked below PPMI/Verily because N is smaller and the access gate is consortium-level with uncertain timeline; ranked above CNS Portugal because the contemporaneous Part III protocol is stronger for T3. Access-only checklist: `scripts/watchpd_request_setup.md`. Do not build scaffold/download code until C-Path membership or Steering Committee approval and raw sensor schema are secured.
- **Runnable prompt-to-artifact objective audit says NOT COMPLETE (2026-05-08).** `audit_prompt_objective_evidence.py` writes `results/prompt_objective_evidence_audit_20260508.{json,md}` and maps 12 explicit user-objective requirements to concrete evidence, including the latest artifact/reproducibility/claim-label guards. Latest result: `goal_complete=false`; the single hard gap is the explicit clean ceiling-break completion condition. T1 has an attempted caveated candidate (`0.7366`, N=93); T3 has no corrected breakthrough (`0.3784`). Do not call the thread goal complete while this audit and `verify_current_goal_state.py` report false.
- **Cache consumer guard audit (2026-05-08):** `audit_cache_consumer_guards.py` writes `results/cache_consumer_guard_audit_20260508.{json,md}`. Latest counts: 4 current safe-cache consumers guarded, 53 model/composer scripts diagnostic-only because they reference missing/partial-manifest caches, 32 non-model/cache-producer references. Do not promote any diagnostic-only consumer result to a headline without first regenerating/backfilling the referenced cache manifest from real evidence and adding `require_cache_manifest`.
- **Transitive/runtime cache dependency audits (2026-05-08):** `audit_transitive_cache_dependencies.py` writes `results/transitive_cache_dependency_audit_20260508.{json,md}` and walks local import closures for 12 current headline/reportable entrypoints. After narrowing `compose_t1_iter12_honest.py`, latest counts are 5 direct diagnostic-cache entrypoints (`compose_t1_iter12_honest.py`, `run_t3_iter41_target_fix.py`, `run_t3_iter5_clinical.py`, `run_t3_iter16_site_ipw.py`, `run_t3_iter49_cops.py`) and 7 conservative transitive-reachability entrypoints. `audit_runtime_cache_dependencies.py` writes `results/runtime_cache_dependency_audit_20260508.{json,md}` and confirms live lightweight paths execute `ablation_v3_features.csv`; the narrowed iter12 composer no longer executes peritem/MOMENT/HC-SSL/walkway caches, and static `velinc_features.csv` reachability is not executed in the iter47 smoke path. Runtime tracing is diagnostic only: do not describe future headlines as cache-manifest-clean until live/reachable diagnostic caches are regenerated/backfilled or broad historical helpers are narrowed.
- **T3 `dst_*` walkway-distillation caveat (2026-05-08):** `audit_dst_walkway_leakage.py` found that all 31 `dst_*` columns in `ablation_v3_features.csv` are included by the current V2 filters. These columns come from `run_ablation_v2.distill_walkway()`, a once-trained historical dev-split XGBoost distiller for pressure-walkway metrics, so they are not fold-local for LOOCV. Three-seed iter47 sensitivity shows the issue is not load-bearing: current Stage 2 CCC `0.3784`, no-`dst_*` CCC `0.3766`, bootstrap delta no-`dst` minus current `-0.0004` with 95% CI `[-0.0479,+0.0523]`, frac>0 `0.480`. Treat this as a disclosure/provenance boundary, not a new tuned model. Pair any corrected T3 current-V2 report with the no-`dst_*` sensitivity until `ablation_v3_features.csv` is regenerated or the distiller is made fold-local.
- **Ablation V3 cache provenance audit (2026-05-08):** `audit_ablation_v3_cache_provenance.py` writes `results/ablation_v3_cache_provenance_audit_20260508.{json,md}`. It verifies `ablation_v3_features.csv` is hash-stable (`b405d90a6a35808d556d726b58bf7d9361d26e020a79091e52c868ee98f9c2b4`), git-tracked, and the live cache opened by lightweight iter12/iter34/iter47 paths, but records `decision=do_not_synthesize_clean_manifest` because exact command/runtime/git/raw-data/fold-scope evidence is incomplete. Do not create a clean manifest from this audit alone. Future cache-manifest-clean headlines require exact regeneration/backfill or narrower reproduction artifacts that avoid the broad V2 cache.
- **Ablation V3 regeneration probe (2026-05-09):** `audit_ablation_v3_regeneration.py` writes `results/ablation_v3_regeneration_probe_20260509.{json,md}`. It is non-destructive and monkeypatches `run_ablation_v3.FEATURE_CACHE` only when full inputs exist. Current remote status is `blocked_missing_regeneration_inputs`: the GPU slave has PD raw CSVs and PD clinical data but is missing controls clinical, control CSVs, and walkway metrics required for full 178-subject V2 cache regeneration. Exact Synapse recovery IDs are now recorded by `scripts/download_weargait_missing_synapse.py`: control clinical `syn55105521`, control CSV folder `syn61370552` (680 CSVs), and walkway metrics `syn64589881`. The recovery preflight artifact `results/weargait_missing_synapse_recovery_preflight_20260509.{json,md}` is dry-run/credential-safe; current status is `missing_inputs` with no token/config present, and full control CSV recovery requires `--confirm-large-control-csvs`. The frozen cache SHA stayed `b405d90a6a35808d556d726b58bf7d9361d26e020a79091e52c868ee98f9c2b4`; no regenerated CSV was written. Do not synthesize a clean manifest until full raw inputs are restored and a non-destructive regeneration succeeds; even then `dst_*` and `cv_*` keep the artifact caveated unless dropped or made fold-local.
- **WearGait raw-data recovery runbook (2026-05-09):** `scripts/weargait_raw_data_recovery_runbook.md` is now the human-facing guide for restoring the three missing Synapse inputs needed by the V2 cache regeneration probe. `audit_weargait_raw_data_recovery_runbook.py` writes `results/weargait_raw_data_recovery_runbook_audit_20260509.{json,md}` and currently passes with decision `raw_data_recovery_runbook_ready_no_download`. It preserves parent project `syn52540892`, recovery IDs `syn55105521` / `syn61370552` / `syn64589881`, the 680-control-CSV large-transfer confirmation, and the no-clean-manifest/no-model-run boundary. Current status remains credentials absent and `missing_inputs`; no download, cache promotion, or model run has occurred.
- **T3 iter47 residual anatomy (2026-05-09):** `audit_t3_iter47_residual_anatomy.py` writes `results/t3_iter47_residual_anatomy_20260509.{json,md}` from saved valid-range-corrected OOF predictions. It reproduces CCC `0.3784` / MAE `7.5280`, but shows the corrected-target failure mode sharply: residual corr(true severity) `-0.7771`, Q1 low-severity mean residual `+10.02`, Q4 high-severity mean residual `-9.20`, WPD within-site CCC `0.0515`, and top global post-hoc residual-feature |r| only `0.290`. This is diagnostic-only and not fold-local feature selection; do not use it as a lockbox gate or launch another scalar WearGait-only feature-fishing pass from it.
- **T3 iter47 CCC-rescale sanity (2026-05-09):** `audit_t3_iter47_ccc_rescale_sanity.py` writes `results/t3_iter47_ccc_rescale_sanity_20260509.{json,md}` from the saved iter47 OOF vector. OOF-level leave-one affine recalibration lowers CCC to `0.2572`; OOF-level variance matching raises CCC to `0.3996`, but the paired-bootstrap CCC delta is small/uncertain (`+0.0208`, CI `[-0.0104,+0.0578]`) and MAE worsens by `+1.1398` (CI `[+0.4659,+1.8440]`). The transform is not fully nested because the second-level calibration set includes OOF predictions from base models that trained on the held-out subject in other folds. Treat this as a CCC-accounting trap, not a reportable model or lockbox route.
- **Current headline influence audit (2026-05-09):** `audit_current_headline_influence.py` writes `results/current_headline_influence_audit_20260509.{json,md}` from saved T1/T3 OOF vectors. No single-subject redline was found: T3 iter47 max absolute leave-one CCC delta is `0.0381` (<0.05), top-5 influence share is `0.284`, and T1 iter34-minus-iter12 matched delta stays positive under all leave-one deletions (minimum `+0.0629`). Iter34 remains a strongest candidate, not a canonical floor replacement, with N=93, P2, and auxiliary-label caveats. Influence remains severity-tail concentrated (T3 abs(target-median) vs abs(delta CCC) r `0.6779`, Gini `0.6009`), so document this as claim-fragility evidence only. Do not filter, retune, or rerun from this audit.
- **T3 iter47 domain residual audit (2026-05-09):** `audit_t3_iter47_domain_residuals.py` writes `results/t3_iter47_domain_residual_audit_20260509.{json,md}` from saved iter47 OOF predictions plus valid-range true Part III item/domain labels. Parsed item totals reproduce the iter47 target exactly (max diff `0.0`). Residuals are dominated by true non-gait burden: `unobservable_non_gait` residual r `-0.8004`, upper-limb brady r `-0.6224`, appendicular brady r `-0.6156`. Privileged oracles are large (`unobservable_non_gait` dCCC `+0.4716`, multidomain Ridge oracle CCC `0.8533`) but require true clinical labels at test time. Treat this as target-anatomy evidence only, not a deployable model, feature-selection gate, or subject-filtering rule.
- **T3 iter47 item-level residual audit is stop-rule evidence, not a roadmap (2026-05-09).** `audit_t3_iter47_item_residuals.py` writes `results/t3_iter47_item_residual_audit_20260509.{json,md}` from saved iter47 OOF predictions plus true Part III item scores. Top residual-correlated items are non-WearGait-observable: item 6 pronation/supination `r=-0.571` (oracle dCCC `+0.282`), item 4 finger tapping `r=-0.528` (dCCC `+0.256`), item 5 hand movements `r=-0.469` (dCCC `+0.226`), item 3 rigidity `r=-0.460` (dCCC `+0.195`). Best gait/balance-observable items are much weaker: item 8 leg agility dCCC `+0.148`, item 7 toe tapping `+0.125`, item 10 gait `+0.091`. Mean `|r(item,residual)|` observable items 7-14 = `0.247` vs non-observable = `0.371`. This is an anatomical ceiling: WearGait's gait/balance protocol does not capture upper-limb bradykinesia, rigidity, or tremor, and the residual structure is explained by target composition, not missing model capacity. Combined with the dead-list (scalar additions, per-item composite, nested stacks, low-degree convex mix, frozen/fine-tuned encoders, local residual smoothing, sample weights, calibration all FAILED), there is **no valid next WearGait-only model route to break T3 CCC**. Document this as stop-rule evidence; do not launch another WearGait-only T3 scalar-feature or calibration screen.
- **Remaining blocker action audit (2026-05-09):** `audit_remaining_blocker_actions.py` writes `results/remaining_blocker_action_audit_20260509.{json,md}` and classifies all current verifier blockers into explicit next-action boundaries. Latest run passes with `35` blockers classified, unclassified blockers `0`, ambiguous blockers `0`, and local WearGait-only model actions remaining `0`. Treat this as a no-repeat guard: valid next actions are gated external data access, raw-data restoration for cache provenance, or paper/provenance hardening; do not start a new WearGait-only model/lockbox run unless a future blocker is explicitly classified as `local_model_action_allowed`.
- **External access readiness audit (2026-05-09):** `audit_external_access_readiness.py` writes `results/external_access_readiness_audit_20260509.{json,md}` and orders the gated-route queue. Latest run passes with six access/request packets ready and zero compute-ready routes before access. Priority order: PPMI/Verily, PPP/PD-VME, WATCH-PD, CNS Portugal/Lobo, Hssayeni/MJFF, ICICLE-GAIT; Mobilise-D CVS, Fay-Karmon, and marital-dyad actigraphy remain watch/request-only. New PPP runbook: `scripts/ppp_pd_vme_request_setup.md`. The first allowed code action after any approval is a read-only schema probe, not a loader, preregistration, cache, or remote model job.
- **Paper generator routing audit (2026-05-09):** `audit_paper_generator_routing.py` writes `results/paper_generator_routing_audit_20260509.{json,md}` and must pass before treating paper/export commands as synchronized. Current paper work routes through `render_current_paper.py` -> `CURRENT_PAPER.html`; `generate_paper_v4.py` and `NEW4.html` are legacy/stale archaeology only. Latest run passes with decision `current_paper_renderer_route_guard_passed`, hard failures `0`, eight active docs checked, and retained `NEW4.html` stale/transductive evidence quarantined.
- **README claim-routing audit (2026-05-09):** `audit_readme_claim_routing.py` writes `results/readme_claim_routing_audit_20260509.{json,md}` and must pass before treating the root README as a safe onboarding/publication surface. The README must open with current post-audit T1/T3 claims (`0.6550`, candidate `0.7366`, T3 `0.3784`, LOSO `0.150`) and may mention old SSL/XGBRanker `0.868` / `0.776` only when locally labeled legacy/retracted/pre-audit/target-contaminated. Latest run passes with decision `readme_current_claim_route_guard_passed`, hard failures `0`, and unguarded stale hits `0`.
- **Legacy manuscript-surface quarantine audit (2026-05-09):** `audit_legacy_manuscript_surfaces.py` writes `results/legacy_manuscript_surface_audit_20260509.{json,md}` and must pass before treating retained pre-audit paper/narrative files as safely quarantined. `paper.tex`, `paper_new2.tex`, `CALIB-EXPERIMENTS.md`, `HOW.md`, `REPRODUCIBILITY.md`, `review_report*.md`, legacy `generate_paper*.py`, and `NEW4.html` / `NEW5.html` / `NEW6.html` must carry near-top stale/do-not-cite banners pointing to `CLAUDE.md`, `paper.md`, `render_current_paper.py`, and `CURRENT_PAPER.html`. Latest run passes with decision `legacy_manuscript_surfaces_quarantined`, hard failures `0`, and 16 legacy surfaces checked.
- **Historical archive-surface quarantine audit (2026-05-09):** `audit_historical_archive_surfaces.py` writes `results/historical_archive_surface_audit_20260509.{json,md}` and must pass before treating old project-note surfaces as safely retained. `CONT.md`, `EXP*.md`, `LEARNINGS.md`, `VNEXT.md`, `NEXTNEXT.md`, `literature_review.md`, `paper_supplement_iter33_gate_demo.md`, `CODEX-PROPOSALS.md`, `PROPOSALS.md`, and `leakage_onepager.html` carry archive-status banners and current-route pointers. `leakage_onepager.html` now uses the iter47 valid-range T3 headline (`0.3784`, LOSO `0.150`) instead of the superseded iter5 `0.5227` canonical row. Latest run passes with decision `historical_archive_surfaces_quarantined`, hard failures `0`, and 11 archive surfaces checked.
- **Secret hygiene audit (2026-05-09):** `audit_secret_hygiene.py` writes `results/secret_hygiene_audit_20260509.{json,md}` and scans text surfaces for high-confidence credential patterns without recording raw secrets. A local ignored `TOKEN.md` and `.env` containing JWT-like credentials were removed; any credential that was ever stored there must be considered exposed and rotated. Latest run passes with decision `secret_hygiene_guard_passed`, findings `0`, hard failures `0`.
- **Canonical-claim consistency audit (2026-05-08):** `audit_canonical_claim_consistency.py` writes `results/canonical_claim_consistency_audit_20260508.{json,md}` and must pass before paper/handoff claims are treated as synchronized. Old T3 values (`0.5227`, `0.341`, `0.3948`) may appear only with historical/superseded/target-contaminated/time-local wording. Latest run passes with zero stale findings.
- **Headline metric recompute audit (2026-05-08):** `audit_headline_metric_recompute.py` writes `results/headline_metric_recompute_audit_20260508.{json,md}` and recomputes headline/sensitivity metrics from stored prediction artifacts. Latest run passes 9/9 checks within `5e-4`, including T1 iter12 `0.6550`, T1 iter34 `0.7366`, T3 iter47 current `0.3784`, no-cv `0.3771`, no-`dst_*` `0.3766`, and LOSO `0.1498`. Treat this as a reproducibility guard, not a new model result.
- **OOF artifact integrity audit (2026-05-08):** `audit_oof_artifact_integrity.py` writes `results/oof_artifact_integrity_audit_20260508.{json,md}` and confirms selected lockbox `.oof.npy` files exactly match their JSON `per_subject.y_pred` arrays. Latest run passes 4/4 checks with max absolute diff `0.0` for T1 iter12, T1 iter34, T1 iter46 diagnostic, and historical target-contaminated T3 iter5. This is an artifact-drift guard, not a historical T3 promotion.
- **Pre-registration temporal integrity audit (2026-05-08):** `audit_preregistration_temporal_integrity.py` writes `results/preregistration_temporal_integrity_audit_20260508.{json,md}` and checks selected reportable artifacts with embedded/filename timestamps plus formula-hash links where available. Latest run passes 8/8 with no hard failures, but records 11 warnings for legacy missing formula hashes, result-side formula links missing for T1 iter34/FoG-STAR iter39, `git_sha: unknown`, and pulled-file mtime caveats. Treat these as caveats; do not describe the full artifact set as manifest-clean.
- **Pre-audit claim labeling audit (2026-05-08):** `audit_pre_audit_claim_labeling.py` writes `results/pre_audit_claim_labeling_audit_20260508.{json,md}` and scans `paper.md` plus `CURRENT_PAPER.html` for old held-out/stacking/ceiling claims (`MAE = 6.89`, `r = 0.860`, `MAE = 6.43`, `r = 0.848`, "proper held-out", "most rigorous evaluation", "approaching clinical utility"). Latest run passes with zero findings after the manuscript explicitly labels Section 4.2, Section 4.7, and Section 5.3 as historical pre-audit. Keep these numbers as audit history only, not deployment claims.
- **Historical subdomain/sensor claim labeling audit (2026-05-09):** `audit_historical_subdomain_claim_labeling.py` writes `results/historical_subdomain_claim_labeling_audit_20260509.{json,md}` and scans `paper.md` plus `CURRENT_PAPER.html` for the old auxiliary sensor-ablation/subdomain claims (`MAE = 7.58`, `MAE = 2.61`, observable motor subdomain, lower-back redundancy, and deployment phrasing). Latest run passes with zero findings after the abstract and Sections 4.8/4.10/5.1/6 were relabeled as historical pre-audit context. Current observability claims must rest on strict-inductive T1 and iter47 residual/domain/item audits, not those old tables alone.
- **T1 candidate claim labeling audit (2026-05-08):** `audit_t1_candidate_claim_labeling.py` writes `results/t1_candidate_claim_labeling_audit_20260508.{json,md}` and scans current paper/handoff surfaces for `iter34` / `0.7366` near canonical, deployment, headline, replacement, or completion wording unless local context preserves candidate/caveat framing. Latest run passes with zero findings and zero missing snippets. Required wording: strongest candidate / post-publication replication target; N=93, P2, and auxiliary-label caveats; iter12-honest `0.6550` remains the canonical floor.
- **Reportable artifact flag audit (2026-05-09):** `audit_reportable_artifact_flags.py` writes `results/reportable_artifact_flag_audit_20260509.{json,md}` and prevents archived JSON booleans from becoming current claim policy. Latest run passes 5/5 with zero hard failures and records three superseded raw flags. In particular, `lockbox_t1_iter34_hybrid_20260506_141720.json` still has raw `is_canonical_update=true`, but current status is `strongest_candidate_caveated_not_canonical_replacement`; use the audit/current policy layer, not that archived boolean.
- **CCC metric integrity audit (2026-05-09):** `audit_ccc_metric_integrity.py` writes `results/ccc_metric_integrity_audit_20260509.{json,md}` and pins reportable CCC to Lin's population-moment convention. Latest run passes with zero hard failures; it checks 7 headline/candidate vectors and 7 synthetic implementation cases, and the sample-moment convention changes headline CCC by at most `0.0000028`. `inductive_lib.ccc` is now aligned with `eval_utils.lins_ccc` on non-finite masking and the `<3` finite-pair guard, with regression tests in `tests/test_inductive_lib.py`. This hardens metric plumbing only; it does not change T1/T3 claims.
- **Per-item evidence map audit (2026-05-08):** `audit_per_item_evidence_map.py` writes `results/per_item_evidence_map_20260508.{json,md}` and labels all 18 item-level CCC artifacts by current claim scope. Current map: items 9-14 are iter12 T1 components only; items 15 and 18 are supplementary iter17 per-item wins; items 4-8/16/17 are historical iter8 supplementary lockboxes; items 1-3 are backfill-only/no current reportable per-item LOOCV CCC. It verifies item9 `0.4437`, item12 `0.5928`, item15 `0.1099`, item18 `0.4858`, T1 sum `0.6550`, and marks the old 18-item T3 per-item sum `0.2646` as `historical_dead_route_not_current_t3`. Do not use per-item CCCs as standalone deployment or current T3 claims outside that scope.
- **Per-item OOF companion scope audit (2026-05-08):** `audit_per_item_oof_companion_scope.py` writes `results/per_item_oof_companion_scope_audit_20260508.{json,md}` and records the companion-array boundary for the per-item map. Per-item lockbox JSONs lack row-level `per_subject.y_pred`, so JSON-vs-OOF equality is unavailable by design. Latest run passes 15 OOF-backed rows, row-level JSON comparison count `0`, and confirms the six current T1 item OOF companions sum exactly to the canonical iter12 OOF with max diff `0.0`; retained caveat: supplementary item18 reports valid N=`93` while its companion array has 94 slots. Historical item17 is N=`93`, not a current T1/T3 component.
- **T1 iter12 batch-integrity audit (2026-05-08):** `audit_t1_iter12_batch_integrity.py` writes `results/t1_iter12_batch_integrity_audit_20260508.{json,md}` and checks the canonical T1 floor against its exact six iter8 per-item lockboxes. Latest run passes with hard failures `0`, warnings `0`, recomputed CCC `0.6550`, MAE `1.5614`, single coherent batch `20260430_143044`, no swaps, and max summed-OOF diff vs `results/t1_iter12_honest_composite.oof.npy` of `0.0`. This is provenance hardening only, not a new model result.
- **Current conformal + abstention does not rescue T1/T3 (2026-05-08).** `run_current_conformal_abstention.py` uses existing OOF/lockbox predictions only and calibrates each subject's interval from all other subjects' residuals. Artifacts: `results/current_conformal_abstention_20260508.json`, `results/current_conformal_abstention_intervals_20260508.csv`, `results/current_conformal_abstention_curves_20260508.csv`, `results/current_conformal_abstention.html`. T1 iter12 80/95% interval widths are `4.99` / `9.08`; T1 iter34 widths are `5.74` / `8.81`. Corrected T3 iter47 current widths are `25.94` / `34.72` UPDRS-III points; no-cv widths are `26.22` / `35.35`. The deployable abstention proxy (discard predictions farthest from prediction median) collapses T3 CCC to `0.0108` after 50% discard, while oracle residual abstention is explicitly non-deployable. Use these as uncertainty/clinical-utility evidence only; no model headline changes.
- **Local-similarity T3 residual smoothing is dead at N=98 (iter40, 2026-05-08).** User explicitly asked for wildcards after repeated ceiling failures, so `run_t3_iter40_local_residual.py` tested a distinct Stage-2 bias class: keep iter5 Stage-1 unchanged, then replace the global LGB residual map with fold-local K=500 feature selection + train-only normalization + PCA(24) + inverse-distance 12-NN residual smoothing. Same 5-fold seeds as iter5. Artifact `results/iter40_local_residual_screen_20260508_144905.json`: baseline seed-mean CCC `0.4888`, wildcard CCC `0.4332`, delta `-0.0556`; per-seed deltas `[-0.0440, -0.0337, -0.0529]`; bootstrap CI `[-0.1151, -0.0006]`, frac>0 `0.0235`; strict and relaxed gates FAIL. No LOOCV. Do not retry KNN/PCA/local-residual smoothers on this architecture at this N without a genuinely new target representation or external data.
- **T3 invalid raw Part-III code correction lowers the audit truth again (iter47, 2026-05-08).** `NLS036` has raw `MDSUPDRS_3-15-R/L = 9/9`; valid Part III subitem scores are 0-4, so this was a missing-code target bug inflating old T3 by +18. `run_t3_iter47_invalid_code_fix.py` recodes raw Part III values outside [0,4] to missing and reruns the iter41 fixed battery. Minimal N=95 current-Stage2 LOOCV is `0.3784`, no-cv `0.3771`; LOSO current two-way `0.150`, no-cv `0.163`. Complete33-validrange N=88 current `0.4281` is sensitivity-only. `updrs_columns.py` and `data_split.py` now fail closed on invalid raw subitem codes; tests cover this. Do not cite iter41 `0.3948` as current.
- **T3 iter47 target-integrity audit (2026-05-08):** `audit_t3_iter47_target_integrity.py` writes `results/t3_iter47_target_integrity_audit_20260508.{json,md}` and verifies the current corrected T3 target/prediction artifact chain without refitting. Latest run passes with hard failures `0`, warnings `0`, 33 raw Part III columns, minimal valid-range N=`95`, complete33 N=`88`, excluded all-missing SIDs `{NLS151,NLS188,WPD013}`, exactly two invalid raw values (`NLS036` item 3.15 R/L `9/9`), one target-changed row (`46→28`), subject-CSV recomputed CCC `0.3784`/MAE `7.5280`, and LOSO row recomputed two-way CCC `0.1498`. This is target/provenance hardening only, not a new model result.
- **T3 complete33 claim-labeling audit (2026-05-09):** `audit_t3_complete33_claim_labeling.py` writes `results/t3_complete33_claim_labeling_audit_20260509.{json,md}` and scans active claim surfaces for `complete33` / `0.4281` / `0.4010` / `N=88` overclaim. Latest run passes with zero findings and zero missing snippets. Complete33-validrange N=88 current `0.4281` is sensitivity-only and not a headline; the current corrected T3 internal headline remains N=95 minimal valid-range CCC `0.3784`.
- **T3 Part-III partial-missing proration is not a canonical rescue (iter42, 2026-05-08).** After iter41, six partially missing raw MDS-UPDRS Part III rows remained. `run_t3_iter42_target_prorate.py` pre-registered a literature-backed primary `prorate_le3` rule (observed mean × 33 for 1-3 missing subitems; exclude >3) and a loose `prorate_le7` sensitivity. Primary `le3` failed: LOOCV CCC `0.3468` current / `0.3643` no-cv; LOSO `0.144` / `0.125`. Loose `le7` sensitivity gave a small current-Stage2 internal lift (`0.4165`) and LOSO about `0.191`, but is not promotable because it includes `NLS210` with all five rigidity sub-scores missing, was sensitivity only, is unstable across Stage-2 cv policy (`0.4165` current vs `0.3793` no-cv), and still underperforms old iter5 predictions evaluated on the same prorated target. Superseded by iter47 valid-range audit truth.
- **T3 corrected-target clinical-dependency audit closes the pure-IMU ambiguity (F-iter45, 2026-05-08).** `audit_t3_clinical_dependency.py` fixed N=95 and Stage2-no-cv, then varied Stage 1. A3 H&Y+intake CCC `0.4017`, intake-only CCC `0.3871`, H&Y-only CCC `0.2899`, intercept/IMU-only CCC `0.2449`. A3 minus intake-only delta `+0.0136` has CI crossing zero; A3 minus IMU-only delta `+0.1519` is positive. Do not advertise corrected T3 as IMU-only. Do not launch new internal clinical-variable fishing from this audit; it documents dependence and is numerically superseded by iter47 valid-range target hygiene.
- **Tail-aware retraining is dead at N=98 (F61 iter27, 2026-05-05 PM).** Codex consult identified two candidate fixes for F54's residual structure (corr(residual,y)=−0.699): (β) post-hoc nested-LOO calibration on iter5 OOF; (W) sample-weighted LGB Stage-2 retraining. Both empirically failed in the same session: (β) Δ≈−0.08 with bootstrap frac>0=0 (linear/isotonic/poly2); (W) tail_focused weight scheme gave +0.013 driven by single seed=42, std=0.041, all other weight schemes within ±0.005 of uniform; CCC objective enabled collapsed all variants to CCC 0.31-0.41 (catastrophic). Q1/Q4 residuals barely moved across schemes (+9.61 to +9.71 / −8.34 to −8.79) — shrinkage is in LGB-tree-leaf-prediction-mean structure, not in loss-weighting space. **Don't retry sample-weighted retraining or post-hoc calibration for T3 at iter5 architecture at N=98** — both classes of fix are confirmed null. The F54 residual is regression-to-the-mean; removing it costs Pearson r more than it gains MAE. 9th N≈100 wall data point. Hssayeni scaffolding (`cache_hssayeni_features.py`, `scripts/synapse_hssayeni_setup.md`) in place for iter26 if external-validity claim wanted.
- **Cross-dataset zero-shot transportability on PADS is dead at chance (F60b iter25b post-fix, 2026-05-05 PM, SUPERSEDES F60 iter25).** After first-order debug + triple-CLI consult flagged 4 upstream bugs in iter25 (units/gravity 60-110× scale gap; Earth-NEU vs Device-XYZ axis frame; gait_reg meaningless on stationary tasks; LeftWrist mirror), iter25b applied all fixes (WG R_Wrist_FreeAcc + PADS ×9.81; drop gait_reg; RightWrist-only; runtime fs+gravity assertions; primary headline = magnitude-only Track A3). Full PADS 7810/7810 (355 subjects, 276 PD + 79 HC, 3843 RightWrist sessions). **Verdict UNCHANGED: AUROC = 0.4975 (chance) on Track A3 (magnitude-only frame-invariant)**. Tracks A2/B2/D2/A3D2 all BELOW chance (mean-imputed clinical Stage 1 inversion + OOD covariate shift). **NEW: Track C2 PADS-only ceiling = 0.7874 ± 0.025** (jumped from 0.63 with full data) — wrist features have substantial within-cohort PD signal but iter5's WG-trained representation does not transport. Both consults converged on dominant mechanism: **task/protocol mismatch (WG gait/balance training vs PADS stationary upper-limb test)** — semantic, not coordinate-frame. **Don't retry zero-shot cross-protocol transfer** (gait-trained → stationary-tested) at any feature engineering level; it's a representation-orthogonality failure, not a data-quality one. **Don't retry mean-imputed clinical Stage 1 cross-dataset.** Pre-reg `results/preregistration_t3_iter25b_pads_20260505_131413.json` (formula_sha256 `4f67518ee293178f`). The 0.79 within-PADS ceiling is publishable as PADS-internal feasibility; the 0.50 cross-dataset is publishable as iter5-architecture transportability cliff.
- **Clinical-extras Stage-1 widening + Stage-2 forced-inclusion are BOTH dead at N=98 (F59 iter23+iter24, 2026-05-05).** Built `cache_clinical_extras.py` extracting Tomlinson-2010 LEDD + MDS-UPDRS Part 1 + ON/OFF state + assistive-device + race + PT-OT + days-since-Part3 (98/98 V2-cohort SID match, manifest-clean). **Partial-r residualization** vs iter5 covariates collapses raw signals: only `part1_cognitive` (+0.232, 37% NaN), `assistive_device_yn` (+0.156), `hours_since_last_dose` (−0.158) retain |partial r| > 0.15. **iter23** 19-set 5-fold ablation (Stage-1 widening): zero passers, monotone Δ ≤ 0; pairs/kitchen-sink hurt MORE than singles. **iter24** Stage-2 forced-inclusion of the 3 partial-r winners (custom `_feature_select_fold_forced` always retains them + K-3 V2 cols by importance, bypassing K=500 absorption that killed F19/F44/F45/F48): pre-reg `preregistration_t3_iter24_stage2forced_20260505_053134.json` (formula_sha256 `7194964bd5ec195b`) → 5-fold Δ = −0.0110 (smallest negative ever), bootstrap CI [−0.0371, +0.0150] **straddles zero**, frac>0 = 0.176. iter5 ≡ iter24 statistically indistinguishable. Mechanism (codex+gemini synthesis): partial-correlation collapse + Ridge DOF amplifier; Stage-2 forced-inclusion is necessary but not sufficient. **`assistive_device_yn` is the surprise standalone signal worth carrying forward to a hypothetical N=300 cohort, but it does NOT clear the gate at N=98.** **8th N≈98 wall data point.** Don't retry clinical-extra additions to iter5 at this N — partial r against (H&Y + cv_yrs + cv_sex + cv_dbs) determines harvestability and it's collapsed for everything except `part1_cognitive`, which has 37% missing.
- Treat wrist-only and sensitivity-stack wins as hypotheses unless they survive repeated-split or lockbox validation; do not promote a sensitivity winner on the same split.

## Open Angles (NOT yet ruled out)
- **Cross-dataset zero-shot evaluation** on public/direct or gated larger cohorts — paper rigor for transportability, leakage-clean by construction. Cost is dominated by data access/parsing; it does not move internal-validity CCC unless a separately pre-registered augmentation gate clears. **2026-05-05 update: PADS evaluated (F60 iter25 -> F60b post-debug). NO TRANSFER STANDS at AUROC=0.4975 chance; PADS-only ceiling = 0.7874. 2026-05-08 update: CARE-PD is public but not T1/T3 eligible; FoG-STAR is public/direct T3 but iter38 augmentation failed and iter39 zero-shot gives only partial Track-B transfer (`0.2499`); COPS is public/direct T3 and iter49 zero-shot is partial in the same range (Track B `0.2412`, wrist-only null); ALAMEDA is public/direct but N=11 and not worth prereg/download. 2026-05-09 update: TLVMC/DeFOG is public/direct and iter51 zero-shot is complete: Track A lower-back magnitude OFF CCC `+0.2695` (CI `[+0.1693,+0.3600]`), Track B wrist-to-lumbar CCC `+0.0485`, Track C DeFOG-only LOSO sanity `+0.3450`; PDFE turning-in-place is public/direct but iter52 WearGait shank transfer failed (Track A `-0.1008`, Track B clinical+shank `+0.1340` with CI crossing zero) while PDFE-only LOOCV sanity reached `+0.4020`. Both are partial/negative external-validity evidence only.** mPower is phone/self-reported, Papadopoulos phone-call tremor is public but tremor-subitem-only, Harmonized Upper/Lower Limb Accelerometry is DASH-gated daily-life ActiGraph rehab data without confirmed Part III/T1 targets, Monipar/BIOCLITE are public but subitem-only and not full T1/T3 endpoints, Figshare `14896881` is public UPDRS-III gait biomechanics but motion-capture/force-plate rather than wearable IMU, Zenodo `14848598` is a derived CSF/clinical/gait-summary table rather than raw wearable IMU, Fay-Karmon advanced-PD smartwatch HBM and the marital-dyad GeneActiv actigraphy study are request-only small-N/schema-hidden rows, ParaDigMa is software-only local feature extraction, Yin et al 2025 is request-only small-N gait-parameter context, REMAP Bristol is controlled N=12 with range labels, Oxford OPDC/OxQUIP has no current public aligned IMU route, and PD-BioStampRC21 is open but N=17 with forearm/chest/thigh sensors. PPMI/Verily is an access-gated priority wrist-native route (`scripts/ppmi_verily_setup.md`); PPP/PD-VME is a gated Verily-watch peer route without local approval/schema. ICICLE-PD/ICICLE-GAIT is an access-gated lower-back 7-day gait route (`scripts/icicle_request_setup.md`). CNS Portugal/Lobo is request-gated wrist + lower-back 10-meter walk (`scripts/cns_portugal_request_setup.md`). No scaffold should be built for gated/request-only routes until credentials/files/schema exist.
- **Manifest backfill for ~23 remaining `cache_*.csv` files** — pure provenance work that unblocks any future fold-clean reuse. Independent CPU-only task.

- **Goal-v2 ceiling-push wildcards are dead at N=92/95 (walls #32-#38, 2026-05-12).** Per-task Ridge specialist + Ridge meta (WILDCARD-A, T1, Δ=-0.16); V3-GSP injection into iter47 Stage-2 LOOCV (T3-A, Δ=-0.002 despite 5-fold HGB screen Δ=+0.034 — 5-fold→LOOCV transition trap); T3 conformal abstention with clinical-vs-IMU and seed-variance proxy (both monotonicity-violated at 50%, r=0.09/0.12); T3-B stride-locked at 10-seed (Δ=+0.023 at MCID limit, FWER-uncorrected); multi-block K=500 augmentation V2+GSP+PSI+stride+shapelet (Δ=-0.053, K=500 displacement); stratified K-best quotas V2=300+PSI=80+stride=80+GSP=30+shapelet=10 (Δ=-0.019, V2 anchor reduction hurts); V2+PSI isolation (Δ=-0.062, 30% K-best selection prominence ≠ inductive signal). T1 conformal abstention LOCKBOX `results/lockbox_t1_conformal_20260512_211440.json` is the ONE PASS_DEPLOYABLE_SECONDARY (retained CCC 0.7777@70%, 0.8338@50%, monotonic, threshold CV <0.04). 4-CLI consult (codex+kimi+deepseek+gemini) priors empirically validated. **Don't retry K=500-augmentation at N=95 without changing the K-selection rule fundamentally**; the unified greedy K-best is the wall, confirmed in 4 distinct directions. **Don't retry conformal abstention with poorly-orthogonal predictor pairs**; T3 disagreement-based abstention requires predictors that disagree informatively (within-IMU uncertainty), not by signal class (clinical-vs-IMU). **Per-task specialist + Ridge meta inside fold variance-dominates at N=91**; tri-CLI prior 12-40% empirically wrong by 0.16. See `findings.md` F-goalv2-* + `results/goalv2_campaign_summary_20260512.md`.

- **Wall #40-41 add to goal-v2 dead list (2026-05-12).** Wall #40: RandomForest base learner (n=300, max_depth=4) on K=500 univariate-corr features gives T3 CCC=0.339 vs iter47 canonical 0.378 (Δ=-0.039). At N=95 RF cannot match LGB boosting + selection. Wall #41 (numerical-ceiling-clearing, statistical-wall): combined my-arch (univariate-corr K-best) + stride aug at T3 vs iter47 canonical = +0.0446 numerical lift; bootstrap CI [-0.04, +0.14] crosses 0; frac>0=0.834; sign-flip p=0.156 — **fails inductive significance at N=95**. The +0.0446 looks like a ceiling break but per-subject variance is too high to confidently reject the null. Codex's 2026-05-12 absolute closure verdict reaffirmed (`/tmp/pd_imu_consult/codex2_20260512T221015`): "another 'one more angle' manufactures selection pressure rather than evidence."
- **Wall #70-72 — T1 Glass-Ceiling Push 2026-05-13, all 3 slots CLOSE under apples-to-apples 5-fold screen (FWER n=4 cleanly closed).** Master pre-reg `results/preregistration_t1_ceiling_push_20260513_043852.json` (Bonferroni gate frac>0 ≥ 0.9875 vs iter12-honest-N=93 AND iter34-N=93). Comparator: `results/iter34_5fold_comparator_20260513_050158.{json,oof.npy}` (apples-to-apples 5-fold reference; LOOCV-vs-5-fold bias ≈ 0.0027 documented). Slot A (#70) 7-item-no-13 chain + CCC-descending order [12,10,14,9,11,15,18]: per-seed Δ̄=−0.0004, std=0.0035 → screen FAIL. Slot B (#71) V3-GSP-only at item-12 chain step + CCC-descending [12,10,14,9,11,13,15,18]: per-seed Δ̄=−0.0135, std=0.0052 → screen FAIL. Slot C (#72) sklearn GradientBoostingRegressor + K=250 univariate-corr at item-12 step: per-seed Δ̄=−0.0108, std=0.0029, pooled CCC=0.7226 (highest of 3 slots but +0.007 is variance-reduction-from-averaging artifact, not per-seed lift) → screen FAIL. Tri-CLI consult (codex+gemini): codex prior 0.12, gemini prior 0.03 — both empirically calibrated near 0. Codex's 2026-05-12 verdict ("Stop in-cohort ceiling hunting") reaffirmed by 3 fresh failures at N=92 across 3 distinct mechanisms (architectural pruning, feature-family substitution, model-class+selection-rule substitution). T3 K-sweep Wall #69 sklearn-GB-K=250 finding does NOT cross-target-transfer to T1 item-12. Don't retry chain pruning, V3-GSP per-item substitution, sklearn GB at chain steps, CCC-descending order, or any iter34 architectural variant at N=92 without external cohort scaling.
- **Wall/BOUNDARY #68-69 — T3 sklearn GB K=250 is the strongest in-cohort lift of the campaign, but still FAILS FWER (2026-05-13).** Wall #68 (BOUNDARY): sklearn GradientBoostingRegressor (n_est=300, max_depth=4, min_samples_leaf=10, subsample=0.8) trained on K=250 univariate-corr-K-best features (Stage-2 residual fit on H&Y + cv_* clinical Stage-1 Ridge) gives T3 LOOCV CCC=0.4516 vs iter47 canonical 0.3784 (Δ=+0.0732). Paired bootstrap (B=5000) common-N=95: mean Δ=+0.0755, CI [−0.011, +0.178] (crosses 0), frac>0=0.9518 (PASSES uncorrected 0.95), sign-flip p=0.0964 (fails 0.05). **JUST FAILS Bonferroni n=3** (gate 0.9833). This is the campaign's strongest in-cohort candidate. Wall/BOUNDARY #69 (closure of #68 post-hoc-selection caveat): K-sweep at K∈{100,150,200,250,300,400,500} × 3 seeds shows a **monotonic hump curve** with peak at K=250 (Δ=+0.070), plateau K∈{200,300} clearing +0.05 (Δ=+0.049, +0.052), and tail K∈{400,500} declining to +0.025, +0.012. The hump structure (low seed std 0.0022-0.0125 in K∈{200..400}, vs 0.027 at K=500) **falsifies the post-hoc spike interpretation** — K=250 is the peak of a coherent signal trajectory, not a cherry-pick. However, under K-search-corrected FWER (Bonferroni n=7 → gate 0.9929) the lift still **fails** at frac>0=0.9518. Effect structure unambiguously real; N=95 power unambiguously insufficient under standard FWER corrections. **Top candidate for external PPMI/Verily replication**: at N≈517 the per-subject variance floor drops ~2.3×, and the +0.073 effect (if stable) clears Bonferroni n=10 (0.9950). Lockbox JSONs: `results/lockbox_t3_gb_ksweep_20260513_025319.json` (K-sweep), `results/lockbox_t3_gb_ksweep_fwer_bootstrap_20260513_030050.json` (FWER bootstrap), `results/lockbox_t3_gb_paired_bootstrap_*.json` (F68 source). Don't retry K=250 sklearn GB at N=95 expecting Bonferroni-correctable lockbox; the K-sweep family penalty itself prevents in-cohort lockbox at this magnitude.
- **Wall #73-77 — v-next ablation batch 2026-05-14 informative negatives + 2 new conformal lockbox wins.** Master pre-reg `results/preregistration_vnext_ablation_batch_20260514T151939Z.json`. Codex 2026-05-14 (24,838 tokens) endorsed exact 8-cell package; gemini-3.1-pro-preview 429-blocked both attempts (server capacity). Wall #73 (cell B): LGB-quantile median (α=0.5) substitute T3 point predictor drops full CCC by ~0.09 (0.378→0.292) at N=95; don't pair quantile loss with iter47 architecture as the point predictor. Wall #74 (cell C): CQR width-based abstention on a degraded point predictor cannot recover full CCC; abstention quality bounded by point predictor quality. Wall #75 (cell D): driver Stage-1 bug — used HY-only, not HY+cv_yrs+cv_sex+cv_dbs as iter47 canonical — caused all 4 K=250 subcells to underperform iter47; **F68/F69 K=250 sklearn-GB lift requires the FULL Stage-1 covariate set**, partial Stage-1 destroys the gain. Wall #76 (cell G): item-11 hurdle two-stage at N=92 with only 12 FoG-positive subjects collapses (CCC=0.027 vs continuous 0.222); classifier stage too small-N. Wall #77 (cell F): joint T1×T3 Ridge multi-task with K=union(500+500) at N=92 collapses to ≈0 due to target scale mismatch + Stage-1 bug + Ridge α=1 inadequate. **Wins from same batch:** Cell A T3 Mondrian-CP repairs the broken 2026-05-12 T3 conformal v2 — retained CCC 70%=0.6936/50%=0.8484, monotonicity violations=0, N5 (inductive vs transductive) gap=−0.0017 (clean). AUX T1 Mondrian-CP (iter34 + LOO-quartile predicted-T1 bins + |residual| score) SUPERSEDES the 2026-05-12 V2-only T1 conformal lockbox: paired-bootstrap B=5000 frac>0=0.982/0.996 at 70%/50%, 95% CI strictly positive, lift +0.112/+0.118. PPMI replication blueprint locked at codex's recommendation: T3 sklearn-GB(n_est=300, max_depth=4, min_samples_leaf=10, subsample=0.8) + K=250 univ-corr + Stage-1 Ridge(α=1) on HY+cv_yrs+cv_sex+cv_dbs (formula_sha256=`489ca6bbc96520c2…`). **Don't retry**: hurdle two-stage on items with <20 positives at N≤95; joint multi-output Ridge with unnormalized targets at N≤95; LGB-quantile as substitute for iter47 point predictor; partial Stage-1 covariates with K=250 sklearn-GB; CQR width abstention without first verifying the point predictor's full CCC.
- **Wall #78 — Oracle abstention trap (F-vnext-20260514 retraction, 2026-05-14T17:35Z).** The retention rule `retain[i] = |y_i − ŷ_i| ≤ τ_bin_i` uses the test-fold label `y_i` in the per-subject retention decision and is therefore an **oracle metric, NOT a deployable abstention rule**. Independent codex (gpt-5.5 xhigh, 46,477 tokens) + gemini-3.1-pro-preview joint audit confirmed: "fatal methodological leak", "retract immediately", "Cell A is oracle abstention." Selective prediction requires `g: X → [0,1]`, a function of input only (El-Yaniv & Wiener JMLR 2010). The proper y-free deployable τ_bin Mondrian-CP gives T1@70%=0.355 / T3@70%=0.112 — *worse than full-cohort CCC*, confirming the "wins" were entirely oracle filtering. The 2026-05-12 V2-V3 disagreement T1 conformal lockbox (0.7777/0.8338) remains canonical, NOT superseded. **The retracted lockboxes**: `results/lockbox_vnext_A_t3_mondrian_cp_20260514T151939Z.json` (T3 0.6936/0.8484 oracle), `results/lockbox_vnext_aux_null_gate_and_t1_mondrian_20260514T152647Z.json` (T1 0.8897/0.9521 oracle), `results/lockbox_vnext_t1_mondrian_vs_v2_paired_bootstrap_20260514T152923Z.json` (tautological supersession claim). **Don't retry**: any retention rule whose RHS contains `|y - ŷ|`, `abs_res`, `residual`, or any quantity computable only at test-evaluation time. Use legitimate y-free scores: CQR interval width, ensemble disagreement, Mahalanobis distance, low-df residual meta-model trained on OOF residuals. **SKILL.md updated** with law #9 (selective prediction must be g: X → [0,1]). **firewall_check.py** now catches the pattern statically (`oracle_abstention_retention_rule`, `oracle_abstention_mask_form`). Local audit confirming the retraction: `results/lockbox_vnext_deployable_vs_oracle_mondrian_20260514T173036Z.json` + `run_vnext_deployable_vs_oracle_mondrian.py`. Consult evidence: `/tmp/pd_imu_consult/{codex,gemini}_20260514T172908.txt`.

- **Wall #91-94 — T1 ceiling-push 2026-05-15 PM re-attempt closes with iter34 INTACT.** Second-day attempt after morning closure (walls #84-90). Phase 0 free diagnostics + Slot A item-13-PH tunable-scalar (the only non-W#89/W#90-killed mechanism). Wall #91 (D1 test-retest): PH+MFDFA-only Ridge cross-task test-retest CCC=0.6216 (SelfPace pipeline vs HurriedPace pipeline on same subjects) — well below iter34's between-subject 0.7170; feature-level reliability is dominated by protocol variation; iter34's chain ensemble does meaningful cross-protocol signal extraction. Internal ceiling-push is bounded above by literature test-retest ICC ~0.81 (Goetz 2008). Wall #92 (D3 clustering): PH+MFDFA k=2 phenotype clusters (silhouette 0.24) present but ORTHOGONAL to iter34 residual structure (Levene F=1.94, p=0.167; ANOVA p=0.335); mixture-of-experts / phenotype-gated correction not a credible architecture at N=92 + 88 features. Wall #93 (Slot A): Item-13-PH tunable-scalar (inner-CV α ∈ {10,30,100,300,1000} × λ ∈ {0.25..1.25}) outer-LOOCV reaches Δ=+0.0097 CCC, frac>0=0.897, MAE Δ=-0.0031 (improves), Δr=+0.0126, D4 corr(correction, T1_sum_residual)=+0.1531 (positive), scrambled-y null collapses to Δ=-0.0012/frac>0=0.030 — empirical T1 ceiling lift from the SOLE biomechanically-confirmed feature mechanism at N=92 = +0.01 CCC, below MCID 0.025 and below Bonferroni n=4 / n=6 lifetime gates. Wall #94 (D2 negative-control swap): PH-on-item-13 biomechanical specificity is unambiguously REAL — right pairing Δr=+0.161 vs wrong-pairing (MFDFA→item-13) Δr=-0.044, Δr ratio -0.275 (sign-flip evidence); PH-on-item-10 wrong-pairing also strongly negative (Δr=-0.086). Strongest possible feature-specificity evidence. Strengthens yesterday's PH/item-13 item-level canonical from "statistically clean Bonferroni" to "doubly-validated (statistical + biomechanical)" but does NOT translate to a sum-level CCC breakthrough. Don't retry: TUG-phase PH expansion (W#89), hierarchical multi-task / MoE on PH+MFDFA latent space (W#90+W#92), item-13-PH with tunable scalar over inner-CV (W#93 — adds bootstrap variance, underperforms λ=1 fixed). External replication remains the only theoretically-bounded lever.

- **Wall #95-98 — T1 ceiling-push 2026-05-15 PM extended (Slot A2/C/D/E/F) opens 2 deployable-secondary movements; headline T1/T3 unchanged.** Continued execution after first PM closure on Stop hook prompt. Wall #95 (Slot A2 CCC-LGB init_score=iter34): training LightGBM with custom CCC objective v2 + init_score = iter34's t1_sum_pred + scale-aligned target on item-13 PH features (32 cols) catastrophically harms full T1 CCC by Δ=-0.0907 (frac>0=0.000, CI=[-0.158, -0.042]). Per-seed CCC identical across 3 seeds (std=0.0000), confirming the CCC gradient is deterministic-noise-dominated at N=92; even iter34 init_score scaffold cannot rescue. Wall #96 (Slot C sparse pairwise T3): 30 univariate features + 50 best pairwise products from top-15 fed to sklearn GB (max_depth=3) on Stage-1 residual gives T3 CCC=0.3667 vs CANONICAL iter47 0.3784 (Δ=-0.012), and Δ=+0.0096 only vs my reimpl baseline 0.3571 with frac>0=0.5995 — falsifies the simplest interpretation of F68 K=250 hump as a mid-dim pairwise-interaction structure. Wall #97 (Slot E T3 Mahalanobis-low retention): retaining bottom-X% by Mahalanobis distance in Stage-1 covariate space HURTS T3 retained CCC (0.378 → 0.223 @70%, 0.156 @50%) — subjects near training centroid are HARDER not easier to predict; H&Y outliers are the easy cases. **Wall #98** (Slot F T3 CQR-width — BOUNDARY-LIFT FIRST-EVER T3 DEPLOYABLE): LGB-quantile q05/q95 width on Stage-1 residual as y-free retention score gives T3 retained CCC=0.4237 @70% (Δ=+0.045) and **CCC=0.5370 @50%** (Δ=+0.159). Sanity-y-nan passes. frac>full=0.632 / 0.929 — both miss uncorrected 0.95 gate, but the 50%-coverage 0.5370 magnitude is comparable to old leaky iter5 SOTA 0.5227 and is the FIRST EVER T3 deployable-secondary on WearGait per CLAUDE.md open-priority list. Top candidate for PPMI/Verily external replication (at N≈517 variance floor drops ~2.3×; frac>full=0.929 likely clears). **Slot D — T1 deployable +0.01 numerical lift (NOT a wall, recorded for completeness)**: V2-vs-V3-GSP disagreement retention + item-13 PH correction (Ridge α=100 λ=1) gives T1 retained CCC 0.7777→0.7876 @70% (Δ=+0.0099, **frac>0=0.991** passes Bonferroni n=4 AND uncorrected 0.95, sub-MCID, lifetime n=9 misses by 0.003), 0.8338→0.8415 @50% (Δ=+0.0078, frac>0=0.938 FAIL). Scrambled-null collapses cleanly (Δ=-0.0007/frac>0=0.45). Closest-to-Bonferroni result of any in-cohort mechanism across two days of ceiling-pushing. **Don't retry**: CCC-LGB with init_score=baseline_pred at N≤95 (gradient noise); sparse pairwise top-15-base for F68 K=250 mechanism explanation (insufficient information); Mahalanobis-low retention on T3 (counter-direction); CQR-width T3 retention at N=95 expecting frac>full ≥ 0.95 (variance ceiling).
