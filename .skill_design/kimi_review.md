I’ll ground the critique in the current paper/rendering artifacts rather than treating the brief abstractly. I’ll inspect the legacy command, renderer validation, and current paper surface, then return a concrete redesign critique.
**Critical Corrections**
The redesign is directionally right, but I would fix these before implementing:

1. The brief contains a T3 LOSO inconsistency. `0.419` and `0.263` average to `0.341`, not `0.150`. Current repo truth in `AGENTS.md` says iter47 valid-range LOSO is `NLS->WPD 0.194`, `WPD->NLS 0.106`, mean `0.150`; `0.341` is historical/pre-iter47. The skill must hard-fail on this kind of contradiction.

2. Source ordering is contradictory. The brief says `AGENTS.md > findings.md > CLAUDE.md`, then says cite only the SOTA table in `CLAUDE.md`. The skill should resolve this as:
   `AGENTS.md` for policy/status labels, `CLAUDE.md` SOTA table for headline numbers, result JSON/OOF artifacts for numeric provenance, `paper.md` only as manuscript surface.

3. Do not let `REQUIRED_SNIPPETS` stay as the primary validation mechanism. In `render_current_paper.py`, it is a brittle string-presence check. It proves wording exists, not that numbers are correct, sourced, role-labeled, or figure-matched.

---

**1. Figure Suite**
I would target 10 figures, with 6 main-text and 4 extended/supplementary. Every figure should be generated by `paper_figures.py` from `results/*.json`, `.csv`, and `.oof.npy`, never from inline constants.

| Figure | What It Shows | Data Sources | Story |
|---|---|---|---|
| `fig1_audit_timeline_benchmark_reset.png` | Timeline from pre-audit headline to iter12/iter34/iter47 audit-truth numbers, with retracted/historical/current lanes | `CLAUDE.md`, `results/canonical_claim_consistency_audit_*.json`, `results/headline_metric_recompute_audit_*.json`, lockbox JSONs | The paper is a benchmark correction, not a simple performance paper. |
| `fig2_t1_vs_t3_observability_ceiling.png` | T1 canonical, T1 candidate, T3 corrected, T3 LOSO, plus theoretical T3 bounds A/D/E | T1 iter12 JSON/OOF, iter34 JSON/OOF, iter47 JSON/OOF, theoretical-ceiling artifact if present | Gait-observable axial severity is learnable; total UPDRS-III is anatomically constrained. |
| `fig3_iter34_vs_iter12_paired_oof.png` | Subject-level paired T1 predictions: iter12 vs iter34 deltas, paired bootstrap distribution, CI, frac>0 | `results/t1_iter12_honest_composite.oof.npy`, iter34 OOF/lockbox JSON, `results/current_headline_influence_audit_*.json` | Iter34 is a real T1 lift, but still candidate/caveated. |
| `fig4_transportability_cliff.png` | LOOCV vs LOSO bars for T1 iter34 and T3 iter47, with site-direction arrows | iter34 LOSO artifact, `run_t3_iter47_invalid_code_fix.py --mode loso` result JSON | Internal validity does not imply cross-site transportability. |
| `fig5_t3_target_hygiene_waterfall.png` | Waterfall: historical iter5/iter41 to iter47, showing all-missing-row exclusion and invalid-code correction | `results/t3_iter47_target_integrity_audit_*.json`, iter41/iter47 JSONs | The T3 benchmark changed because labels were corrected, not because modeling got worse. |
| `fig6_item_level_observability_map.png` | MDS-UPDRS-III item CCCs grouped by observable gait/balance vs upper-limb/rigidity/tremor/non-observable, highlighting item15 and item18 | `results/per_item_evidence_map_*.json`, per-item lockbox JSONs, `results/t3_iter47_item_residual_audit_*.json` | Total UPDRS-III error is explained by item observability. |
| `fig7_leakage_audit_gate_matrix.png` | Null-gate panels: scrambled-label, SID-shuffle, canary, library-exclusion, transductive sanity; include iter34 P1/P2/P4 if artifacts exist | iter34 F73 leakage audit JSON, null-gate result JSONs | The candidate is not transductive leakage, but P2 flags OOD fragility. |
| `fig8_learning_curve_asymptote.png` | Iter22 learning-curve fit: `CCC(N) = 0.5975 - 2.1308*N^(-0.6408)` with observed points and asymptote | iter22 LC sweep JSON/CSV | More WearGait-like N helps but does not erase the architecture/target ceiling. |
| `fig9_t3_residual_anatomy.png` | Residual vs true severity quartiles, WPD/NLS site split, residual correlation with non-gait burden | `results/t3_iter47_residual_anatomy_*.json`, `results/t3_iter47_domain_residual_audit_*.json` | T3 residuals are systematic and anatomical, not random model noise. |
| `fig10_external_transportability_context.png` | FoG-STAR, COPS, TLVMC/DeFOG, PDFE external rows with Track A/B/C labels and “external-only” badges | external zero-shot JSONs, `results/external_result_claim_labeling_audit_*.json` | External datasets support transportability context only, not internal headline updates. |

Figures I would avoid:
`MAE-only leaderboard`, generic sensor schematic, raw ablation table as a plot, giant spaghetti learning curves, and any figure that mixes pre-audit and post-audit numbers without a visible status label.

---

**2. Validation Rules**
Add a dedicated claim-audit script, e.g. `paper_claims_audit.py`, and make `render_current_paper.py` call it before writing `CURRENT_PAPER.html`.

Required checks:

1. **Typed Claim Ledger**
Create `results/current_paper_export/claim_ledger.json` with:
`claim_id`, `target`, `metric`, `value`, `unit`, `model`, `protocol`, `N`, `cohort`, `role`, `source_artifact`, `source_sha256`, `paper_locations`, `figure_locations`.

2. **Numeric Claim Extraction**
Regex-extract all numeric claims from `paper.md`:
CCC, MAE, r, CI, frac>0, N, seed count, p-value, AUROC, widths, deltas.

Every extracted number must be one of:
`ledger_claim`, `citation_literature`, `historical_context`, `method_parameter`, `dataset_descriptive`.

Unclassified numbers fail.

3. **Role-Label Consistency**
Each claim has exactly one role:
`canonical`, `strongest_candidate`, `sensitivity`, `historical_pre_audit`, `target_contaminated`, `external_only`, `diagnostic_only`, `oracle_non_deployable`.

Fail if:
`0.7366` appears near “canonical replacement”.
`0.5227`, `0.3948`, `0.341`, `0.776`, `0.868`, `6.89` appear without historical/retracted/target-contaminated context.
`0.4281` appears without `complete33`, `N=88`, and `sensitivity-only`.

4. **One-Data-Source Rule**
For each `claim_id`, there must be one primary source artifact. Do not allow the same headline number to be sourced sometimes from `CLAUDE.md`, sometimes from `paper.md`, sometimes from old JSON.

5. **Figure Annotation Match**
Every number printed in figure labels, legends, titles, and captions must appear in `claim_ledger.json`.
The generated figure script should emit `results/current_paper_export/figure_claims.json`; audit compares that to the ledger.

6. **Cohort-N Consistency**
Examples:
`T1_iter12` must be `N=94`.
`T1_iter34` must be `N=93`.
`T3_iter47` must be `N=95`.
`complete33` must be `N=88` and sensitivity-only.
`T1 item18` companion caveat must preserve the N mismatch if discussed.

7. **Protocol Compatibility**
Fail if a paragraph directly compares metrics without compatible protocol labels:
LOOCV vs 5-fold screen vs held-out vs LOSO vs external zero-shot.
Pre-audit held-out MAE cannot be ranked against post-audit LOOCV CCC as model superiority.

8. **Artifact Existence And Hash**
Every ledger source path must exist under `/home/fiod/medical/results/` or be an explicitly allowed source file like `CLAUDE.md`.
Record SHA256 and mtime in the ledger.

9. **OOF Recompute Check**
For core claims, recompute CCC/MAE from OOF arrays or per-subject JSON, not from stored summary fields.
This should cover:
`T1 iter12`, `T1 iter34`, `T3 iter47`, T3 LOSO, and any paired-bootstrap figure.

10. **Forbidden Semantic Context**
Current forbidden snippets are too literal. Add regex/context rules:
`deployment-ready`, `clinical utility`, `breakthrough`, `solves`, `state of the art`, `held-out test set` within ±200 chars of pre-audit numbers unless also near `historical`, `pre-audit`, `leakage`, or `not deployment`.

11. **Snippet Manifest Generation**
Do not hand-edit `REQUIRED_SNIPPETS` in `render_current_paper.py`.
Generate `results/current_paper_export/required_snippets.json` from the ledger and known policy guards, then let renderer consume it.

---

**3. Workflow Orchestration**
Cleanly parallelizable phases:

1. Truth extraction can run in parallel:
`CLAUDE.md`/`AGENTS.md` policy extraction, result artifact scan, current `paper.md` claim extraction, current manifest analysis.

2. Figure data discovery can run in parallel:
T1 OOF discovery, T3 OOF discovery, per-item artifact discovery, external transportability artifact discovery, leakage audit artifact discovery.

3. External tri-CLI review parallelizes cleanly:
`codex` for academic prose, `gemini` for scientific narrative, `opencode`/Kimi for cautionary structure.

4. Figure rendering can parallelize per figure after the claim ledger is locked.

Irreducible serial dependencies:

1. Claim ledger must be built before edits and figures.
2. Figure scripts must be generated before figure captions are finalized.
3. `paper.md` edits must precede `render_current_paper.py`.
4. Rendering must precede external review if reviewers inspect `CURRENT_PAPER.html`.
5. Feedback triage must precede final render.
6. Final validation must be the last step.

Where Nature reviewers will catch corner cutting:

1. Mixing LOOCV, LOSO, held-out, and external zero-shot without visual/status separation.
2. Presenting iter34 as canonical after explicitly describing N=93, auxiliary-label, and P2 caveats.
3. Using MAE as the headline for total UPDRS-III while CCC is the primary post-audit metric.
4. Showing external datasets as validation of internal T3 performance.
5. Stating “no leakage” when P2 is borderline; say “no evidence of transductive leakage under P1/P4, with P2 indicating OOD fragility.”
6. Omitting multiple-comparisons correction for the iter33/iter34 family.
7. Treating oracle T3 bounds as deployable model performance.
8. Not distinguishing clinical-augmented T3 from IMU-only deployment.

---

**4. Nature Framing Risks**
Reject-and-resubmit phrasing to ban:

1. “Deployment-ready UPDRS-III prediction”
Use: “strict-inductive cautionary benchmark.”

2. “T3 performance approaches clinical utility”
Use: “valid-range T3 remains below a deployment-grade transportability threshold.”

3. “Iter34 improves the canonical T1 benchmark”
Use: “iter34 is the strongest candidate; iter12 remains the canonical floor.”

4. “WearGait predicts total UPDRS-III”
Use: “WearGait partially predicts total UPDRS-III, with residuals dominated by non-gait/upper-limb burden.”

5. “External validation confirms generalization”
Use: “external zero-shot rows show partial transportability and protocol dependence.”

6. “LOSO CCC = 0.150” without site directions and target version.
Must state valid-range iter47 and site directions from the correct artifact.

7. “Perfect T1 would bound T3 at 0.683” without explaining oracle/deployability.
Bound D is theoretical/oracle framing, not attainable model evidence.

8. “P2 soft fail is not leakage” too strongly.
Better: “P2 is interpreted as OOD fragility because P1/P4 and canary/library gates do not support a transductive leak, but it remains a candidate caveat.”

Key omissions reviewers will punish:

1. No explicit target-construction correction narrative for all-missing rows and invalid `9/9` codes.
2. No statement that `dst_*` walkway distillation is not fold-local and is sensitivity-checked.
3. No multiple-comparisons paragraph for iter33/iter34.
4. No cohort-size/LOOCV variance limitation.
5. No distinction between clinical-augmented and sensor-only deployment.
6. No current-vs-historical claim legend in tables and figures.

---

**5. Skill Structural Choices**
I would not make this three independent skills. Make one orchestrator skill plus concrete repo scripts.

Recommended layout:

`/home/fiod/medical/.claude/skills/paper-update/SKILL.md`
Top-level workflow, policies, phase order, tri-CLI prompts, acceptance criteria.

`/home/fiod/medical/paper_claims_audit.py`
Single robust script that builds the claim ledger, extracts paper claims, checks consistency, and writes `results/current_paper_export/claim_audit.json`.

`/home/fiod/medical/paper_figures.py`
Single robust figure pipeline that generates all figures and `figure_claims.json`.

`/home/fiod/medical/render_current_paper.py`
Keep as renderer, but make it consume claim-audit output instead of owning long hard-coded snippet lists.

Pros of one skill:
Simpler operator UX.
One source of workflow truth.
Less risk that sub-skills drift from current PD-IMU policy.
Matches repo preference for simple, robust scripts.

Pros of sub-skills:
Reusable if you maintain multiple manuscripts.
Cleaner mental separation of figures vs claim audit vs prose review.

Cons of sub-skills:
Higher drift risk.
More places to update when canonicals change.
Harder to enforce a single locked claim ledger.

Best compromise:
One skill, three scripts. Do not split into sub-skills unless another repo needs the same claim-audit machinery.

---

**Concrete Implementation Changes To The Brief**
Add these requirements:

1. The skill must hard-fail on contradictions between user-provided numbers and artifact/`AGENTS.md`/`CLAUDE.md` truth, especially the T3 LOSO `0.150` vs `0.341` conflict.

2. The skill must produce:
`results/current_paper_export/claim_ledger.json`
`results/current_paper_export/claim_audit.json`
`results/current_paper_export/figure_claims.json`
`results/current_paper_export/review_responses.json`

3. The skill must never let `paper.md` be the source of a number. It is an output surface only.

4. The skill should require a “claim ledger locked” checkpoint before editing `paper.md`.

5. The final report should include:
claim deltas, figure deltas, source artifacts, failed/waived review suggestions, render status, and residual caveats.
